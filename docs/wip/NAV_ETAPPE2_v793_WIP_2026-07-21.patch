diff --git a/index.html b/index.html
index d7748b1..7043ebc 100644
--- a/index.html
+++ b/index.html
@@ -9695,21 +9695,23 @@ function ArbeitsscheinView({arbeitsscheine,setArbeitsscheine,monteure,ww,curUser
   _react.useEffect.call(void 0, ()=>{if(canSync&&_authToken&&navigator.onLine)_juprowaFetchMonteure();},[canSync]);
   /* v3.9.792 Etappe 2: NUR die Sub-Tabs (liste/kalender/dispo/qrscan) in die History — "form" ist Detail (Etappe 3). */
   const _asSubValid=["liste","kalender","dispo","qrscan"];
-  const [sub,setSub]=_react.useState.call(void 0, function(){var _st;try{_st=(history.state||{}).sub;}catch(_e0){}return _navSubResolve(_st,'',_asSubValid,'liste');});
-  _react.useEffect.call(void 0, function(){if(typeof _regSubView==='function')_regSubView(setSub,function(t){return _asSubValid.indexOf(t)>=0;});return function(){if(typeof _unregSubView==='function')_unregSubView();};},[]);/* v3.9.792 Etappe 2: aktiven Sub-Tab registrieren + im laufenden Eintrag vermerken (nur Nav-Subs) */
+  const [sub,_setSubRaw]=_react.useState.call(void 0, function(){var _st;try{_st=(history.state||{}).sub;}catch(_e0){}return _navSubResolve(_st,'',_asSubValid,'liste');});
+  /* v3.9.793 (Etappe 2 Nachbesserung): Sub-Tab-Wrapper wie _setBxTab/_setCdTab — NUR die Nav-Subs (liste/kalender/dispo/qrscan) in die History; "form" = Detail (Etappe 3), kein Push. _setSubRaw = roher useState-Setter (auch fuer popstate-Restore registriert). */
+  const _setSub=function(t){_setSubRaw(t);if(_asSubValid.indexOf(t)>=0)_navPush({sub:t});};
+  _react.useEffect.call(void 0, function(){if(typeof _regSubView==='function')_regSubView(_setSubRaw,function(t){return _asSubValid.indexOf(t)>=0;});return function(){if(typeof _unregSubView==='function')_unregSubView();};},[]);/* v3.9.792 Etappe 2: aktiven Sub-Tab registrieren + im laufenden Eintrag vermerken (nur Nav-Subs) */
   /* v3.9.309: Kachel-Scroll — Klick auf Status-Kachel (Gesamt/Offen/Fertig/Einzel-Status) setzt
      Filter+Liste-Tab und scrollt zur Liste. Vorher: User klickt Kachel, Liste ist ausserhalb des
      Viewports → User denkt "macht nichts". Mobile-Buero-UX kritisch. */
   const _scheinListRef=_react.useRef.call(void 0, null);
   const _scrollToScheinListe=(status)=>{
-    setFilterStatus(status);setSub("liste");
+    setFilterStatus(status);_setSub("liste");
     setTimeout(()=>{const el=_scheinListRef.current;if(el&&el.scrollIntoView)el.scrollIntoView({behavior:"smooth",block:"start"});},80);
   };
   /* v3.9.82 Sprint-61.2: Filter-Persistence (localStorage) AS-Liste status/art/quick. */
   const _asFilterLoad=(k,d)=>{try{const v=localStorage.getItem("epk_as_"+k);return v||d;}catch(e){return d;}};
   const _asFilterSave=(k,v)=>{try{localStorage.setItem("epk_as_"+k,v||"");}catch(e){console.warn("[silent-ls]",e&&e.message||e);}};
   const [filterStatus,setFilterStatus]=_react.useState.call(void 0, ()=>{const f=window.__asFilter;if(f){delete window.__asFilter;return f;}return _asFilterLoad("filterStatus","alle");});
-  _react.useEffect.call(void 0, ()=>{if(window.__asFilter){setFilterStatus(window.__asFilter);setSub("liste");delete window.__asFilter;}},[]);
+  _react.useEffect.call(void 0, ()=>{if(window.__asFilter){setFilterStatus(window.__asFilter);_setSub("liste");delete window.__asFilter;}},[]);
   /* v3.9.489: __asOpenId Deep-Link — wenn Chef-Portal (oder anderer Caller) eine konkrete AS-ID setzt, öffne direkt das Edit-Formular sobald arbeitsscheine geladen sind. */
   _react.useEffect.call(void 0, ()=>{if(window.__asOpenId){const _aId=window.__asOpenId;const _a=arbeitsscheine.find(x=>x.id===_aId);if(_a){delete window.__asOpenId;openEdit(_a);}}},[arbeitsscheine]);
   _react.useEffect.call(void 0, ()=>{_asFilterSave("filterStatus",filterStatus);},[filterStatus]);
@@ -9718,8 +9720,8 @@ function ArbeitsscheinView({arbeitsscheine,setArbeitsscheine,monteure,ww,curUser
     if(!window.__asDeepLink||!arbeitsscheine.length)return;
     const nr=window.__asDeepLink;delete window.__asDeepLink;
     const found=arbeitsscheine.find(a=>(a.nummer||"").toUpperCase()===nr.toUpperCase());
-    if(found){setAsScannedAs(found);setSub("qrscan");}
-    else{setAsScanMsg("❌ Arbeitsschein "+nr+" nicht gefunden");setSub("qrscan");}
+    if(found){setAsScannedAs(found);_setSub("qrscan");}
+    else{setAsScanMsg("❌ Arbeitsschein "+nr+" nicht gefunden");_setSub("qrscan");}
   },[arbeitsscheine.length]);
   const [filterArt,setFilterArt]=_react.useState.call(void 0, ()=>_asFilterLoad("filterArt","alle"));
   _react.useEffect.call(void 0, ()=>{_asFilterSave("filterArt",filterArt);},[filterArt]);
@@ -9780,7 +9782,7 @@ function ArbeitsscheinView({arbeitsscheine,setArbeitsscheine,monteure,ww,curUser
     if(found){setAsScannedAs(found);setAsScanMsg("");asStopScan();}
     else{setAsScanMsg("❌ Kein Arbeitsschein gefunden: "+code);}
   };
-  const asStartScan=async()=>{setAsScannedAs(null);setAsScanMsg("");setAsManualCode("");setSub("qrscan");
+  const asStartScan=async()=>{setAsScannedAs(null);setAsScanMsg("");setAsManualCode("");_setSub("qrscan");
     try{const stream=await navigator.mediaDevices.getUserMedia({video:{facingMode:"environment",width:{ideal:640},height:{ideal:480}}});asStreamRef.current=stream;if(asVideoRef.current){asVideoRef.current.srcObject=stream;asVideoRef.current.play();}setAsScanning(true);
       if("BarcodeDetector"in window){const det=new BarcodeDetector({formats:["qr_code"]});
         const tick=async()=>{if(!asVideoRef.current||asVideoRef.current.readyState<2){asScanTimerRef.current=requestAnimationFrame(tick);return;}
@@ -9864,8 +9866,8 @@ function ArbeitsscheinView({arbeitsscheine,setArbeitsscheine,monteure,ww,curUser
   const sorted=_react.useMemo.call(void 0, ()=>[...filtered].sort((a,b)=>{const d=sortDir==="asc"?1:-1;let va=a[sortCol]||"";let vb=b[sortCol]||"";if(sortCol==="monteur"){va=(monteure.find(m=>m.id===va)||{}).n||va;vb=(monteure.find(m=>m.id===vb)||{}).n||vb;}if(sortCol==="nummer"){const na=parseInt((va+"").replace(/\D/g,""))||0;const nb=parseInt((vb+"").replace(/\D/g,""))||0;return (na-nb)*d;}if(sortCol==="aufgenommen"||sortCol==="terminVorschlag"||sortCol==="terminBestaetigt"){const da=va?new Date(va).getTime():0;const db=vb?new Date(vb).getTime():0;return (da-db)*d;}return va.toString().localeCompare(vb.toString(),"de")*d;}),[filtered,sortCol,sortDir,monteure]);
 
   /* v3.9.346: openNew entfernt — kein manueller Neu-Anlege-Einstieg mehr (Action-Bar-Button + Sub-Nav-Tab raus). Scheine kommen IMMER aus OFFA. openEdit bleibt unveraendert (Bearbeiten bestehender). */
-  const openEdit=a=>{setForm({...a});setEditId(a.id);setSub("form");};
-  /* v3.9.719 P1-c: vorlagenFill-Bus entfernt — er war der letzte Neu-Modus-Einstieg (setEditId(null)+setSub("form")).
+  const openEdit=a=>{setForm({...a});setEditId(a.id);_setSub("form");};
+  /* v3.9.719 P1-c: vorlagenFill-Bus entfernt — er war der letzte Neu-Modus-Einstieg (setEditId(null)+_setSub("form")).
      Arbeitsscheine entstehen ausschliesslich in OFFA; das Formular ist nur noch via openEdit (editId gesetzt) erreichbar. */
   /* v3.9.42 Sprint-27 F3 Tier-1: in-flight Guard gegen Doppel-Submit (Rapid-Click → multiple SQ.push für NEW Scheine → Duplikate). Window 800ms, useRef damit Re-Render sicher. */
   const _saveAsInFlightRef=_react.useRef.call(void 0, 0);
@@ -9922,9 +9924,9 @@ function ArbeitsscheinView({arbeitsscheine,setArbeitsscheine,monteure,ww,curUser
        Client-Stand NICHT konkurrierend server-aktualisierte Felder (z.B. juprowa_sync_at/_id durch
        Auto-Sync waehrend der Editor offen war). JSON.stringify-Vergleich deckt material/fotos-Arrays;
        fehlende Baseline ({}) → voller Diff = bisheriges Full-Row-Verhalten (kein Regress). */
-    const _asBody=(b=>{if(!('sigMA'in b)&&!('sigKunde'in b))return b;const c={...b,unterschriftMonteur:b.sigMA||"",unterschriftKunde:b.sigKunde||""};delete c.sigMA;delete c.sigKunde;return c;})(_isPush?{..._diff,push_pending:true,local_updated_at:new Date().toISOString()}:_diff);/* v3.9.418: AS-Signatur sigMA/sigKunde → reale Spalten unterschrift_monteur/_kunde. */if(Object.keys(_asBody).length>0)SQ.push({url:editId?"/api/arbeitsscheine/"+editId:"/api/arbeitsscheine",method:editId?"PUT":"POST",body:_asBody});/* v3.9.756 #31g-Konsolidierung: kein W2-Direktwrite + kein Sofort-Push mehr — Push via per-Schein-Debounce-Klammer (nach dem SQ-Flush -> genau 1 Push, kein Heilzyklus). Der lokale ↑-Clear passiert im Push-.then via window.__epkAsPushDone (v3.9.532-Verhalten erhalten). */if(_isPush&&navigator.onLine&&typeof _juprowaSchedulePush==="function")_juprowaSchedulePush(editId);try{window._epkVibrate&&window._epkVibrate("save");}catch(e){console.warn('[silent-dom]',e&&e.message||e);}_optionalChain([window, 'access', _116 => _116.__toast, 'optionalCall', _117 => _117(_shouldAutoClose?_se+" AS automatisch abgeschlossen":(editId?_se+" Schein aktualisiert":_se+" Schein angelegt"))]);setSub("liste");setEditId(null);/* v3.9.42 Sprint-27 F2 Tier-1: saveAs gibt _finalForm zurück — Caller (Speichern&PDF) bekommt auto-close-aware Form für genAsPdf statt stale `form`. */return _finalForm;};
+    const _asBody=(b=>{if(!('sigMA'in b)&&!('sigKunde'in b))return b;const c={...b,unterschriftMonteur:b.sigMA||"",unterschriftKunde:b.sigKunde||""};delete c.sigMA;delete c.sigKunde;return c;})(_isPush?{..._diff,push_pending:true,local_updated_at:new Date().toISOString()}:_diff);/* v3.9.418: AS-Signatur sigMA/sigKunde → reale Spalten unterschrift_monteur/_kunde. */if(Object.keys(_asBody).length>0)SQ.push({url:editId?"/api/arbeitsscheine/"+editId:"/api/arbeitsscheine",method:editId?"PUT":"POST",body:_asBody});/* v3.9.756 #31g-Konsolidierung: kein W2-Direktwrite + kein Sofort-Push mehr — Push via per-Schein-Debounce-Klammer (nach dem SQ-Flush -> genau 1 Push, kein Heilzyklus). Der lokale ↑-Clear passiert im Push-.then via window.__epkAsPushDone (v3.9.532-Verhalten erhalten). */if(_isPush&&navigator.onLine&&typeof _juprowaSchedulePush==="function")_juprowaSchedulePush(editId);try{window._epkVibrate&&window._epkVibrate("save");}catch(e){console.warn('[silent-dom]',e&&e.message||e);}_optionalChain([window, 'access', _116 => _116.__toast, 'optionalCall', _117 => _117(_shouldAutoClose?_se+" AS automatisch abgeschlossen":(editId?_se+" Schein aktualisiert":_se+" Schein angelegt"))]);_setSub("liste");setEditId(null);/* v3.9.42 Sprint-27 F2 Tier-1: saveAs gibt _finalForm zurück — Caller (Speichern&PDF) bekommt auto-close-aware Form für genAsPdf statt stale `form`. */return _finalForm;};
   const storno=async id=>{const _sPre=arbeitsscheine.find(x=>x.id===id);/* v3.9.535 Defense-in-Depth: Monteur darf abgeschlossenen Schein nicht stornieren (UI-Buttons sind schon gegated). */if(isMonteurRole&&!isAdmin&&_sPre&&!AS_GRP_OFFEN.includes(_sPre.scheinstatus)){if(window.__toast)window.__toast("🔒 Abgeschlossener Schein — Storno nur durch Büro/Admin","warn");return;}/* v3.9.81 SPRINT-60 F-1: native confirm to _confirmModal (EP-CI-Styling, Sprint 40/44 standard). */ if(!await _confirmModal("Arbeitsschein wirklich stornieren?",{variant:"danger"}))return;const s=arbeitsscheine.find(x=>x.id===id);const isJup=s&&s.juprowa_id;setArbeitsscheine(prev=>prev.map(a=>a.id===id?{...a,scheinstatus:"storniert",push_pending:isJup?true:a.push_pending}:a));SQ.push({url:"/api/arbeitsscheine/"+id,method:"PUT",body:isJup?{scheinstatus:"storniert",push_pending:true,local_updated_at:new Date().toISOString()}:{scheinstatus:"storniert"}});/* v3.9.756 #31g-Konsolidierung: Push via per-Schein-Debounce-Klammer (kein W2-Direktwrite, kein Sofort-Push). */if(isJup&&navigator.onLine&&typeof _juprowaSchedulePush==="function")_juprowaSchedulePush(id);if(s)_optionalChain([pushNotif, 'optionalCall', _118 => _118("as_status","Arbeitsschein storniert",s.nummer+" — "+s.kundName)]);_optionalChain([window, 'access', _119 => _119.__toast, 'optionalCall', _120 => _120("⛔ Arbeitsschein storniert")]);};
-  const deleteAs=async id=>{if(!canDo("as_delete",curUser))return;/* v3.9.613 #6: Handler an Render/canDo (as_delete=isA||isPL||isB) + DB-RLS (as_write=is_staff, inkl. buero) angleichen — vorher lokales isAdmin(=admin||PL) -> buero sah den Loesch-Button, Klick war No-Op (toter Button) */if(!await _confirmModal("Arbeitsschein endgültig löschen?",{variant:"danger"}))return;setArbeitsscheine(prev=>prev.filter(a=>a.id!==id));SQ.push({url:"/api/arbeitsscheine/"+id,method:"DELETE"});setSub("liste");setEditId(null);};
+  const deleteAs=async id=>{if(!canDo("as_delete",curUser))return;/* v3.9.613 #6: Handler an Render/canDo (as_delete=isA||isPL||isB) + DB-RLS (as_write=is_staff, inkl. buero) angleichen — vorher lokales isAdmin(=admin||PL) -> buero sah den Loesch-Button, Klick war No-Op (toter Button) */if(!await _confirmModal("Arbeitsschein endgültig löschen?",{variant:"danger"}))return;setArbeitsscheine(prev=>prev.filter(a=>a.id!==id));SQ.push({url:"/api/arbeitsscheine/"+id,method:"DELETE"});_setSub("liste");setEditId(null);};
   const verschieben=(id,d)=>{if(!d)return;const s=arbeitsscheine.find(x=>x.id===id);/* v3.9.535 Defense-in-Depth: Monteur darf Termin eines abgeschlossenen Scheins nicht verschieben (Grunddaten/Office). */if(isMonteurRole&&!isAdmin&&s&&!AS_GRP_OFFEN.includes(s.scheinstatus)){if(window.__toast)window.__toast("🔒 Abgeschlossener Schein — Termin nur durch Büro/Admin","warn");return;}const pp=s&&s.juprowa_id;setArbeitsscheine(prev=>prev.map(a=>a.id===id?{...a,terminBestaetigt:d,push_pending:pp?true:a.push_pending}:a));SQ.push({url:"/api/arbeitsscheine/"+id,method:"PUT",body:pp?{termin_bestaetigt:d,push_pending:true,local_updated_at:new Date().toISOString()}:{termin_bestaetigt:d}});/* v3.9.756 #31g-Konsolidierung: Push via per-Schein-Debounce-Klammer (kein W2-Direktwrite, kein Sofort-Push). */if(pp&&navigator.onLine&&typeof _juprowaSchedulePush==="function")_juprowaSchedulePush(id);};
   const updAs=(id,updates)=>{const s=arbeitsscheine.find(x=>x.id===id);
     /* v3.9.535 Monteur-Schutz (deckt ALLE updAs-Aufrufer ab: Listen-Inline-Termin/Status etc.):
@@ -10171,7 +10173,7 @@ function sharePdf(){
       , React.createElement('div', { className: "tab-bar", style: {borderBottom:"2px solid "+V.bd,marginBottom:14}}
         /* v3.9.346: Sub-Nav-Tab '✏️ Neu' (id:"form") entfernt — kein manueller Neu-Anlege-Einstieg mehr. Der form-View-Render-Block bleibt; er wird nur via openEdit (bestehender Schein) aktiviert. */
         /* v3.9.720 P1-d: auf isMob Label KLEIN unter dem Icon (column-Flex) statt ausgeblendet — das 🗓-Dispo-Icon ohne Text fand niemand. Alle Tabs gleich, Tap-Target >=44px. */
-        , [{id:"liste",i:"📋",l:"Liste"},{id:"qrscan",i:"📷",l:"QR Scan"},{id:"kalender",i:"📅",l:"Kalender"},...((["admin","buero","projektleiter"].indexOf(curUser.role)>=0)?[{id:"dispo",i:"🗓",l:"Dispo"}]:[])].map(t=>React.createElement('button', { key: t.id, onClick: ()=>{if(t.id==="qrscan")asStartScan();else{asStopScan();setSub(t.id);}_navPush({sub:t.id});/* v3.9.792 Etappe 2: Sub-Tab in die History */}, style: {display:"inline-flex",flexDirection:isMob?"column":"row",alignItems:"center",justifyContent:"center",gap:isMob?2:5,lineHeight:1.1,padding:isMob?"6px 9px":"10px 18px",border:"none",background:"transparent",color:sub===t.id?"#f97316":V.dm,fontWeight:sub===t.id?700:500,borderBottom:"3px solid "+(sub===t.id?"#f97316":"transparent"),marginBottom:-2,cursor:"pointer",fontFamily:"inherit",whiteSpace:"nowrap",minHeight:44}}, React.createElement('span',{style:{fontSize:isMob?17:15,lineHeight:1}},t.i), React.createElement('span',{style:{fontSize:isMob?9:13}},t.l)))
+        , [{id:"liste",i:"📋",l:"Liste"},{id:"qrscan",i:"📷",l:"QR Scan"},{id:"kalender",i:"📅",l:"Kalender"},...((["admin","buero","projektleiter"].indexOf(curUser.role)>=0)?[{id:"dispo",i:"🗓",l:"Dispo"}]:[])].map(t=>React.createElement('button', { key: t.id, onClick: ()=>{if(t.id==="qrscan")asStartScan();else{asStopScan();_setSub(t.id);}/* v3.9.793: Sub-Tab via _setSub-Wrapper (pusht in die History) */}, style: {display:"inline-flex",flexDirection:isMob?"column":"row",alignItems:"center",justifyContent:"center",gap:isMob?2:5,lineHeight:1.1,padding:isMob?"6px 9px":"10px 18px",border:"none",background:"transparent",color:sub===t.id?"#f97316":V.dm,fontWeight:sub===t.id?700:500,borderBottom:"3px solid "+(sub===t.id?"#f97316":"transparent"),marginBottom:-2,cursor:"pointer",fontFamily:"inherit",whiteSpace:"nowrap",minHeight:44}}, React.createElement('span',{style:{fontSize:isMob?17:15,lineHeight:1}},t.i), React.createElement('span',{style:{fontSize:isMob?9:13}},t.l)))
       )
 
       /* ═══ QR SCAN ═══ */
@@ -10207,7 +10209,7 @@ function sharePdf(){
           , React.createElement('div', { style: {display:"flex",gap:8,flexWrap:"wrap"}}
             , React.createElement('button', { onClick: ()=>{openEdit(asScannedAs);}, style: bpS}, "✏️ Bearbeiten" )
             , React.createElement('button', { onClick: ()=>genAsPdf(asScannedAs), style: {...bpS,background:"linear-gradient(135deg,#3b82f6,#2563eb)"}}, "📄 PDF" )
-            , React.createElement('button', { onClick: ()=>{asStopScan();setAsScannedAs(null);setAsScanMsg("");setSub("qrscan");setTimeout(asStartScan,300);}, style: {...bpS,background:"linear-gradient(135deg,#6b7280,#4b5563)"}}, "📷 Nächsten scannen" )
+            , React.createElement('button', { onClick: ()=>{asStopScan();setAsScannedAs(null);setAsScanMsg("");_setSub("qrscan");setTimeout(asStartScan,300);}, style: {...bpS,background:"linear-gradient(135deg,#6b7280,#4b5563)"}}, "📷 Nächsten scannen" )
           )
         )
       ))
@@ -10455,12 +10457,12 @@ function sharePdf(){
       /* v3.9.780 dead-code entfernt: OFFA-Import-Vorschau-Render-Zweig (manueller PDF-Import) — sub/setSub-Sub-Tab-State unberuehrt. */
 
       /* v3.9.719 P1-c: AS-Vorlagen Sub-Tab + Panel entfernt (Anlage nur in OFFA) */
-      , sub==="dispo"&&React.createElement(DispoPanel, {arbeitsscheine: arbeitsscheine, monteure: monteure, wpHistory: wpHistory, abs: abs, onUebernehmen: (scheinId,monteurId,iso,dauerMin,startHHMM)=>{/* v3.9.733 #20: Startzeit (terminZeit -> DB termin_zeit, was MonteurTafel v536 liest) reitet via terminBestaetigt->AK_TERMIN mit, kein eigener Push-Key. */updAs(scheinId,{terminBestaetigt:iso,monteur:monteurId,dauer:_dispoMinToHHMM(dauerMin),terminZeit:startHHMM||""});if(window.__toast)window.__toast("✓ Vorschlag übernommen — Termin "+(startHHMM||"")+" + Monteur gesetzt, Push läuft");}, onOpenSchein: (scheinId)=>{/* v3.9.718 P1-b: Chip/Warteliste -> Schein im Edit-Formular oeffnen (setSub liste + openEdit) */ setSub("liste");var _a=(arbeitsscheine||[]).find(x=>x.id===scheinId);if(_a){delete window.__asOpenId;openEdit(_a);}}, onDrop: (scheinId,iso,startHHMM,dauerMin)=>{/* v3.9.740 #22a: EIN Schreibgesetz — jede Dispo-Geste schreibt {terminBestaetigt,terminZeit,dauer} via updAs (byte-gleich zu ✓ Uebernehmen); terminBestaetigt+dauer sind Push-Felder -> Auto-Push zu OFFA, terminZeit reitet via AK_TERMIN mit. Nur der angefasste Schein (Kaskaden-Push-Verbot). */updAs(scheinId,{terminBestaetigt:iso,terminZeit:startHHMM||"",dauer:_dispoMinToHHMM(dauerMin)});if(window.__toast){var _s=(arbeitsscheine||[]).find(x=>x.id===scheinId);var _nr=(_s&&_s.nummer)||scheinId;var _wt=["So","Mo","Di","Mi","Do","Fr","Sa"][new Date(iso+"T00:00:00").getDay()];var _h=Math.round((dauerMin||0)/60*10)/10;window.__toast("✓ "+_nr+" → "+_wt+" "+iso.slice(8,10)+"."+iso.slice(5,7)+". "+(startHHMM||"")+", "+_h+"h — gespeichert & an OFFA gemeldet");}}, onToggleBlock: canSync?function(workerId,iso,grund){/* v3.9.760 #1 Teil 2: Sperren-Write auf dispo_blocks (EIGENE Tabelle — NICHT arbeitsscheine, Whitelist bleibt 5; KEIN Push-Feld, KEIN Juprowa/OFFA). grund===null -> DELETE, sonst INSERT. window.__dispoBlocks sofort spiegeln, damit der Panel-Recompute (_setTick) die Sperre sieht (Kap 0 + data-hardblock, #22a greift automatisch). r.ok-gated: nie grün ohne Erfolg; 42P01/404 -> sichtbarer Hinweis statt stillem Fehlschlag (v699). Direkter REST (kein SQ), weil r.ok synchron gebraucht wird. */return (async function(){try{if(grund===null){await _sbDeleteWhere("dispo_blocks","worker_id=eq."+encodeURIComponent(workerId)+"&datum=eq."+encodeURIComponent(iso));var _b=window.__dispoBlocks||{};delete _b[workerId+"_"+iso];window.__dispoBlocks=_b;if(window.__toast)window.__toast("🔓 Sperre aufgehoben");}else{await _sbPost("dispo_blocks",{worker_id:workerId,datum:iso,grund:grund});var _b2=window.__dispoBlocks||{};_b2[workerId+"_"+iso]=grund;window.__dispoBlocks=_b2;if(window.__toast)window.__toast("🚫 Tag gesperrt: "+grund);}return{ok:true};}catch(e){var _m=String(e&&e.message||e);if(/42P01|HTTP404|does not exist|could not find the table|relation .*dispo_blocks/i.test(_m)){if(window.__toast)window.__toast("⚠️ Sperren-Tabelle noch nicht angelegt (dispo_blocks) — SQL DISPO_BLOCKS_v1.sql ausführen","error",6000);}else{if(window.__toast)window.__toast("❌ Sperre nicht gespeichert: "+_m.slice(0,120),"error");}return{ok:false};}})();}:null, onRefreshScheine: ()=>_asPullFresh(setArbeitsscheine)})
+      , sub==="dispo"&&React.createElement(DispoPanel, {arbeitsscheine: arbeitsscheine, monteure: monteure, wpHistory: wpHistory, abs: abs, onUebernehmen: (scheinId,monteurId,iso,dauerMin,startHHMM)=>{/* v3.9.733 #20: Startzeit (terminZeit -> DB termin_zeit, was MonteurTafel v536 liest) reitet via terminBestaetigt->AK_TERMIN mit, kein eigener Push-Key. */updAs(scheinId,{terminBestaetigt:iso,monteur:monteurId,dauer:_dispoMinToHHMM(dauerMin),terminZeit:startHHMM||""});if(window.__toast)window.__toast("✓ Vorschlag übernommen — Termin "+(startHHMM||"")+" + Monteur gesetzt, Push läuft");}, onOpenSchein: (scheinId)=>{/* v3.9.718 P1-b: Chip/Warteliste -> Schein im Edit-Formular oeffnen (setSub liste + openEdit) */ _setSub("liste");var _a=(arbeitsscheine||[]).find(x=>x.id===scheinId);if(_a){delete window.__asOpenId;openEdit(_a);}}, onDrop: (scheinId,iso,startHHMM,dauerMin)=>{/* v3.9.740 #22a: EIN Schreibgesetz — jede Dispo-Geste schreibt {terminBestaetigt,terminZeit,dauer} via updAs (byte-gleich zu ✓ Uebernehmen); terminBestaetigt+dauer sind Push-Felder -> Auto-Push zu OFFA, terminZeit reitet via AK_TERMIN mit. Nur der angefasste Schein (Kaskaden-Push-Verbot). */updAs(scheinId,{terminBestaetigt:iso,terminZeit:startHHMM||"",dauer:_dispoMinToHHMM(dauerMin)});if(window.__toast){var _s=(arbeitsscheine||[]).find(x=>x.id===scheinId);var _nr=(_s&&_s.nummer)||scheinId;var _wt=["So","Mo","Di","Mi","Do","Fr","Sa"][new Date(iso+"T00:00:00").getDay()];var _h=Math.round((dauerMin||0)/60*10)/10;window.__toast("✓ "+_nr+" → "+_wt+" "+iso.slice(8,10)+"."+iso.slice(5,7)+". "+(startHHMM||"")+", "+_h+"h — gespeichert & an OFFA gemeldet");}}, onToggleBlock: canSync?function(workerId,iso,grund){/* v3.9.760 #1 Teil 2: Sperren-Write auf dispo_blocks (EIGENE Tabelle — NICHT arbeitsscheine, Whitelist bleibt 5; KEIN Push-Feld, KEIN Juprowa/OFFA). grund===null -> DELETE, sonst INSERT. window.__dispoBlocks sofort spiegeln, damit der Panel-Recompute (_setTick) die Sperre sieht (Kap 0 + data-hardblock, #22a greift automatisch). r.ok-gated: nie grün ohne Erfolg; 42P01/404 -> sichtbarer Hinweis statt stillem Fehlschlag (v699). Direkter REST (kein SQ), weil r.ok synchron gebraucht wird. */return (async function(){try{if(grund===null){await _sbDeleteWhere("dispo_blocks","worker_id=eq."+encodeURIComponent(workerId)+"&datum=eq."+encodeURIComponent(iso));var _b=window.__dispoBlocks||{};delete _b[workerId+"_"+iso];window.__dispoBlocks=_b;if(window.__toast)window.__toast("🔓 Sperre aufgehoben");}else{await _sbPost("dispo_blocks",{worker_id:workerId,datum:iso,grund:grund});var _b2=window.__dispoBlocks||{};_b2[workerId+"_"+iso]=grund;window.__dispoBlocks=_b2;if(window.__toast)window.__toast("🚫 Tag gesperrt: "+grund);}return{ok:true};}catch(e){var _m=String(e&&e.message||e);if(/42P01|HTTP404|does not exist|could not find the table|relation .*dispo_blocks/i.test(_m)){if(window.__toast)window.__toast("⚠️ Sperren-Tabelle noch nicht angelegt (dispo_blocks) — SQL DISPO_BLOCKS_v1.sql ausführen","error",6000);}else{if(window.__toast)window.__toast("❌ Sperre nicht gespeichert: "+_m.slice(0,120),"error");}return{ok:false};}})();}:null, onRefreshScheine: ()=>_asPullFresh(setArbeitsscheine)})
 
       , sub==="form"&&(React.createElement('div', { className: "epk-tab-fade", key: "as-form" }
         , React.createElement('div', { style: {display:"flex",justifyContent:"space-between",alignItems:"center",marginBottom:12,flexWrap:"wrap",gap:8}}/* v3.9.250 Mobile: Header umbrechen — Titel + Juprowa/Push-Badges + Buttons ragten sonst über den Rand */
           , React.createElement('div', { style: {fontSize:16,fontWeight:800,minWidth:0,wordBreak:"break-word"}}, editId?"✏️ Schein bearbeiten":"📋 Neuer Arbeitsschein", " " , React.createElement('span', { style: {color:"#f97316",fontFamily:mono}}, form.nummer), form.juprowa_id&&React.createElement('span', {style:{fontSize:10,color:"#06b6d4",marginLeft:8,fontWeight:400,verticalAlign:"middle"}},"🔗 Juprowa #",form.juprowa_id, form.juprowa_sync_at&&React.createElement('span',{style:{color:V.dm,marginLeft:4}},"Sync: ",window._fmtStampLocal(form.juprowa_sync_at))), form.push_pending&&React.createElement('span',{style:{fontSize:10,color:"#f59e0b",marginLeft:8,fontWeight:600,verticalAlign:"middle"}},"⬆ Push ausstehend"), form.last_push_at&&!form.push_pending&&React.createElement('span',{style:{fontSize:10,color:_okG(COLORS.SUCCESS),marginLeft:8,fontWeight:400,verticalAlign:"middle"}},"✓ Gepusht: ",window._fmtStampLocal(form.last_push_at)), form.push_error&&React.createElement('span',{style:{fontSize:10,color:COLORS.ERROR,marginLeft:8,fontWeight:400,verticalAlign:"middle"}},"❌ ",form.push_error.substring(0,40)), _isVerwaist(form)&&React.createElement('span',{style:{fontSize:10,color:"#b45309",marginLeft:8,fontWeight:700,verticalAlign:"middle"}},"⚠ in OFFA prüfen"))
-          , React.createElement('div',{style:{display:"flex",gap:6,alignItems:"center"}}, canSync&&editId&&form.juprowa_id&&form.push_pending&&React.createElement('button',{onClick:()=>doSinglePush(editId),disabled:jupPushing,style:{...bpS,background:jupPushing?"#6b7280":"linear-gradient(135deg,#f59e0b,#d97706)",fontSize:12,padding:"6px 12px",opacity:jupPushing?.7:1}},jupPushing?"⏳...":"☁️↑ Push"), React.createElement('button', { onClick: ()=>{setSub("liste");setEditId(null);}, style: bsS()}, "✕ Abbrechen" ))
+          , React.createElement('div',{style:{display:"flex",gap:6,alignItems:"center"}}, canSync&&editId&&form.juprowa_id&&form.push_pending&&React.createElement('button',{onClick:()=>doSinglePush(editId),disabled:jupPushing,style:{...bpS,background:jupPushing?"#6b7280":"linear-gradient(135deg,#f59e0b,#d97706)",fontSize:12,padding:"6px 12px",opacity:jupPushing?.7:1}},jupPushing?"⏳...":"☁️↑ Push"), React.createElement('button', { onClick: ()=>{_setSub("liste");setEditId(null);}, style: bsS()}, "✕ Abbrechen" ))
         )
         /* v3.9.533: Hinweis-Banner wenn Grunddaten für Monteur gesperrt (abgeschlossener Schein). */
         , _asMtLocked&&React.createElement('div',{style:{padding:"8px 12px",marginBottom:10,borderRadius:8,border:"1px solid "+COLORS.WARNING,background:_dark?"rgba(234,179,8,.10)":"rgba(234,179,8,.08)",fontSize:12,color:_dark?"#fbbf24":"#a16207",fontWeight:600}},"🔒 Dieser Schein ist abgeschlossen ("+((AS_STATUS[_asAuthStatus]||{}).l||_asAuthStatus)+"). Du kannst nur noch Durchgeführte Arbeiten, Notizen, Dauer und die Unterschrift ändern — Grunddaten sind gesperrt.")
@@ -10573,7 +10575,7 @@ function sharePdf(){
             , React.createElement('div', { style: {display:"flex",gap:6,flexWrap:"wrap",marginBottom:10}}
               , React.createElement('button', { onClick: saveAs, className: "epk-btn-hover", style: {...bpS,flex:1}}, "💾 " , editId?"Aktualisieren":"Speichern")
               , editId&&React.createElement('button', { onClick: ()=>genAsPdf(form), style: {...beS,flex:1}}, "📄 PDF" )
-              , editId&&form.scheinstatus!=="storniert"&&!_asMtLocked/* v3.9.534: Monteur darf abgeschlossenen Schein nicht stornieren */&&React.createElement('button', { onClick: ()=>{storno(editId);setSub("liste");}, style: bdS}, "⊘ Storno" )
+              , editId&&form.scheinstatus!=="storniert"&&!_asMtLocked/* v3.9.534: Monteur darf abgeschlossenen Schein nicht stornieren */&&React.createElement('button', { onClick: ()=>{storno(editId);_setSub("liste");}, style: bdS}, "⊘ Storno" )
               , editId&&canDo("as_delete",curUser)&&React.createElement('button', { onClick: ()=>deleteAs(editId), style: {...bdS,fontSize:11}}, "🗑️")
             )
           )
