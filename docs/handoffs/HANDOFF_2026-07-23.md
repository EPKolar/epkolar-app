# Handoff 23.07.2026 — Lösch-/Ladepfad-Fixes (v3.9.820–823) + Datenvorfall

## Ausgangslage
Sebastian meldete: ein in der App gelöschter Mitarbeiter (Aliti) lag weiter in der DB.
Chat-Claude verifizierte per Supabase-SQL: `workers`-Zeile vorhanden, `active=1`, kein Austritt;
kein Login; 0 AS / 0 time_entries / 0 absences; **14 weekplan_rows + 1 fahrzeuge.fahrer** referenzierten ihn.

## Vier Bugs, alle derselben Familie (Fehler wird verschluckt, UI zeigt Altstand)

- **v3.9.820 `delMonteur`**: `setMonteure(filter)` optimistisch + `SQ.push(DELETE)` fire-and-forget.
  PostgREST liefert bei RLS-gefiltertem DELETE **204 = ok mit 0 Zeilen** → `_sbDelete` wirft nicht →
  stille Divergenz.
  **FIX:** awaited DELETE + `Prefer: return=representation`, lokal nur bei **genau 1 Zeile**,
  0 Zeilen → Ablehnungs-Toast. `workers` in `_RLS_SILENT_DENIAL_LABELS`.
  Dazu: Austritts-Filter in Dispo-`feld` + WeekPlan-`fieldMA`; `P028` → `JUPROWA_RETIRED`.

- **v3.9.821 `delUser`**: identisches Muster **inkl. BEDINGUNGSLOSEM Erfolgs-Toast**. Gleicher Fix.
  Dazu: Austritts-Filter in ZeiterfassungView-`fieldMA` + AdminPanel-`_noLogin`.

- **v3.9.822 ROOT CAUSE**: `monteure` wurde auf **ZWEI Wegen** gesetzt, nur **EINER** mappte
  (Cache-Pfad roh aus IndexedDB, Server-Pfad gemappt). Gecachte Objekte ohne `austritt` →
  Filter sah `undefined` → „kein Austritt" → MA blieb sichtbar.
  **FIX:** ein einziger PURE `_mapWorker` für beide Wege (idempotent); `workers`-Fetch scheitert nicht
  mehr still (`console.error` + `window.__epkWorkerLoadErr` + Warn-Toast, **Cache bleibt Fallback**).

- **v3.9.823 UI**: Sektion **„Ehemalige (N)"** (default zugeklappt, EIN Zeilen-Renderer)
  + **Austritts-Badge für ALLE Rollen** (war für Admins unsichtbar: dort nur `<input type=date>`).

## Datenvorfall (wichtig für Nachfolge-Session)
Beim Aufräumen wurden **w3 (Cracana)** UND **mqyxfca35x6i (Aliti)** **HART** aus `workers` gelöscht.
Cracana hatte Lohn-/Projekthistorie: **8 time_entries = 53 h (10.–24.03.2026)**, **9 absences Krankenstand
(23.06.–03.07.2026)**, **1 AS S075314**, ~10 weekplan_rows.
Es gibt **KEINE Foreign Keys** auf `workers` → das DELETE lief klaglos durch, die Daten verwaisten.
Beide wurden mit **IDENTISCHER ID** wieder angelegt → alle Verweise greifen wieder
(Verweis-Check: **0** verwaiste Zeiteinträge / Abwesenheiten / Scheine / Fahrer).

**UNWIEDERBRINGLICH VERLOREN** (nur per Supabase-**PITR** rückholbar):
`stundensatz` (steht jetzt **0**), `SVNR`, `eintritt`, Telefon, Adresse **beider**.
**Wirkung `stundensatz=0`:** die Kalkulation prüft `>0` und fällt auf `_PAUSCHAL_SATZ` zurück (verifiziert) —
**kein Rechenfehler**, aber Cracanas 53 h werden pauschal statt echt bewertet.

## Endstand
- Beide: `austritt='2026-07-22'` (**soft**), `workers=11`, live verifiziert.
- **Live 3.9.823 auf Pages bestätigt** (Chat-Claude, no-store-Fetch).
- **Entscheidung Sebastian: SOFT statt hart.** Zeitdaten/Projektdaten bleiben.
  Begründung: die `workers`-Zeile ist die **einzige Namensauflösung**; ohne sie zeigen
  BWB/Stundenzettel Stunden ohne Mitarbeiter.

## Offen (nach Priorität)
1. ~~**PITR-Entscheidung**~~ → **ERLEDIGT 23.07., siehe Nachtrag.** Entscheidung Sebastian: **kein PITR.**
2. ~~**Löschschutz in `delMonteur`**~~ → **GEBAUT + LIVE als v3.9.824**, siehe Nachtrag.
3. **FK-/`ON DELETE RESTRICT`-Konzept** für `workers` (DDL, Human-Run-Gate).
   **NICHT CASCADE** — das würde Zeiteinträge mitreißen.
4. **`.catch(()=>null)`-Inventur** im Boot-Bundle (u.a. `_kioskFieldWorkers`), Familie v599/v699/v822.
   Eigener systematischer Durchgang; offline-gewollte Fälle bewusst ausnehmen.
5. **Stufe B Namens-Snapshot** (`time_entries.worker_name` etc.) — **VERTAGT** bis echter Auslöser;
   vorher Aufbewahrungsfristen mit Lohnverrechner klären und FinkZeit-Ablöse abwarten
   (dann ist die Domänengrenze klar).
6. Restbefund aus v820-Review: dritter `fieldMA`-artiger Filter geprüft und in v821 mitgezogen —
   **nichts offen**.

## Merksatz
**Austritt = heute zählt als AKTIV** (letzter Arbeitstag). Als Test gepinnt. Nicht „aufräumen".

---

# CC-Endreport (Bau-Seite)

## Gebaut + gepusht
| Version | Commit | Inhalt | pytest |
|---|---|---|---|
| **3.9.820** | `665d60f` | `delMonteur` ehrlich (awaited DELETE + `return=representation` + 1-Zeilen-Guard, kein SQ-Weg, keine Offline-Queue); `workers` in `_RLS_SILENT_DENIAL_LABELS`; Austritts-Filter in Dispo-`feld` (`_heute` vor `feld` gezogen → now-treu) + WeekPlan-`fieldMA`; `P028` aus `JUPROWA_WORKER_MAP` → `JUPROWA_RETIRED` (P026/Kiener unberührt) | 1995 / 13 |
| **3.9.821** | `db40014` | `delUser` ehrlich (gleiches Muster, Erfolgs-Toast erst nach bestätigtem Delete); `users` in `_RLS_SILENT_DENIAL_LABELS`; Austritts-Filter in ZeiterfassungView-`fieldMA` (Geschäftsführer bewusst NICHT zusätzlich ausgeschlossen) + AdminPanel-`_noLogin`. **Auth-Pfade tabu** (nur `public.users`, per Test gepinnt) | 2002 / 13 |
| **3.9.822** | `664bd3f` | **Root-Cause-Fix**: PURE, idempotenter `_mapWorker` (window-exportiert, Feldliste byte-identisch zum Server-Mapper) für **beide** Ladewege; `workers`-Fetch loggt + setzt `window.__epkWorkerLoadErr`, einmaliger Warn-Toast bei Fehler/leer, Cache bleibt Fallback. Scope hart nur dieser Fetch | 2009 / 13 |
| **3.9.823** | `c171d81` | Mitarbeiter-Tab: Sektion „Ehemalige (N)" (default zugeklappt, kein Persist, kein Render bei 0, EIN Zeilen-Renderer für beide Blöcke) + Austritts-Badge in Liste **und** Admin-Detail; Nicht-Admin-Zweig byte-identisch | 2015 / 13 |

## Gates (je Version identisch gefahren)
`python scripts/node_check.py index.html` → **exit 0** · `python scripts/_bracket_check.py index.html` →
Baseline **`() -1`** gehalten · `node sql/_check_version.js` → Triple synced (APP_VERSION + SW_VER + CACHE_NAME) ·
voller `python -m pytest tests/ -q` grün · Browser-Mount (Playwright gegen `python -m http.server 8899`):
**0 JS-Errors**, `#root` gefüllt, SW_VER passend.

## Live-Verifikationen (nicht nur Tests)
- **v820**: `_ezHeuteISO()` = `2026-07-23` im Browser bestätigt.
- **v822**: Roh-Cache-Objekt `{name:'Cracana',role:…,austritt:'2026-07-22'}` → gemappt `austritt` erhalten;
  `_mapWorker(_mapWorker(x)) === _mapWorker(x)` **true**; Sichtbarkeits-Probe: **ungemappt → sichtbar (der Bug)**,
  **gemappt → unsichtbar (gefixt)**.
- **v823**: ganze Kette Roh-Cache → `_mapWorker` → Partition: Cracana → **Ehemalige**;
  ohne Austritt **und** „Austritt = heute" → **aktiv**.
- **DB**: Verweis-Check nach der Wiederherstellung **0/0/0/0** (Zeiteinträge/Abwesenheiten/Scheine/Fahrer).

## Bewusste Testanpassungen (obsolete Pins, ehrlich nachgezogen)
- `test_juprowa_monteur_v754::test_map_hat_kiener_aliti` → **`test_map_hat_kiener_p028_stillgelegt`**:
  der Pin „`'P028':'mqyxfca35x6i'` steht in der Map" ist durch den Auftrag **bewusst obsolet** — eine Map-Zeile
  auf einen gelöschten Worker würde beim Pull eine Zombie-Referenz schreiben. Neu gepinnt: P028 **nicht** in der
  Map, **in** `JUPROWA_RETIRED`; P026 unberührt.
- `test_v394_confirm_migration::test_delMonteur_uses_confirmmodal`: das 600-Zeichen-Regex-Fenster mit
  `}\s*;`-Terminator matchte den nun mehrzeiligen Body nicht mehr → Schnitt auf den echten Funktionsabschluss
  **und verschärft**: natives `confirm()` im Body jetzt **explizit verboten**.

## Neue Tests
`test_mitarbeiter_loeschen_v820.py` (7) · `test_deluser_austritt_v821.py` (7) · `test_mapworker_v822.py` (7) ·
`test_ehemalige_v823.py` (6) — u.a. 1-Zeilen-Guard **vor** lokalem Entfernen, kein `SQ.push` im Löschpfad,
Auth-Pfad-Tabu, `_mapWorker`-Idempotenz, Partition lückenlos/überschneidungsfrei inkl. Grenzfall „Austritt = heute".

## Gotcha für Folge-Sessions
Beim Python-`replace(...,1)`+`count()==1`-Bau kollidiert der **APP_VERSION-`/*`-Kommentar** mit einem
gleichlautenden **Code-Kommentar** (`v3.9.XXX <Titel>`) → Anker mit Untertitel/`(Paket X)` eindeutig machen.
Ist in v818 und v819 je einmal reingelaufen (Assert bricht **vor** dem Save ab → kein halber Stand).

---

# Nachtrag 23.07. — Punkt 1 entschieden, Punkt 2 gebaut (v3.9.824 live)

## Punkt 1 — PITR: ENTSCHIEDEN, kein Restore

**Entscheidung Sebastian: kein PITR. Die Felder werden neu eingetippt.** Damit entfällt das
Verfallsdatum ersatzlos.

Nachzutragen für `w3` (Cracana) und `mqyxfca35x6i` (Aliti):
`stundensatz`, `svnr`, `eintritt`, `phone`, `geb_dat`, `fs`, `fs_nr` — alle im Mitarbeiter-Tab
editierbar. `adresse`/`notfallkontakt` waren projektweit leer, da ist nichts verloren gegangen.

Befunde, die die Entscheidung getragen haben (vorher nicht dokumentiert):
- **`stundensatz` hat GENAU EINE Verwendung** — `_projKalkKosten` (Chef-Portal, kalkulatorische
  Projektkosten für Ampel + Budget-Karte, Fallback `_PAUSCHAL_SATZ`=85). **Nicht Lohn, nicht Faktura.**
- **Kiener, Lindhuber und Riedmann haben ebenfalls `stundensatz=0`** — 4 von 11 laufen ohnehin auf
  dem Pauschalfallback. „0" ist hier kein Ausreißer, sondern der Normalzustand.
- **`svnr` hat im Code null Konsumenten** — reines Eingabefeld. Source-of-Truth ist die Lohnverrechnung,
  nicht die App. Gleiches gilt für `eintritt`/`geb_dat`/`fs_nr`.
- **Kein zweiter Fundort in der DB** (`bescheinigungen.geb_dat`/`fs_nr` sind leer). Die `MONT`-Seed-
  Konstante in `index.html` ist **Demo-Müll**, nicht die Realdaten (z.B. Paschinger `geb_dat`
  1985-03-12 vs. real 1974-08-21) — **niemals** als Wiederherstellungsquelle verwenden.
- **PITR wäre ohnehin das falsche Werkzeug gewesen:** ein Supabase-Restore setzt das *gesamte Projekt*
  zurück, alle Daten seit 23.07. 08:00 wären weg. Der zerstörungsfreie Weg auf Pro wäre das
  **herunterladbare logische Tagesbackup** gewesen (Database → Backups, 7 Tage) — daraus die zwei
  Zeilen greifen, gezieltes UPDATE, kein Restore. Für künftige Fälle: **das ist der Weg**, nicht PITR.
  (Caveat: ist das PITR-Add-on aktiv, gibt es stattdessen *physische* Backups ohne Download.)

## Punkt 2 — Löschschutz `delMonteur`: v3.9.824, gepusht + live

| Version | Commit | pytest |
|---|---|---|
| **3.9.824** | `0e91961` | 2028 / 13 |

**Harter Block, kein „trotzdem löschen".** `delMonteur` zählt vor dem Löschen server-seitig die
Referenzen; bei > 0 wird der Löschvorgang abgebrochen und der Dialog zeigt die Zahlen. Der
Bestätigen-Knopf heißt **„Austrittsdatum setzen"** und öffnet via `setSel` den Mitarbeiter — der
Ausweg führt also zum vorgesehenen Weg statt ins Leere. Der normale Lösch-Confirm erscheint nur noch
bei **0** Referenzen (ein soeben vertippt angelegter Mitarbeiter bleibt löschbar).

Geprüfte Referenzquellen (`_WORKER_REF_DEFS`):
`time_entries.worker_id` · `absences.worker_id` · `arbeitsscheine.monteur` · `fahrzeuge.fahrer` ·
`users.monteur_id`

**FAIL-CLOSED:** neuer strikter Leser `_sbSelectStrict` wirft bei **jedem** Nicht-2xx, auch 401/403.
`_sbGet`/`_sbGetUsersSafe` liefern bei Auth-Fehlern `[]` — „leer = keine Referenzen" wäre hier exakt
der Silent-Denial-Fehler aus v820, nur mit Datenverlust als Folge. Kann nicht geprüft werden, wird
nicht gelöscht.

**`weekplan_rows` bewusst NICHT im Block.** Überschreibbare Planungsdaten (jsonb `z->Tag->ma`, per
PostgREST nur über eine 7-fach-OR-`cs`-Konstruktion greifbar); sonst wäre jeder je verplante
Mitarbeiter dauerhaft unlöschbar. Der Vorfall ist trotzdem vollständig gedeckt — **Aliti hängt an
`fahrzeuge.fahrer`** (TU-118HD), Cracana an Zeiteinträgen/Abwesenheiten/AS.

**Live-Probe im echten Bundle** (Browser-Mount, 0 JS-Errors, `#root` 99 KB, SW_VER 3.9.824) — die
puren Funktionen gegen die realen Vorfallszahlen:
- Cracana `{entries:8,entriesH:53,abs:9,as:1}` → Summe **18**, Text
  `• 8 Zeiteinträge (53 h)\n• 9 Abwesenheiten\n• 1 Arbeitsschein` (Singular korrekt)
- Aliti `{fz:1}` → Summe **1**, Text `• 1 Fahrzeug (Fahrer)` — hätte den Vorfall gestoppt
- `{}` / `undefined` / `{entries:'acht'}` → **0** (kein NaN, das `NaN>0`=false wäre ein stiller Durchlass)
- Deploy live bestätigt: `raw.githubusercontent` **und** `epkolar.github.io` auf **3.9.824**.

**Bewusste Testanpassung:** `test_confirm_modal::test_del_monteur_migrated`. Das Fenster
`\{[^}]*await _confirmModal\([^)]*Mitarbeiter` setzte voraus, dass zwischen Funktionsanfang und
erstem Dialog **kein `}`** steht — seit dem Referenz-Check (Arrow-Funktionen, Objektliterale) ist das
nicht mehr so. Schnitt jetzt auf den echten Funktionsabschluss (Muster wie
`test_v394_confirm_migration` seit v820), Absicht unverändert und um das Verbot von nativem
`confirm()` verschärft. Neu: `tests/test_loeschschutz_v824.py` (13).

## Was der Block NICHT leistet
- Er greift **nur in der App**. Ein DELETE per Supabase-Dashboard/SQL läuft weiterhin klaglos durch —
  dagegen hilft ausschließlich **Punkt 3 (FK / `ON DELETE RESTRICT`, DDL, Human-Run-Gate)**.
- `delUser` (Login löschen) ist unberührt und bleibt es — ein Login zu entfernen zerstört keine
  Namensauflösung.

---

# Nachtrag 2 — Punkt 3 gebaut (DB-Löschschutz, gestaged, Human-Run-Gate)

## `sql/WORKERS_DELETE_GUARD_v1.sql` — BEFORE-DELETE-Trigger statt Foreign Keys

**Datei gestaged, NICHT angewandt** (Human-Run-Gate). Verifiziert: nach dem Validierungslauf
`trigger_da=0, funktion_da=0` — auf Produktion liegt nichts.

**Warum Trigger, nicht FK.** Der Auftrag heißt „ON DELETE RESTRICT-Konzept, NICHT CASCADE" — also
die *Semantik* „Löschen blocken wenn referenziert". Echte FK-Constraints würden das leisten, aber
**zusätzlich jeden Kind-Write gegen `workers.id` prüfen**. Die App schreibt bei „kein Fahrer/Monteur"
den **Leerstring `''`**, nicht NULL (`VehicleView.addFz: fahrer:""`, AS-Formular `montId=""`). `''`
ist nicht NULL → ein FK würde diese INSERTs ablehnen (kein `workers.id=''`) und Fahrzeug-/Schein-
Anlage brechen. Das zu umgehen hieße alle Write-Pfade auf NULL umzubauen — breiter, riskanter App-
Eingriff. Ein `BEFORE DELETE`-Trigger auf `workers` erreicht dieselbe Semantik, fasst die Kind-Spalten
aber nicht an → `''` bleibt erlaubt, **keine App-Änderung nötig**.

**Deckt die volle Historie ab (20 Tabellen), nicht nur die App-5.** Als DB-seitige *letzte*
Verteidigungslinie (greift auch bei DELETE per SQL-Editor/Dashboard, gegen das die App nichts
ausrichtet) schützt der Trigger bewusst mehr: alle worker-referenzierenden Historie-/Lohn-/Compliance-/
Zuordnungstabellen. **Bewusst NICHT geschützt:** `dispo_blocks` + `weekplan_rows` (transiente
Planungsdaten, überschreibbar — dieselbe Begründung wie beim App-Block; sonst wäre jeder je verplante
MA dauerhaft unlöschbar). Die geschützte Liste steht als editierbares `VALUES`-Array im File — eine
Zeile streichen/ergänzen genügt.

**Preflight-Datenstand (23.07., read-only):** 0 Waisen in allen Spalten. Nur Leerstrings (die den
Trigger nicht stören). → **keine Datenbereinigung nötig**, der Trigger wirkt rein vorwärts.

**Validierung (Transaktion, zurückgerollt):** `delete from workers where id='w1'` →
`ERROR 23001: Mitarbeiter Norbert Paschinger (w1) kann nicht geloescht werden — es haengen noch Daten
daran: Zeiteintraege (9), Abwesenheiten (12), Arbeitsscheine (97), Fahrzeuge (Fahrer) (1), Logins (1),
Kompetenzen (7), Projekt-Zuordnungen (3). Bitte stattdessen ein Austrittsdatum setzen.` SQLSTATE 23001
= `restrict_violation`, semantisch exakt ON DELETE RESTRICT. Namensauflösung + Zahlen wie im App-Block.

**Wechselwirkung mit dem App-Block (v3.9.824):** Der App-Block ist die schnelle Vorderseite (Dialog mit
Zahlen, 5 Tabellen). Der Trigger ist die Hinterseite (alle 20). Feuert der Trigger bei einem App-DELETE
— nur möglich wenn ausschließlich eine der „kleinen" Tabellen referenziert und **keine** der App-5 —
fängt `delMonteur` die Exception und zeigt „Löschen fehlgeschlagen" (generisch, aber **kein stiller
Verlust**). Optionale spätere Angleichung: App-Block auf dieselbe Tabellenliste ziehen. Nicht Teil
dieses Files.

## FÜR SEBASTIAN — vor dem Ausführen zu bestätigen
1. **Ausführen:** Datei im Supabase SQL-Editor (Projekt `jiggujpruejkaomgxarp`) laufen lassen;
   Verifikations-Queries stehen am Dateiende (Trigger da? / Test-DELETE muss mit 23001 fehlschlagen).
2. **Zwei Ermessens-Tabellen** im Schutzumfang — falls du sie *nicht* blocken willst, jeweils die Zeile
   aus dem `VALUES`-Array streichen:
   - `worker_projects` (Projekt-Zuordnungen) + `worker_kompetenzen` (Kompetenzen) — reine Zuordnungen,
     die einen längst ausgeschiedenen Test-MA am ehesten „unlöschbar" machen könnten. **Empfehlung:
     drin lassen** — ein echter Austritt läuft ohnehin über `austritt`, nicht über Löschen.
   - `dispo_blocks` ist bereits **ausgenommen** (transient). Falls du es doch blocken willst: Zeile
     ergänzen (Muster im File).
