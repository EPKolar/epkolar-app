# Handoff 26.09.2026 — v3.9.956, drei Entscheidungen umgesetzt, Lauf wieder zu

**`origin/main` = `a4b50e5`.** Alle sieben Tore grün: `node_check` ·
`_bracket_check` `() -1 {} 0 [] 0` · Klammerbilanz gegen den Vorgänger
identisch · `_check_version` 3.9.956 dreifach synchron · md5 der 7
geschützten Funktionen unverändert · **pytest 3153 grün**, 11 skipped,
8 xfailed · `bestand.py` 118 Begriffe in 17 Gruppen.

## Zuerst lesen

1. `docs/ENTSCHEIDUNGEN-OFFEN.md` — **fünfzehn** Fragen jetzt, nicht dreizehn.
   Die zwei neuen sind **14** (vier Ansichten ohne Überschrift — vier Wörter
   von dir) und **15** (das Klammertor beurteilt 28 % der Datei).
2. `docs/ABSCHLUSSBERICHT_UI_UMBAU.md`, ab „Nachtrag 26.09.2026" — was gebaut
   wurde, was bewusst nicht, und die Vorher/Nachher-Messung.
3. `docs/befunde/SCHLUSSMESSUNG_v3.9.954.md` — die unabhängige Schlussmessung,
   527 Zeilen.
4. `docs/befunde/WEEKPLAN_UND_USERS.md` — WeekPlan-Geometrie und die
   `users`-Tabelle, gemessen statt vermutet.

## Was in dieser Etappe passiert ist

| Commit | Was |
|---|---|
| `bed6549` | v3.9.954 · `docs/BERECHTIGUNG_AUSWERTUNGEN.md` — nichts gebaut, nur aufgeschrieben |
| `11e365a` | v3.9.955 · VBautag auf `BP_MOB` — eine Mobilschwelle in der ganzen App |
| `a4b50e5` | v3.9.956 · D9 Seitenüberschriften + der Klammertor-Riegel |

Drei Punkte, alle drei fertig. Der Lauf ist danach **wieder geschlossen** —
keine weitere Stufe ohne neue Freigabe.

## Die Verhaltensänderung, die du wissen musst

**Zwischen 600 und 767 px zeigt das Bautagebuch ab jetzt die Desktop-Fassung**
statt der Handy-Fassung. Das betrifft ein Tablet im Hochformat. Gemessen bei
390, 640, 767 und 1440 px, vorher **und** nachher:

* bei 640 und 767 px ändert sich **genau eine** Größe, und sie wird besser:
  18 Textstellen verlassen den Bereich unter 12 px (33 → 15)
* **nichts bricht um, nichts geht verloren** — kein Knopf, kein Feld, keine
  neue Kürzung, kein neuer Roller, kein Tippziel unter 44 px
* bei 390 und 1440 px **null** Unterschiede, wie es sein muss

🔴 Der **erste** Messdurchgang meldete „null Unterschiede an allen vier
Breiten" und war wertlos: alle 29 `isMob`-Stellen in VBautag liegen im
Formular oder in einer Eintragskarte, und bei leerem Server mit geschlossenem
Formular kann die Änderung gar nicht sichtbar werden. Die Köder blieben stumm,
also lautete das Urteil *nicht gemessen* — nicht *kein Befund*. Der zweite
Durchgang lief mit geöffnetem Formular.

## Was nur du tun kannst

* **Vier Überschriften.** Wochenplanung, Zeiterfassung, Flotte, Startseite
  haben keine, und es gibt dort keinen geeigneten Text. Eine erfundene
  Überschrift sieht richtig aus und behauptet etwas, das niemand entschieden
  hat — deshalb steht dort nichts. Frage 14.
* **`/hooks` einmal öffnen.** Der git-add-Riegel feuert seit über zwanzig
  Commits nicht. Er ist korrekt eingetragen und mit `jq -e` geprüft; der
  Einstellungswächter beobachtet `.claude/` erst, wenn dort beim Sitzungsstart
  schon eine Datei lag. Was ungeschützt blieb: ein versehentliches
  `git add -A` hätte in jedem dieser Commits fremde Dateien mitgenommen. Es ist
  nicht passiert — jeder Commit nennt seine Dateien einzeln und die Dateizahl
  wurde gegengelesen. Aber das war Disziplin, kein Riegel.
* **Die drei OFFA-Scheine.** `anon` sieht wegen RLS nirgends eine Zeile, belegt
  mit Köder. Die fertige, rein lesende Abfrage steht in
  `docs/befunde/OFFA_SCHEINE.md`.
* **Soll der Klammer-Streicher neu gebaut werden?** Frage 15. Halber Tag, und
  das Ergebnis ist eine neue Grundlinie, die erst geprüft werden muss.

## Was gemessen und nicht gebaut wurde

* 🔴 **S-1:** `FahrzeugView` führt `☰` und `⊞` ohne `title` und ohne
  `aria-label`, bei 375, 390 **und** 1440 px. Kosten: zwei Attribute, kein
  Pixel. Nicht gebaut, weil der Lauf für drei Punkte offen war.
* Die **Projekt-Reiterzeile rollt auch bei 1440 px** (+158 px), nicht nur am
  Telefon. Die Entscheidung „bleibt rollbar" war nur für das Telefon begründet.
* **Drei der sieben Zusagen** des Laufs geben die eigenen Beschlüsse nicht her.
  Zwei nur dem Wortlaut nach (720-px-Tabelle, AS-Spalte bis 494 px) — aber eine
  Zusage muss „0 **außer diesen beiden, namentlich**" lauten, sonst wird beim
  nächsten Lauf der Riegel grün gemacht statt der Code repariert.
* **WeekPlan:** die 56 kleinen Schriften sind bestätigt und sind **zwei
  Familien**. Der Engpass ist nicht `width:130` (das sitzt in der
  Excel-Exportvorschau) und nicht `minWidth:800` (bei 1440 px inaktiv), sondern
  eine Überbestimmung der drei Streifen: `96 % + 72 px`, passt erst ab 1800 px.
  Drei Vorschläge mit Gegenmessung in `docs/befunde/WEEKPLAN_UND_USERS.md`.
* **`public.users` führt kein `austritt`** (18 Spalten gemessen, 20
  Ersatznamen geprüft, alle 400 `42703`). `workers.austritt` existiert,
  `public.monteure` nicht, kein FK zwischen beiden. Folge: `_maIstEhemalig` ist
  für jede `users`-Zeile immer `false` — ein Ausgetretener **mit** Login wird
  nicht gefiltert, einer **ohne** Login schon.

## Nicht eingecheckt, absichtlich

`_mess_stand_954.html` und `_mess_stand_955.html` in der Repowurzel — je 3,6 MB
eingefrorene Kopien, die Beweisstücke der Vorher/Nachher-Messung
(md5 `359be144…` und `24d8565d…`). Sie liegen untracked da; wenn du sie nicht
brauchst, können sie weg. Sieben MB doppeltes HTML in die Historie zu legen
schien mir der schlechtere Tausch.

## Die Riegel dieser Etappe

| Datei | Was er festhält |
|---|---|
| `test_eine_mobilschwelle_v955.py` | `BP_MOB` ist die einzige Mobilschwelle; die sechs `isTab`-Stellen **namentlich** mit ihrer Ansicht; Köder |
| `test_d9_seitenueberschriften_v956.py` | beide Bauprovisorien-Zustände tragen eine `h2` mit `margin:0`; jede **neue** Überschrift in den vier offenen Ansichten wird ROT |
| `test_klammertor_blindheit_v956.py` | gestrichener Anteil ≤ 73 %, kein **Backtick**-Treffer > 250.000 Zeichen |

`test_mobil_fundament_v932.py` wurde **verschärft**, nicht gelockert: es nennt
die sechs Tablet-Stellen jetzt einzeln statt sie zu zählen, und die zweite Zahl
dort **steigt** (28 → 29 umgestellte Stellen).

## Eine Regel für jeden, der hier schreibt

**In `index.html` gehört kein Backtick in einen Kommentar.** Diese Datei
zitiert in Kommentaren mit Backticks, und der Streicher in `_bracket_check.py`
setzt das Template-Literal-Muster vor das Kommentarmuster: ein Backtick schließt
das nächste offene Literal, und der Code dazwischen fällt aus der Bilanz. In
meinem Fall 375.741 Zeichen. Das Tor wurde rot — diesmal in der günstigen
Richtung.
