# Handoff 2026-07-20 — Session-Ende (Kontext voll)

**Arbeitsklon:** `C:\repos\epkolar-app`. Push `git push origin main`. Verify curl raw + github.io (~1 Min Lag).
**LIVE-HEAD:** **v3.9.765** (`203a3de`). **pytest 1802 grün.** Working tree clean.
**Pflicht-Gates je Push:** `python scripts/node_check.py index.html` (0) · `python scripts/_bracket_check.py index.html`
(`() -1`) · `node sql/_check_version.js` · voller `python -m pytest tests/ -q` **NACH dem Bump** · Version-Triple
(APP_VERSION + SW_VER + CACHE_NAME). Kein `?.` (optional chaining). Kommentar-Listenmarker **nie** `1)`/`2)`
(unbalanciertes `)` bricht `test_invariants` bracket-baseline −7) → `(1)`/`(2)` oder `1.`. Bump Zeile ~2854
(APP_VERSION-Chain) via Python-Script (zu groß für Edit).

## In dieser Session live gegangen (v3.9.755 → v3.9.765)
- **755** #31g Push-Queue Selbstheilung · **756** #31g-Konsolidierung (W2 stillgelegt → Whitelist 5, per-Schein-
  Debounce-Klammer `_juprowaSchedulePush`, Mount-Aufholer) · **757** #31a Monteur-Guard (vs public.workers) +
  #31b Pull-Echo-Schutz · **758** Aufholer-Wiring-Fix (App-Start statt tab-gated) · **759** #28d Vollbilanz +
  Dead-Code #8–13 · **760** #1 Teil 2 🚫-Sperr-Toggle (dispo_blocks-Write via Eltern-Prop) · **762/763** P3
  Toggle-Position/Kollision · **764** #28 Planungskern V2 (28a Fahrminuten + 28b Nachbarschafts-Bonus) ·
  **765** #19b Geo-Selbstnachzieh (Nominatim+OSRM → plz_geo/plz_distanz, CSP += OSRM-Host).
- **31g live-abgenommen** (S075354/S075353). **Smoke-Volllauf: 0 Bugs** (`docs/SMOKE_LOG_v758.md`, alle 4 Rollen;
  SM-01/SM-02 als kein-Bug geschlossen). **Register 0–31 im Wesentlichen komplett.**

## #19b RLS-Vorprüfung (READ-ONLY, via Supabase-MCP) — Ergebnis: Policies vorhanden UND ausreichend, KEIN SQL nötig
Beide Tabellen `public.plz_geo` + `public.plz_distanz` existieren, **RLS aktiv** (`relrowsecurity=true`, nicht forced),
je **2 PERMISSIVE Policies** (voller Wortlaut):

| Tabelle | Policy | cmd | Rollen | USING | WITH CHECK |
|---|---|---|---|---|---|
| `plz_geo` | `plz_geo_select_authed` | SELECT | `{authenticated}` | `true` | — |
| `plz_geo` | `plz_geo_write_staff` | **ALL** | `{authenticated}` | `is_staff()` | `is_staff()` |
| `plz_distanz` | `plz_distanz_select_authed` | SELECT | `{authenticated}` | `true` | — |
| `plz_distanz` | `plz_distanz_write_staff` | **ALL** | `{authenticated}` | `is_staff()` | `is_staff()` |

```sql
-- is_staff() (SECURITY DEFINER, STABLE):
SELECT EXISTS (SELECT 1 FROM public.users u
  WHERE u.auth_user_id = auth.uid() AND u.role IN ('admin','buero','projektleiter'));
```
**Deutung:** App-Client schreibt als Rolle `authenticated`; `*_write_staff` (cmd=ALL, WITH CHECK `is_staff()`) deckt
INSERT+UPDATE ab, sofern `is_staff()`=true = **admin/buero/projektleiter** = exakt die canSync-Rollen, die #19b
auslösen. Table-Grants: `authenticated` hat INSERT/UPDATE/SELECT (RLS ist der Filter). **→ #19b-Writes funktionieren
für Büro/PL/Admin.** SELECT für alle `authenticated` (inkl. monteur/lager_display — nur Lesen, Geo-Referenzdaten
unkritisch). `anon` per RLS default-deny (keine anon-Policy). **Keine RESTRICTIVE-Policies.** Kein
`sql/B_19b_geo_rls.sql`, kein Commit.

## Baseline vor Sebastians Live-Test (read-only count, Vorher-Stand)
- **`plz_geo` = 20 Zeilen**
- **`plz_distanz` = 171 Zeilen**
Nach dem #19b-Live-Test (Büro/PL/Admin öffnet Dispo → Selbstnachzieh im Hintergrund) sollten beide wachsen.

## OFFENE Live-Co-Verifikationen (mit Sebastian)
1. **#19b DB-Writes:** als Büro/PL/Admin die Dispo öffnen → Console `GEO NACHZIEH` → `plz_geo`/`plz_distanz` füllen
   sich (RLS lässt Staff-INSERT zu, verifiziert). Gegencheck: obige Counts steigen. Dann greift #28 mit echten
   Fahrminuten.
2. **#28b Office-Plan-Sicht:** kurzer Blick der Disponentin auf einen realen Wochenplan — 28b bündelt nahe PLZ
   (<15 Fahrmin) jetzt stärker auf einen Tag (gewollt, weniger Anfahrten).
3. **#1 Teil 2 Toggle-Klick:** Sperren/Entsperren klicken → `dispo_blocks`-DB-Gegencheck (INSERT/DELETE).

## OFFENER BEFUND — "Neu berechnen"-Button (Fix NICHT gebaut, wartet auf Freigabe)
`index.html:9653` — der Button ruft nur `_setTick(t=>t+1)` = **deterministischer Recompute mit unveränderten
Daten = sichtbar wirkungslos**. Gewünschter Fix:
1. onClick `_setTick` → **`_doDispoRefresh()`** (existiert `index.html:9406`, in DispoPanel; zieht `dispo_blocks`
   frisch → `window.__dispoBlocks` → tickt; Geste-Guard greift automatisch; read-only, v745 gewahrt).
2. Zusätzlich AS-Stand einmalig frisch ziehen über den **bestehenden 5-Min-Poll** (`_juprowaSync`). **HALT-Befund:**
   Es gibt **keinen sauberen Trigger aus DispoPanel** — `_juprowaSync` ist nicht window-exportiert und braucht
   `setArbeitsscheine` (Parent-State, in DispoPanel nicht verfügbar); `window.__doSync` ist der SQ-Flush, NICHT der
   AS-Pull. Ein sauberer Weg bräuchte eine **neue Eltern-Prop** (z.B. `onRefreshData`, die im Parent `_juprowaSync(
   null,setArbeitsscheine,true)` triggert) — das ist neues Wiring, kein „bestehenden Pfad wiederverwenden". Zudem:
   `_juprowaSync` PULLT (PATCHt arbeitsscheine mit OFFA-Daten) — als App←OFFA-Sync vertretbar, aber der Button säße
   in DispoPanel (v745 „kein Write im Panel-Body"). **→ Vor dem Bau entscheiden:** (a) nur Schritt 1 (dispo_blocks-
   Refresh + Recompute, sofort machbar, kein AS-Pull), ODER (b) Schritt 1 + neue `onRefreshData`-Prop für den
   AS-Pull (Parent triggert `_juprowaSync`, Panel bleibt schreibfrei). Test-Pin: onClick ruft `_doDispoRefresh`,
   nicht bloß `_setTick`.

---

## CC-BEFEHL für die nächste Session
> **„Neu berechnen"-Button-Fix bauen (Freigabe-Entscheid oben):** Standard = Variante (b) — Schritt 1
> (`_doDispoRefresh`) + neue Prop `onRefreshData` in DispoPanel, die der Parent (ArbeitsscheinView) mit
> `_juprowaSync(null,setArbeitsscheine,true)` (bestehender 5-Min-Poll-Pfad, einmalig) belegt → DispoPanel bleibt
> schreibfrei (v745), der Pull läuft im Parent. Button-onClick: `onRefreshData && onRefreshData(); _doDispoRefresh();`.
> Test-Pin (onClick ruft `_doDispoRefresh` + `onRefreshData`, nicht bloß `_setTick`), Signatur-Pins nachziehen
> (v715–718/v734/v760 pinnen die DispoPanel-Signatur — neue Prop ergänzen), Headless-Mount, volle Gates, Version-Bump.
> Falls Sebastian nur Variante (a) will: nur onClick→`_doDispoRefresh()`, kein AS-Pull, keine neue Prop.
> **Danach offen:** die 3 Live-Co-Verifikationen (oben) mit Sebastian; #18b (re-eval, „nicht möglich" laut v728);
> #6 E2-🚛-UI / #7 Büro-Extras A5 (brauchen Spec). Register sonst durch.

**Memory-Quellen:** `project_epkolar_dispo_register` (Register-Stand + Gate-Lektionen), `reference_epkolar_repo_path`,
`feedback_epkolar_pytest_io`. Supabase-MCP ist in dieser Session read-authorisiert (nur `SELECT` genutzt, TABU
gewahrt: kein DDL/DML, kein arbeitsscheine-/OFFA-/Auth-Write).

---

# Nachtrag — v3.9.766 gebaut & live (Commit `34b857a`)

**HEAD:** **v3.9.766** (`34b857a`, auf `origin/main` verifiziert). **pytest 1809 grün** (1802 + 7 neu).
Working tree clean. Version-Triple synced (APP_VERSION + SW_VER + CACHE_NAME).

## GEFIXT — "Neu berechnen" ist keine Attrappe mehr
`index.html` Dispo-Kopf: `onClick` von `_setTick(function(t){return t+1;})` auf **`_doDispoRefresh()`** (v761,
Z.~9407) umgestellt. Der zieht `dispo_blocks` frisch → `window.__dispoBlocks` → tickt **erst danach**. Der
Geste-Guard (`_gestureRef`/`_dragRef`) greift automatisch (Klick während Drag reisst keinen Chip aus der Hand),
der Pfad bleibt **READ-ONLY** — **v745-Garantie gewahrt** (kein Write im Panel-Body).

**Test `tests/test_dispo_neuberechnen_v766.py` (7 Pins):** Button ruft `_doDispoRefresh` · Button ruft **nicht**
mehr bloss `_setTick` (Regressions-Pin auf den Alt-Befund) · `_doDispoRefresh`-Rumpf zieht wirklich
`_sbGet("dispo_blocks")` + setzt `window.__dispoBlocks` + tickt · v745-readonly (kein `_sbPost/_sbPatch/_sbDelete/
updAs` im Rumpf) · Geste-Guard vorhanden · **kein `_juprowaSync` im Panel** · DispoPanel-Signatur unveraendert.

**Gates:** `node_check` 0 · `_bracket_check` `()` **-1** (Baseline), `{}` 0, `[]` 0 · `_check_version` 3.9.766 synced ·
voller pytest **nach** dem Bump 1809 gruen · kein optional chaining im Diff.

**Headless-DispoPanel-Mount — reproduzierbar OHNE Live-Login (neuer Hausweg):**
`python -m http.server 8777` auf den Arbeitsklon + Playwright-MCP → `ReactDOM.flushSync(()=>ReactDOM.createRoot(div)
.render(React.createElement(DispoPanel,{...Mock-Props})))`. Ergebnis: **Mount ohne Throw, 0 Console-Errors**, Button
gefunden, und der reale Klick (mit gestubbtem `_sbGet`) loest **`_sbGet("dispo_blocks")`** aus = **Verhaltens**-Beweis
der Verdrahtung, nicht nur Statik-Match. So laesst sich das Pflicht-Gate kuenftig ohne Sebastian-Login fahren.

## BEWUSST OFFEN GEBLIEBEN — AS-Stand-Frische beim Button-Klick (wartet auf Sebastian-Freigabe)
Der Auftrag lautete "bestehenden Pfad wiederverwenden, KEINEN neuen Fetch-Pfad bauen; wenn kein sauberer Trigger
existiert: halt + Befund melden". Nachgeprueft (nicht uebernommen):
- `_juprowaSync` ist eine **Top-Level-Funktion** (`index.html:3483`) und damit aus dem Panel lexikalisch **sehr wohl
  sichtbar** — der Blocker ist NICHT die Sichtbarkeit, sondern das **zweite Argument**.
- `setArbeitsscheine` existiert nur in **`ArbeitsscheinView`** (`index.html:9685`); **`DispoPanel`**
  (`index.html:9354`) bekommt es nicht (Signatur: `{arbeitsscheine,monteure,wpHistory,abs,onUebernehmen,
  onOpenSchein,onDrop,onToggleBlock}`).
- `window.__doSync` ist der SQ-Flush, **nicht** der AS-Pull.
→ Jeder Einmal-Trigger braeuchte eine **neue Eltern-Prop** = neues Wiring, kein Bestandspfad → **nicht gebaut**.

**Wenn Sebastian freigibt (Variante b), ist es ein Einzeiler an der Render-Stelle:** `index.html:10565` liegt
**innerhalb** von `ArbeitsscheinView`, dort ist `setArbeitsscheine` in Scope → Prop `onRefreshData:()=>_juprowaSync(
null,setArbeitsscheine,true)` ergaenzen, im Panel `onClick:function(){onRefreshData&&onRefreshData();
_doDispoRefresh();}`. **Dann mitziehen:** Signatur-Pins v715–718/v734/v760 **und** der neue
`test_dispopanel_signatur_unveraendert` + `test_kein_as_pull_im_panel` (letzterer pinnt aktuell bewusst das
Gegenteil), Headless-Mount, volle Gates. Der Pull laeuft im Parent → Panel bleibt schreibfrei (v745).
Aktuell haengt der AS-Stand weiter am bestehenden **5-Min-`_juprowaSync`-Poll**.

## Baseline-Counts (Wiederholung, vor Sebastians #19b-Live-Test)
- **`plz_geo` = 20 Zeilen**
- **`plz_distanz` = 171 Zeilen**
Beide muessen nach dem Live-Oeffnen der Dispo als Buero/PL/Admin wachsen — das ist der Gegencheck fuer die offene
#19b-DB-Write-Co-Verifikation (RLS ist geprueft, `*_write_staff` deckt INSERT/UPDATE fuer Staff, **kein SQL noetig**).

## Offen fuer die naechste Session
1. Die 3 Live-Co-Verifikationen mit Sebastian: **#19b DB-Writes** (Counts oben) · **#28b Office-Plan-Sicht** ·
   **#1-Teil-2 Toggle-Klick** (`dispo_blocks` INSERT/DELETE).
2. **Freigabe-Entscheid** zur AS-Stand-Frische oben (Variante b = Einzeiler + Prop, oder es bleibt beim 5-Min-Poll).
3. **#18b** (re-eval, laut v728 "nicht moeglich") · **#6** E2-🚛-UI · **#7** Buero-Extras A5 (beide brauchen Spec).
   Register 0–31 sonst durch.

**Deploy-Hinweis:** `raw.githubusercontent` zeigte direkt nach dem Push **3.9.766**, `github.io` (Edge) stand nach
~2 Min Polling noch auf 3.9.765 = normaler Pages-Build-Lag. **NICHT nachtriggern** (Throttle), HEAD ist kumulativ,
die Edge springt von selbst nach. Vor dem Live-Test kurz die Version im Footer pruefen.

---

# Nachtrag 2 — v3.9.767: AS-Frische am Button (Teil A) + nur genehmigte Abwesenheit (Teil B)

**HEAD:** **v3.9.767**. **pytest 1817 gruen.** Version-Triple synced. Working tree clean.

## SCHRITT-0-BEFUNDE (vor dem Bau gemeldet, beide entschieden)

**A — Ist der AS-Pull vom Push/Drain trennbar? JA.** `_juprowaSync` (Z.3483) ist kein reiner Pull: er faehrt
die OFFA-RPC `juprowa_fetch_worksheets`, schreibt via `_sbPatch`/`_sbPost` auf `arbeitsscheine` (+`activity_log`)
und ruft am Ende **`_juprowaDrainPending(10)`** = OFFA-Push. Ein Button, der `_juprowaSync` ruft, wuerde also
gequeuete Pushes flushen (TABU-Konflikt). **Aber Schritt 3 im Rumpf war bereits ein reiner DB-Pull**
(`_sbGet("arbeitsscheine")` -> `_mapArbeitsschein` -> `setArbeitsscheine`). Genau den braucht der Button.
**Umgesetzt:** herausgezogen als `_asPullFresh(setArbeitsscheine)`; `_juprowaSync` ruft **denselben Helper**
-> EIN geteilter Pfad, keine Dublette, 5-Min-Poll verhaelt sich unveraendert. **TABU bleibt unangetastet:
ein Klick loest KEINEN OFFA/Juprowa-Push aus** (per Test gepinnt: `_juprowaDrainPending`/`_juprowaPush`/
`juprowa_fetch_worksheets`/`_sbPatch`/`_sbPost` duerfen in `_asPullFresh` nicht vorkommen).

**B — Blockt beantragte Abwesenheit heute die Planung? JA.** `_dispoAbwAbzug` (Z.4883) hatte woertlich
"genehmigt+beantragt blocken, abgelehnt frei" und floss ueber `absAbz` in `kapAbzug` = echte Kapazitaet,
nicht nur Optik. Das Rausnehmen ist damit eine **inhaltliche Planungsaenderung** (die Dispo plant ab jetzt auf
Tage mit beantragtem, noch nicht genehmigtem Urlaub; wird der Urlaub genehmigt, muss umgeplant werden).
**Read-only-Gegenpruefung der Live-Daten (Supabase-MCP, nur SELECT):** `absences` = **110 genehmigt
(46 ab heute) / 9 abgelehnt / 0 beantragt / 0 NULL-Status**. Heisst: (1) keine Alt-Zeile ohne Status faellt
still aus der Sperre, (2) auf heutigem Stand aendert sich **kein einziger Plan** — die Aenderung wirkt erst ab
dem naechsten Urlaubsantrag. Sebastian-Entscheid: **BEIDES** (Anzeige UND Kapazitaet) auf "nur genehmigt".

## TEIL A — "Neu berechnen" bringt frische Arbeitsscheine
- Neuer Helfer **`_asPullFresh(setArbeitsscheine)`** (vor `_juprowaSync`): reiner DB-Pull, READ-ONLY, gibt
  `{ok,count}` bzw. `{ok:false,error}` zurueck, faengt intern (nie haengen, nie werfen).
- Neue Prop **`onRefreshScheine`** an `DispoPanel`; **Callsite** im Render (`ArbeitsscheinView`):
  `onRefreshScheine: ()=>_asPullFresh(setArbeitsscheine)`. Der Pull passiert im **Parent** — das Panel bleibt
  fetch- und schreibfrei (**v745 gewahrt**), im Panel steht nur der Prop-Aufruf.
- **Button-onClick:** `onRefreshScheine()` -> `.then(...)` -> `_doDispoRefresh()`. Die Reihenfolge ist der
  ganze Punkt: der Recompute muss den **frischen** AS-Stand sehen, sonst rechnet der Klick denselben Plan.
  Reject-Zweig ruft `_doDispoRefresh` ebenfalls (ein fehlgeschlagener Pull darf den Recompute nicht schlucken).
  Faellt die Prop weg (Alt-Callsite/Test), bleibt exakt das v766-Verhalten.

## TEIL B — Overlay UND Kapazitaet nur noch bei status=genehmigt
- `_dispoAbwAbzug`: `if(String(ab.status||"genehmigt").toLowerCase()!=="genehmigt")return 0;`
  (vorher: nur `==="abgelehnt"` -> 0). Damit blocken **beantragt / ausstehend / abgelehnt** nicht mehr.
- **Konsistenz Anzeige==Kapazitaet ergibt sich automatisch**, weil der Overlay-Chip (`blockGrund` ueber
  `_dispoAbwLabel`) aus `absAbz` abgeleitet wird (`absAbz>=normMin` -> Volltag-Chip, `absAbz>0` -> Teiltag-Chip).
  Kein zweiter Statusfilter noetig — und per Test gepinnt, damit spaeter niemand eine zweite Regel einzieht.
- **Fehlender Status blockt weiter** (Default "genehmigt") — bewusst konservativ. Achtung fuer spaeter:
  `absMap` (Z.~7683) defaultet fehlenden Status dagegen auf **"beantragt"**; die beiden Defaults widersprechen
  sich theoretisch, praktisch nicht (0 NULL-Stati live). Bei Datenimporten im Blick behalten.

## Tests
- **NEU `tests/test_dispo_asfrische_abw_v767.py`:** `_asPullFresh` ist reiner Pull (TABU-Pin auf Drain/Push/RPC/
  Writes) · `_juprowaSync` nutzt denselben Helper (Drift-Schutz) · Button-**Reihenfolge** (Pull-Index < Recompute-
  Index, `.then` vorhanden) · Callsite verdrahtet · kein optional chaining · `_dispoAbwAbzug` statisch **und**
  **node-eval-Matrix** `[480,0,0,0,480,240,0,0]` (genehmigt Volltag/Teiltag blockt, beantragt+ausstehend+abgelehnt 0,
  fehlender Status blockt) · Overlay haengt an `absAbz`.
- **`test_dispo_neuberechnen_v766.py` mitgezogen** wie beauftragt: `test_kein_as_pull_im_panel` ->
  **`test_kein_as_pull_im_panel_body`** (Aussage jetzt: Pull-Mechanik bleibt im Parent — `_juprowaSync`/
  `_asPullFresh`/`_sbGet("arbeitsscheine")` duerfen im Panel-Body NICHT vorkommen, `onRefreshScheine` schon);
  Button-Regex auf mehrzeiliges onClick (`re.S`); Signatur-Pin -> `test_dispopanel_signatur_v767`.
- **Signatur-Pins nachgezogen** (Prop-Ergaenzung): v714, v715, v716, v717, v718, v734, v760.
- **`test_dispo_plan_v714.py` inhaltlich gedreht:** die Alt-Zeile `ok(_dispoAbwAbzug({status:'beantragt'},510)===510,
  'beantragt blockt')` pinnte genau das alte Verhalten -> jetzt `===0` + zusaetzlicher `genehmigt blockt weiter`-Pin.
  **Das ist die einzige Stelle, an der eine bestehende Verhaltens-Zusicherung bewusst umgedreht wurde** (Sebastian-Entscheid).

## Gates
`node_check` 0 · `_bracket_check` `()` **-1**, `{}` 0, `[]` 0 · `_check_version` 3.9.767 synced · voller pytest
**1817 gruen** nach dem Bump · kein optional chaining im Diff · **Headless-DispoPanel-Mount** (lokaler
`http.server` + Playwright-MCP): Mount ohne Throw, 0 Console-Errors, und der reale Klick ergab die Sequenz
**`as-pull` -> `as-pull-fertig` -> `sbGet:dispo_blocks`** = die Reihenfolge ist verhaltensecht belegt, nicht nur
statisch. TABU eingehalten: kein OFFA/Juprowa-Push, kein Auth, arbeitsscheine-Whitelist unveraendert (der
Pull ist ein GET), kein Push auf `.github/workflows/*`.

## Offen (unveraendert)
Die 3 Live-Co-Verifikationen (#19b DB-Writes gegen Baseline plz_geo 20 / plz_distanz 171 · #28b Office-Plan-Sicht ·
#1-Teil-2 Toggle-Klick) · **#18b** re-eval · **#6** E2-🚛-UI / **#7** Buero-Extras A5 (brauchen Spec).
**Neu zu beobachten:** sobald der erste Urlaub beantragt wird, plant die Dispo auf diesen Tag — das ist ab jetzt
gewollt (Sebastian-Entscheid), sollte aber beim ersten realen Fall einmal mit der Disponentin angeschaut werden.

---

# Nachtrag 3 — v3.9.768: Zulagen auf Lohnzettel-Wahrheit + Label „Entfernungszulage"

**HEAD:** **v3.9.768**. **pytest 1827 grün.** Version-Triple synced. EP Kolar hat genau zwei Zulagen.

## (a) Was ändert sich je Punkt — mit €-Wirkung

| Punkt | Was gebaut | Ändert Zahlen | €-Wirkung |
|---|---|---|---|
| **1 Satz Entfernungszulage** | `taggeldAb6h` **11,94 → 11,71** (Lohnzettel LA 2740, steuerfrei §26) | **JA** | **−0,23 € je qualifiziertem Tag.** 16 Tage: 191,04 € → **187,36 €** (−3,68 €) |
| **1 Satz Montagezulage** | `montagezulage{2026}` + `montagezulageStd` **1,155 → 1,13** (LA 4060, steuerpflichtig) | **JA** | **−0,025 € je Stunde.** 8,5-h-Tag: 9,8175 € → **9,605 €**. 167 h: 192,89 € → **188,71 €** (−4,18 €) |
| **1 2027-Sätze** | **nicht erfunden** — 2027 aus der Satz-Map entfernt | nur 2027 | Ein Tag in 2027 fällt auf `montagezulageStd` (1,13) zurück statt auf den unbelegten 1,178 → **Lohnverrechner-Prüfpunkt** |
| **2 Label** | „Taggeld" → „Entfernungszulage" in Report-Titel, Tabellen-Spaltenkopf, **CSV-Header**, Admin-Gruppe + Feldlabels, Hinweistext | **NEIN** | 0 € — reine Anzeige. Feldnamen `taggeld*` unangetastet (Variante B abgelehnt) |
| **3 Nächtigung** | `taggeldNacht` 62,04 € + >11h-Stufe **bleiben stehen**, im Admin-UI als „ohne Funktion (EP Kolar: keine Nächtigung)" markiert | **NEIN** | 0 € — toter Zweig bleibt tot |
| **4 P1b Juli rückwirkend** | **kein Code nötig** | NEIN | 0 € |

**Begründung zu 3 (warum stehen lassen statt entfernen):** Entfernen wäre eine Verhaltensänderung an einer
lohnrelevanten Funktion ohne Not. Die Live-Daten zeigen, dass der Zweig ohnehin nie erreicht wird — Juni 2026:
**16 Tage > 6 h, 0 Tage > 11 h**, längster Tag 9,5 h. Markieren ist risikoärmer als löschen und macht den
Zustand für den Admin ehrlich sichtbar.

**Begründung zu 4 (kein Code):** Das Vergabe-Panel hat bereits **◀/▶-Monatsnavigation** (`_shiftYm`), die
Monatsauswahl ist frei. Juni und Juli sind über die bestehende UI flaggbar — **P1b ist ein reiner
Büro-Handgriff, kein Feature.** Kein rückwirkender Auto-Batch gebaut (war auch nicht gewünscht).

**Kein DB-Write nötig — und das war nicht selbstverständlich:** `system_config` hat **keinen** `kv_rules`-Datensatz
(read-only geprüft). `_kvLoadRules` merged nur, wenn der Key existiert → die App läuft auf `KV_RULES_FALLBACK`.
Die Code-Änderung ist damit vollständig wirksam; ein lohnrelevanter DB-Write wurde **vermieden**, nicht vergessen.

## Gegenrechnung am Lohnzettel — im echten Browser verifiziert

Nicht nur im Test, sondern im geladenen Browser gegen `window.KV_RULES` gerechnet:

| Gegenrechnung | Erwartet (Lohnzettel) | App liefert |
|---|---|---|
| Montagezulage 167 h × 1,13 | 188,71 € | **188,71 €** ✅ |
| Entfernungszulage 16 × 11,71 | 187,36 € | **187,36 €** ✅ |
| 1 Tag 8,5 h × 1,13 | — | **9,605 €** |

⚠️ **Rechen-Korrektur:** 8,5 × 1,13 = **9,605 €**, nicht 9,61 € — der node-eval-Test hat diese Ungenauigkeit
in meiner ersten Testerwartung gefangen (deshalb pinnt er jetzt 3 Nachkommastellen). Im Report/CSV wird auf
2 Stellen gerundet ausgegeben.

## (b) ⚠️ OFFENER PRÜFPUNKT — Montagezulage-Menge (Rückfrage an den Lohnverrechner)

**Lohnzettel 167 h vs. Zeiteinträge 158 h (Riedmann, Juni 2026).** Die **Formel** der App ist korrekt und
identisch mit „Menge × Satz" (Summe der Stunden geflaggter Tage × Satz) — es weicht die **Mengenbasis** ab.
Geprüfte Kandidaten, alle ≠ 167: alle Juni-Stunden Riedmann **158,00** · nur Tage > 6 h **137,50** · alle
Monteure zusammen **165,00**. `stempel_log` ist im Juni **leer** (keine Alternativquelle), und Riedmann hatte
**keine genehmigte Abwesenheit** im Juni (nur 34 h abgelehnten Urlaub) — bezahlte Abwesenheit erklärt die
Lücke also nicht.
**Frage an den Lohnverrechner:** führt die Lohnverrechnung eigene Stunden (Feiertage, Überstunden, Zuschläge),
die nicht in `time_entries` stehen? Oder betrifft der Lohnzettel einen anderen Zeitraum/eine andere Person?
**Bis das geklärt ist, ist die App-Zulage eine VORSCHAU-Näherung, nicht die maßgebliche Zahl.**

**Das steht jetzt auch in der App**, damit das Büro die Zahl nicht für bare Münze nimmt:
- Report-Hinweis: „⚠ VORSCHAU — der Lohnverrechner ist maßgeblich … Lohnzettel 167 h vs. Zeiteinträge 158 h …"
- Vergabe-UI, **beide** Zweige (mobil + Desktop): „⚠ Vorschau — Lohnverrechner maßgeblich"

## Weitere offene Lohnverrechner-Punkte (nur markiert, nichts gebaut)
1. **2027-Sätze** für beide Zulagen (bewusst nicht geraten).
2. **Definition „Menge" bei der Entfernungszulage** — zählt Tage? Welche Tage qualifizieren? Die App nimmt
   „Tages-Anwesenheit > 6 h" an; bei 16 Tagen trifft das den Lohnzettel exakt, ist aber nicht belegt.
3. Offenes **Z6** (Metallgewerbe vs. Metallnebengewerbe) aus `docs/ZULAGEN_VORLAGE_2026-07.md`.

## Tests
`tests/test_zulagen_saetze_v768.py` (10 Pins): Sätze im Fallback · **2027 nicht erfunden** (prüft den
Satz-*Schlüssel* im wirksamen Block, nicht das Wort — der Erklärtext nennt 2027 bewusst) · Beleg-Kommentare
LA 2740/LA 4060 · **node-eval Montagezulage** `[9.605, 188.71, 9.605, 0]` (inkl. 2027-Fallback + ohne-Flag=0) ·
**node-eval Entfernungszulage** `[11.71, 0, 0, 187.36, 16, 0]` (inkl. Grenzfall exakt 6,0 h = 0 €) ·
Anzeige-Strings umbenannt · **kein Anzeige-String sagt mehr „Taggeld"** (Kommentare ausgenommen) ·
**Feldnamen unverändert** (pinnt die Ablehnung von Variante B) · tote Stufen markiert · Vorschau-Hinweis
in Report + beiden Vergabe-Zweigen.

**Drei Alt-Tests mussten nachgezogen werden** (sie pinnten die abgelösten Schätzwerte):
`test_kv_rules_v3647::test_kv_kernwerte` (11,94 → 11,71; 1,155 → 1,13; `stand`-Präfix) ·
`test_kv_montagezulage_v3671::test_kv_rules_map_present` (Map-Pin auf `{2026:1.13}`) ·
`…::test_komma_coercion` (Fallback-Default 1,155 → 1,13). Jeweils mit Begründung im Test kommentiert.

## Gates
`node_check` 0 · `_bracket_check` `()` **-1**, `{}` 0, `[]` 0 · `_check_version` 3.9.768 synced · voller
pytest **1827 grün** nach dem Bump · kein optional chaining · **Headless-Mount**: `KVZulagenReport` **und**
`DispoPanel` ohne Throw, 0 Console-Errors, Report-Text enthält „Entfernungszulage" und **kein** „Taggeld".
TABU eingehalten: kein OFFA/Juprowa-Push, kein Auth-Write, `arbeitsscheine`-Whitelist unverändert,
kein Write auf `system_config` (war nicht nötig).
