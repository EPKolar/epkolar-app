# Handoff 21.08.2026 — Swipe-Fix + Büro-Einteilung + PlanRadar-Richtung (v3.9.825 → 826)

**Stand:** Beide Fixes **live auf `epkolar.github.io/epkolar-app`** verifiziert
(APP_VERSION **3.9.826-supabase**, sw.js CACHE_NAME **epkolar-v3.9.826**).
Arbeitsklon `C:\repos\epkolar-app`, `main` = `06eb5ca`, working tree sauber, nichts ungepusht.

---

## Gebaut + gepusht

| Version | Commit | Inhalt | Gates |
|---|---|---|---|
| **3.9.825** | `98f356c` | **Swipe im Browser repariert** (User-Report: „in der PWA geht kein Wischen mehr, installiert schon") | node_check 0 · bracket `() -1` · version-sync · pytest 2030/13 · Playwright |
| **3.9.826** | `06eb5ca` | **Büro-Rolle: Arbeitsscheine-Einteilung wie admin** (User-Report: „Damen im Büro sehen keine Monteurs-Timeline/Kalender") | dieselben · pytest 2033/13 · Playwright |

### v3.9.825 — Swipe im Browser (touch-action)
**Symptom:** Wischen ging nur als **installierte (standalone) PWA**, nicht im Browser-Tab.
**Root Cause (code-level, keine Spekulation):** Der `useSwipe`-Hook (~6002) fängt horizontale
Wische über `onTouchStart`/`onTouchEnd` **ohne `preventDefault`**. Ohne `touch-action:pan-y`
auf dem Wisch-Container behandelt der **Browser** die horizontale Geste selbst
(Page-Scroll / History-Navigation) → der Hook sieht nie eine saubere Geste. Standalone gibt
es diese konkurrierenden Browser-Gesten nicht → derselbe Code lief.
`.main-pad` (mainSwipe) hatte den Constraint schon seit **v3.9.313** (mit genau dieser
Begründung im Kommentar); die **zwei anderen** useSwipe-Flächen blieben ohne:
- `shellSwipe` (Projekt-interne Navigation dashboard/zeit/berichte/…, ~14905)
- `absSwipe` (Urlaubs-Tabs, ~21250)

**Fix:** `touchAction:"pan-y"` auf beide Container (zwei inline-Styles). Keine Logik/Schema/Auth.
**Beleg:** Playwright computed `touch-action` — kaputt=`auto`, Fix=`pan-y`, Referenz(`.main-pad`)=`pan-y`
→ Fix verhaltensgleich zur funktionierenden Fläche. Test `tests/test_swipe_touchaction.py`
(Invariante: **jede** useSwipe-Fläche muss `pan-y` tragen; deckt alle 3 Callsites + Zähler ab).

### v3.9.826 — Büro macht die Einteilung
**Symptom:** Büro (`buero`) sah bei den Arbeitsscheinen keinen Monteurs-Kalender / keine Timeline.
**Root Cause:** `ArbeitsscheinView` (~9882) definierte `isAdmin` als `admin||projektleiter` — **`buero` fehlte** (9884).
Der **Dispo-Tab war für buero schon offen** (10457, explizite Liste `admin/buero/projektleiter`),
aber der Kalender-Monteur-Auswähler (10655) hängt an `isAdmin` und fällt für Nicht-Admin auf ein
festes Badge der **eigenen** monteurId zurück → buero (ohne monteurId) sah leeren Kalender, keine
Monteurs-Auswahl. `canSync` (9885) enthielt buero längst; 9884 war die vergessene Stelle.
**Fix:** `buero` in `ArbeitsscheinView.isAdmin` → Kalender-Monteur-Auswähler + Monteur-/SB-Filter +
Dispo-Inhalt wie admin/PL. **`isMonteurRole` bleibt `monteur/helfer`** → die `isMonteurRole&&!isAdmin`-
Schutzriegel ändern sich für buero **nicht** (buero war nie Monteur; geprüft: alle isAdmin-Nutzungen
in der Komponente sind View/Filter/Select, nichts destruktiv-Admin-only). Analog Abwesenheiten-`isAdmin`
(v3.9.453). Test `tests/test_buero_as_dispo.py` (isAdmin enthält buero; isMonteurRole NICHT; Dispo-Tab-Liste bleibt).

**Gates je Version identisch:** `python scripts/node_check.py index.html` → 0 ·
`python scripts/_bracket_check.py index.html` → Baseline `() -1` · `node sql/_check_version.js` → synced ·
voller `python -m pytest tests/ -q` grün · Playwright-Mount 0 JS-Fehler + Versionsabgleich.

---

## 🎯 PlanRadar — die eigentliche Richtung (Sebastian, 21.08.)

**„PlanRadar muss immer mehr in die App — sauber und für den User einfach."** Das ist der
strategische Fokus, nicht weitere Kleinfehler-Jagd. **In `ROADMAP.md` als eigene Sektion
verankert** (🎯 STRATEGISCHE RICHTUNG) mit Ist-Stand + priorisierten Schritten.

**Kernbefund (Code-Analyse v3.9.826):** Die PlanRadar-Schleife **existiert bereits** —
kein Greenfield: Plan-Viewer (Pan/Zoom/PDF), **Pins an Plan-Koordinate** (Tap+Drag),
**Mängel als draggbare Pins**, Ticket-Objekt (Status/Prio/Frist/Kommentar-Journal/Status-Historie),
polymorphe Foto-Queue, **Einzel-Ticket-PDF mit Pin-Ausschnitt**, Bulk-Zuweisung, reifer Offline-Sync.
Vorgeschichte: `docs/handoff/STAND-2026-06-18-PLANRADAR.md`.

**Nächster Schritt mit dem besten Hebel: ① Plan-Gesamtreport-PDF** — eine Seite = Gesamtplan
mit **allen nummerierten Pins + Legende** (Nr → Titel/Status/Frist/Verantwortlich). Rein additiv,
alle Bausteine liegen vor (`_tkRenderPdfPage` rendert die Planseite schon als Canvas, `_tkCropPin`
für die Pin-Zeichnung, `_ticketNr` für die Nummern, `genMangelPdf`-Layout für die Legende). ~1 Funktion,
kein Schema/Backend. Danach der kleine Teil von **②** (VMang-Defect-Pins am Plan editierbar).
Voll priorisierte Tabelle in ROADMAP.

---

## Offen für die nächste Session

### A · Sebastian, Handarbeit (aus Handoff 23.07., weiter offen)
Stammdaten von **Cracana (`w3`)** + **Aliti (`mqyxfca35x6i`)** nachtragen — `stundensatz`, `svnr`,
`eintritt`, `phone`, `geb_dat`, `fs`, `fs_nr` (Mitarbeiter-Tab). Kein Datenverlustrisiko mehr
(Löschschutz v3.9.824 App+DB scharf).

### B · Punkt 4 — Silent-Catch-Inventur (GEMESSEN, entscheidungsreif, noch nicht umgesetzt)
Vollständige Messung heute (v3.9.826): **149 `.catch(...)`** gesamt; ~113 benigne (Offline-gewollt/
Telemetrie/Fehlertext-Reads), **~34 Stiller-Ausfall-Risiko**.
**Systemische Wurzel:** `_sbGet` (~1937) / `_sbGetOrder` (~1945) geben bei **401/403 still `[]`** zurück
(werfen NICHT) → die nachgeschalteten `.catch(()=>[])` feuern gar nicht, der Aufrufer rendert „keine Daten".
Der **fertige Baustein liegt schon im Code**: strikter Leser **`_sbSelectStrict` (~1986)** wirft bei
JEDEM Nicht-2xx — wird aber **nur** im Löschschutz (`_workerRefs`) benutzt, **nicht** in den Ladepfaden.
Konkrete Kandidaten (Zeilen v3.9.826):
- **Kiosk-AS-Lader** `_kioskWeekArbeitsscheine` (~2017/7695): `?json:null` → Wandpanel zeigt still die
  **alte Woche** weiter. Exakter Zwilling von `_loadKioskFahrzeuge` (~2030), das mit `window.__kioskFzErr`
  gehärtet ist — hier fehlt genau das. **Kleinster, klarster Fix mit vorhandenem Muster.**
- **Boot-Lade-Kette** (~7689–7701): 11 Kollektionen `.catch(()=>null)`, nur `getWorkers` (7690) v822-gehärtet
  (Warn-Toast + `__epkWorkerLoadErr`). Die anderen 11 rendern Ladefehler als leere Liste ohne Hinweis.
  Fix: den v822-else-Zweig auf alle 11 spiegeln.
- **Dashboard-Falsch-Null** (~8111/8119/8154/8156, 12774–12782, 13859–13863): Read-/Auth-Fehler wird als
  beruhigende „0 offene Mängel / 0 Material / 0 überfällig" gerendert. Fix: strikter Leser + „—"/„Fehler" statt `0`/`[]`.
- **Lohn-/Zeit-Reads** (~13892/21811/22998 finkzeit, 20976 urlaubskontingent, 20986/27492 absence_files):
  lohnrelevant — „nicht lesbar" wird zu „0 Tage/keine Atteste".
- Kleiner Rand: Supplier-Reads (~18715/18722); Schreib-Best-Effort ~3239 PhotoQ.update, ~9256/12857 auth_user_id-Relink.

**Empfehlung:** eigener systematischer Durchgang; zuerst der Kiosk-Zwilling (klar, risikoarm),
dann die Boot-Kette + Dashboard-Nullen als **eine** kohärente Ausrollung von `_sbSelectStrict`/
v822-Muster. **Heute bewusst NICHT gemacht** — das ist eine breite Änderung über viele Render-Pfade
und gehört nicht an einen Tag mit zwei anderen Deploys (App läuft grundsätzlich, Fokus PlanRadar).

### C · Punkt 5 — Namens-Snapshot: bleibt **vertagt** (Aufbewahrungsfristen + FinkZeit-Ablöse).

### D · PlanRadar-Ausbau (siehe ROADMAP-Sektion) — der eigentliche Fokus.

---

## Gotchas / Lektionen dieser Session
- **Swipe browser vs. standalone = fast immer `touch-action`.** Ein Touch-Handler ohne `preventDefault`
  braucht `touch-action:pan-y` (bei horizontalem Swipe) auf dem Container, sonst frisst der Browser die
  Geste; standalone fällt der Unterschied nicht auf → Bug nur im Browser-Tab. Vergleich mit einer
  funktionierenden Fläche fand die eine, die den Constraint schon hatte.
- **Rollen-Gate-Bugs: Tab sichtbar ≠ Inhalt nutzbar.** Der Dispo-Tab war für buero offen, aber der
  Inhalt hing an einem `isAdmin`, das buero ausschloss. Vor dem Fix geprüft, dass `isMonteurRole` buero
  NICHT enthält → die verneinenden Schutzriegel kippen nicht.
- **Teilwort-Falle vermieden:** „timeline" traf zuerst die Abwesenheiten-Timeline (20949/21367); die
  gemeldete AS-Timeline lag woanders (Kalender-Monteur-Auswähler + Dispo). Nicht den ersten Treffer fixen.

---

# Nachtrag — Autonomer Aufräum-/PlanRadar-Lauf (21.08., Auftrag „vollautonom alles Offene + Richtung PlanRadar")

**Fünf** Auslieferungen diese Session, `main` = `92d4529`, alle live-verifiziert (Pages 3.9.829), Baum sauber.

| Version | Commit | Inhalt | Status |
|---|---|---|---|
| 3.9.825 | `98f356c` | Swipe im Browser (touch-action:pan-y auf shellSwipe+absSwipe) | live |
| 3.9.826 | `06eb5ca` | Büro-Einteilung (buero in ArbeitsscheinView.isAdmin) | live |
| 3.9.827 | `ecae3ab` | Kiosk-AS-Ladefehler sichtbar (`__kioskAsErr`) — **Punkt B, Kiosk-Zwilling ERLEDIGT** | live |
| 3.9.828 | `550a658` | **PlanRadar ① Plan-Gesamtreport-PDF** (`_genPlanReportPdf`, Button „📄 Plan-Report") | live |
| 3.9.829 | `92d4529` | **PlanRadar ② Defect-Pins editierbar** (Klick → QuickEditPin, schreibt `defects`) | live |

**Punkt B (Silent-Catch) — Teilstand:** Der **Kiosk-Zwilling ist erledigt** (v3.9.827, `__kioskAsErr`).
**Bewusst NICHT autonom gemacht:** die Boot-Kette (11 Slots) + Dashboard-Falsch-Nullen + Lohn-/Zeit-Reads.
Grund: der wirksame Kern ist der **Auth-Silent-Fall** — `_sbGet`/`_sbGetOrder` geben bei **401/403 still `[]`**
zurück (werfen NICHT), also fangen die Boot-`.catch` ihn gar nicht; ihn wirklich zu schließen heißt entweder
`_sbGet` global auf Werfen umzubauen (trifft JEDEN Aufrufer, viele verlassen sich auf `[]`) oder `_sbSelectStrict`
in die Ladepfade zu ziehen (breit). **Beides ist ohne Live-DB-Auth-Test zu riskant für einen unbeaufsichtigten
Lauf** und braucht Sebastians Blick auf die Fehler-UX (wann „—/Fehler" statt „0"). Bleibt als entscheidungsreifes
Paket (Abschnitt B oben, mit allen Zeilennummern). Das ist kein Ausweichen, sondern die Grenze, wo grüne Gates
nichts über die Live-DB sagen.

**PlanRadar ① — geliefert (v3.9.828):** `_genPlanReportPdf(plan,pins,monteure,layers,proj)`, Button
„📄 Plan-Report" in der Plan-Viewer-Aktionsleiste (Gate admin/PL/buero). Ein PDF = Gesamtplan je Plan-Seite +
alle **nummerierten** Pins (Kreisfarbe=Status) + Legendentabelle (Nr/Titel/Status/Frist/Verantwortlich).
Additiv, baut auf `_tkRenderPdfPage`/`_tkLoadImg`/`_pdfStr`/`TICKET_STATUS`; Nummern deckungsgleich mit
On-Screen `_ticketNr`. Playwright: end-to-end mit synthetischem Plan → echte PDF erzeugt + Erfolgs-Toast.
Test `tests/test_plan_report_pdf.py`.

**PlanRadar ② — geliefert (v3.9.829):** Klick auf einen VMang-Mängel-Pin öffnet jetzt das bestehende
QuickEditPin-Popup (statt nur Info-Toast). Speichern schreibt die **kanonischen** `defects`-Felder
(status/prio/frist/zugewiesen) via `SQ.push` PUT `/api/defects/:id`; Ticket-Status → MANGEL_ST rückgemappt
(Playwright: sauberer Round-Trip aller 3 Defect-Status, jeder Wert gültig). QuickEditPin um zwei optionale
Props erweitert (`statusOptions`/`hideJournal`, Defaults = unverändertes Ticket-Verhalten). Test
`tests/test_defect_pin_editable.py`; obsoleten Signatur-Pin in `test_planradar_quickedit_v3138` nachgezogen.

**Erreichte Grenze des autonomen Laufs — was BEWUSST offen bleibt (kein Ausweichen, sondern Qualitätsgrenze):**
- **PlanRadar ③ (Formular-/Checklisten-Builder):** braucht eine **Design-Entscheidung** (Feldtypen) und eine
  neue Tabelle `checklist_templates` = **DDL → Human-Run-Gate/Sebastian**. Mittel–groß, kein reiner
  Additiv-Schritt. Nicht unbeaufsichtigt zu bauen.
- **PlanRadar ④ (externe Subunternehmer):** groß — neue Entität + Auth/RLS + Portal + E-Mail. Braucht Design + DDL.
- **Punkt B Rest (Silent-Catch Boot-Kette/Dashboard-Nullen):** der Auth-Silent-Kern (`_sbGet`→[] bei 401/403)
  ist ein globaler Umbau, der Live-DB-Auth-Test + Sebastians Fehler-UX-Entscheid braucht (s. Abschnitt B).
- **Sebastian-DDL-Altbestand:** `sql/WORKERS_DELETE_GUARD_v1.sql` liegt scharf; `migrate_defects_consolidate_v3112.sql`
  (Doppelfeld-Konsolidierung) ist laut `_mapDefect`-Kommentar noch Sebastians Lauf.

Alle LOW-RISK/ADDITIV/CONTAINED-Posten sind erledigt. Der Rest ist die Tier „braucht Design/DDL/Live-DB".

---

# Nachtrag 2 — Bug-Hunt + Struktur-Hunt (21.08., 2 read-only Agenten, jeder Fund selbst gegengeprüft)

**Weitere Auslieferungen:** v3.9.830 (③a typisierte Checklisten-Felder), v3.9.831 (Struktur:
Status-Maps konsolidiert), v3.9.832 (Checklisten-Reload-Merge), v3.9.833 (Forms-Reload-Merge PUT).
`main` = `e79e165`, Pages 3.9.833.

## Struktur-Hunt
- **Kein entfernbarer toter Top-Level-Code** (638 Defs gemessen; nur `_safeSessionSet` count==1, per Guard-Test bewusst behalten).
- **Status-Map-Duplikate → konsolidiert (v3.9.831, LIVE):** Mangel↔Ticket-Maps lagen je 2× inline (u.a. frisch aus meinem eigenen v829 ②) → jetzt Modul-Konstanten `_MANGEL2TICKET_ST`/`_TICKET2MANGEL_ST`.
- **Offen (niedrig):** 31× `catch(e){}` brechen die `catch(_e){}`-Konvention (verhaltensneutral, kosmetisch); Prio-Array `["kritisch"…]` 2× statt `Object.keys(TICKET_PRIO)`. Kein Bug, reine Kosmetik — bewusst gelassen.

## Korrektheits-Hunt — 5 Funde (35 geprüft, 16 wegen Guards verworfen)

**GEFIXT + LIVE (die sichere Merge-Klasse):**
- **#3 Checklisten-Reload-Merge (v3.9.832):** Checklisten waren die EINZIGE Kollektion ohne Pending-Schutz → frisch angelegte Checkliste verschwand beim Reload. Merge wie entries/AS. Playwright-verifiziert.
- **#4 Forms-Reload-Merge PUT (v3.9.833):** editiertes, nicht gedraintes Formular sprang beim Reload zurück. Pending-PUT-Schutz nachgezogen. Playwright-verifiziert.

**VERIFIZIERT, aber BEWUSST NICHT autonom gefixt (Lohn/DB/Matching — brauchen Sebastian):**
- **#1 (P0/P1, LOHN) — AS-Zeit-Übernahme-Marker `ze_uebernommen` wird bei einem Monteur an einem ABGESCHLOSSENEN Schein nie gesetzt.** `_asZeitUebernahme` (~10130) insertet den `time_entry`, dann `updAs(…{ze_uebernommen:true})` — dort greift der Monteur-Riegel (~10221) `if(_isMt){if(!AS_GRP_OFFEN.includes(scheinstatus)){toast;return;}}` und kehrt VOR dem Marker-Write zurück → Marker nie persistiert → beim nächsten Speichern erneut Insert; spätere Zeit-Korrektur greift nie. Die Fahrzeit-/Arbeitszeit-Felder (~10788/10789) sind für Monteure NICHT `disabled`.
  **SCHWERE HÄNGT AN EINER DB-FRAGE (Sebastian bitte prüfen): Hat `time_entries` einen UNIQUE-Constraint auf `arbeitsschein_id`?** Falls NEIN → **P0 Doppel-Lohn** (echte Doppelbuchung). Falls JA → 23505 bei jedem Save + festhängender Marker + nicht durchgreifende Korrektur.
  **Zwei Fix-Kandidaten (nicht autonom gebaut):** (a) die Zeit-Felder für Monteure an abgeschlossenen Scheinen `disabled:_asMtLocked` setzen (UI-Guard, verhindert den Auslöser); (b) den `ze_uebernommen`-Marker-Write NICHT durch den Monteur-Riegel blockieren (System-Bookkeeping ≠ User-Edit). Beide berühren einen Lohn-Pfad → mit Sebastian abstimmen.
- **#2 (P1, LOHN) — PZE-Stundenzettel-PDF exportiert IMMER den ganzen Kalendermonat**, ignoriert den freien `von/bis`-Bereich (Bildschirm/Excel nutzen ihn). `_pzePdf` (~11421) rechnet `von=ym+'-01'`/Monatsende; Aufrufer (~11680) übergibt nur `String(von).slice(0,7)`; EZ-daysMap filtert hart `iso.slice(0,7)!==ym` (~11444). **ABER: das PDF heißt „Personalzeit-MONATSblatt" und zeigt im Kopf ehrlich `von bis` des Monats — evtl. Design-gewollt.** DESIGN-FRAGE an Sebastian: soll der Übergabezettel (a) den freien Bereich honorieren (dann EZ-Filter auf von/bis umstellen — Lohn-Zulagen-Mathe!), (b) die Datumsfelder auf Monatsgrenzen schnappen, oder (c) bei Nicht-Monats-Bereich warnen? Nicht autonom, weil es Zulagen-Rechnung (LA 2740) berührt.
- **#5 (P2, mittel) — JUPROWA-Worker-Auto-Zuordnung per Teilstring** `wN.includes(pu)` (~3507) → bei ähnlichen Nachnamen (MAI→MAIER, BAUER/BAUERNFEIND) kann Schein-Zeit dem falschen Worker zugeordnet UND dauerhaft in `epkolar_juprowa_wmap` gecacht werden (Teilworttreffer-Klasse, projektbekannt). Braucht Exakt-Wort-Prüfung — aber Änderung am Matching ist heikel (könnte bestehende korrekte Treffer brechen) + braucht ein reales Nachnamen-Kollisions-Beispiel aus dem Roster. Mit Sebastian verifizieren, bevor angefasst.

**Ehrlichkeitsbilanz des Hunts:** ~35 Kandidaten, 5 gemeldet, 16 wegen vorhandener Guards verworfen (u.a. EZ-Rechenkern konsistent, 5-Min-Rundung korrekt/WYSIWYG-gewollt, entries/AS/abs/defects-Merges bereits geschützt, doSync-Retry sauber).

---

# Nachtrag 3 — Swipe endgültig gelöst + Freundlichkeit + PDF-Ausbau (21.08., v834–841)

`main` = `dbee986`, Pages 3.9.841. Insgesamt **17 Auslieferungen** diese Session (v825–841).

**PDF-Ausbau (v840/841, beide Playwright/Node-verifiziert):**
- **v840 Checklisten-Protokoll-PDF** (`_genChecklistPdf`) — Button „📄 PDF" im Checklisten-Detail: A4-Protokoll mit Punkten [x]/[ ] (inkl. typisierter ③a-Werte), Anmerkungen, Fortschritt, **zwei Unterschriften**. Gab es vorher nicht.
- **v841 `_pdfStr` transliteriert statt strippt** — EIN Fix für ALLE PDFs: Gedankenstriche/typografische Anführungszeichen/Ω/μ/… wurden vorher **still entfernt** (jsPDF kann nur latin1); jetzt lesbar transliteriert (Ω→Ohm, μ→u, –/—→-, „"→"). Wichtig für Elektro-Messwerte + User-Beschreibungen. latin1 (²/³/°/µ, Umlaute) bleibt.

## Swipe im Browser — die ganze Saga (wichtigste Lektion)
Der User meldete mehrfach „Wischen geht nicht" und korrigierte meine Fehldiagnosen — **dreimal nachgebessert**:
1. **v825 (FALSCH/unvollständig):** `touch-action:pan-y` auf die Wisch-Container. Ich meldete „gefixt", hatte aber nur belegt, dass die **CSS-Eigenschaft gesetzt** ist — nie die echte Geste reproduziert. User: „geht noch immer ned".
2. **v834 (richtige Ebene):** Reproduktions-**Harnisch** (Kopie des `useSwipe`-Hooks + synthetische Touch-Events) bewies: die Hook-**Logik ist korrekt**, der Fehler liegt in der **Event-Zustellung**. Die horizontale Browser-„Wisch-zurück"-Navigation frisst die Geste (touchcancel). Die steuert **`overscroll-behavior-x`, NICHT `touch-action`** — html/body hatten nur `-y:contain`. Fix: `overscroll-behavior:contain` (beide Achsen). → half der Projekt-Ansicht.
3. **v836 (vollständig):** User: Projekt-Ansicht ging, aber **Haupt-Menü + Zeiterfassung nur im Querformat**. overscroll-behavior-x reichte im Hochformat nicht. Fix: **nativer non-passive `touchmove`-`preventDefault`** im Hook (React `onTouchMove` ist passive → dort wirkungslos → nativ via `ref`; Callsites spreaden `{...swipe}`, `React.createElement` zieht `ref` aus dem Spread → keine Callsite-Änderung). Harnisch-belegt: horizontal→gehalten+feuert, vertikal→frei, Tabelle/Input→scrollbar.

**Lektionen (in Memory [[feedback_swipe_touchaction_browser_standalone]] korrigiert):**
- „Touch-Geste nur standalone/gedreht, nicht im Browser/Hochformat" → **`overscroll-behavior-x`** prüfen, nicht `touch-action`.
- „computed CSS-Wert gesetzt" beweist NICHT, dass die Geste geht. **Reproduzieren (Harnisch) statt annehmen.**
- React `onTouchMove` ist passive → `preventDefault` nur über nativen non-passive Listener.
- **Caveat Zeiterfassung:** swipen DIREKT auf der Stunden-Tabelle scrollt bewusst die Tabelle (Skip-Liste); Tab-Wechsel dort auf freiem Bereich. Falls der User das Tabellen-Swipe will → eigener, kniffliger Schritt (Konflikt mit horizontalem Tabellen-Scroll).
- **Deploy-Falle:** ein Fix „kommt nicht an", wenn der alte Service Worker gecacht ist → nach Deploy Hard-Reload.

## PlanRadar + Freundlichkeit (v830/835/837/838/839)
- **v830 ③a** typisierte Checklisten-Felder (Zahl/Text/Datum/Auswahl, kein DDL) — LIVE.
- **v837** Plan-Report ① bekam eine **Status-Zusammenfassung** im Kopf (offen/in Bearbeitung/erledigt/überfällig).
- **Copy-Audit** (read-only Agent): PlanRadar/Leerzustände sind **großteils schon vorbildlich**; die echten Wins waren **internes Siezen** (die App duzt sonst durchgängig) + zwei Sackgassen-Leerzustände.
  - **v835** Plan-Viewer entsiezt + einladender. **v838** durchgängig: Bautagebuch/Fotos/Monatszettel/DATANORM/PWA-Install/Passwort-Reset auf du; 2 Leerzustände → Einladungen; „Cache leeren" → „Speicher freigeben". **v839** zwei Fehlermeldungen mit Wie-weiter. Kundenportal bleibt bewusst **formell (Sie)**.

## Grenze / offen
- **Tiefere PlanRadar-UI-/Visual-Arbeit braucht Augen:** die PlanRadar-Oberflächen liegen hinter dem Login — Mount zeigt sie nicht. Für Layout/Visual-Polish (nicht Copy) **Screenshot des störenden Screens oder Test-Login nötig**. Blind nur Copy (sicher), kein Layout.
- **Verbleibende Struktur-Funde bewusst gelassen** (rein kosmetisch, Churn): 31× `catch(e){}`→`catch(_e){}`, Prio-Array aus `TICKET_PRIO` ableiten.
- **Für Sebastian (aus Bug-Hunt, Nachtrag 2):** #1 AS-Marker Doppel-Lohn-Risiko (DB-UNIQUE auf `time_entries.arbeitsschein_id` prüfen), #2 PZE-PDF Monat-vs-Bereich (Design), #5 JUPROWA-Teilstring.

---

# Nachtrag 4 — Eingeloggt GEMESSEN (Swipe live bestätigt) + Urlaub-Hunt (21.08.)

Auf „na dann mess es" hin: **eingeloggtes UI ohne echte Zugangsdaten erreicht** — Offline-Session per
localStorage injiziert (`epkolar_auth`={at:fakeJWT,rt,exp} + `epkolar_user` mit id), neu geladen →
`window._curUser()` = Test Admin, App-Shell rendert (Daten-401s erwartet). Technik in Memory
[[reference_epkolar_login_messen]].

**SWIPE HOCHFORMAT — LIVE GEMESSEN, nicht mehr angenommen:** in der echten Zeiterfassung im Hochformat
(390px) auf `.main-pad` einen horizontalen `touchmove` dispatcht → **`defaultPrevented === true`**. D.h.
der v3.9.836-**native touchmove-Riegel ist dort aktiv und feuert `preventDefault`** → der Browser kann die
horizontale Geste nicht mehr abfangen → **der Swipe geht jetzt im Hochformat.** (Nebenbefund: `.main-pad`
hat `overscroll-behavior-x: auto` — irrelevant, weil der native Riegel vorher stoppt; html/body-`contain`
aus v834 saß nicht am inneren Container.) Der User hatte v834 getestet; **v836 löst seinen Fall** — jetzt belegt.

**Urlaub/Resturlaub-Hunt (14 geprüft, 10 verworfen — Subsystem für Vollzeit-Datenstand rechnerisch sauber):**
- **P1-latent (Teilzeit-Lohn): Terminal-Antrag rechnet mit hart 38,5 h.** `_submitAntrag` ~Z.6973
  `_stdVonTagK(d,38.5)` — das Stempel-Terminal macht bewusst kein `urlaubskontingent`-SELECT (Datenschutz),
  skaliert aber NICHT mit der echten Wochenstunde. Teilzeit-MA (woche 19,25) am Terminal → `hours=8,5` statt
  4,25 → **doppelter Urlaubsabzug/Tag**. Heute no-op (alle Vollzeit), zündet beim ersten Teilzeit-MA am Terminal.
- **P1-bedingt: Umbenennung eines MA verwaist alle Alt-Absences → Resturlaub springt auf voll.** `_resturlaubK`
  ~Z.21146 sucht rein über `worker.name` (Key `NAME_DATE`); kein Re-Key bei Umbenennung. „Huber"→„Hueber" →
  `resturlaub("Hueber")` findet die alten `Huber_...`-Einträge nicht → zeigt volle 192,5 h statt Rest. Namens-
  Kollisionsschutz sitzt nur im Kontingent-SAVE (id-Lookup), NICHT im Saldo-READ.
- P2 (Anzeige, nicht lohnrelevant): `_absStats` Volltag-als-Halbtag bei knapp unternorm; AuswertungView
  ausstehend-Zähler ohne Datumsfilter (HomeView ist der bewusste Sonderfall).
Beide P1 an Sebastian (latent + lohn) — nicht autonom geändert.

## First-read für die Folge-Session
`ROADMAP.md` (🎯-Sektion) + diese Datei + `docs/handoff/STAND-2026-06-18-PLANRADAR.md`.
