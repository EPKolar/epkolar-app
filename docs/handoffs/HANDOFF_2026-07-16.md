# Handoff 16.07.2026 (Nacht) — Dispo-Assistent A1–A3 live, Rest offen

**Arbeitsklon:** `C:\repos\epkolar-app`. Push `git push origin main`, Remote-Verify per curl raw/github.io.
**Stand HEAD:** **v3.9.716** (`6ab3216`). **pytest 1590 grün.** Working tree clean, alles gepusht.
**Vorlauf:** siehe `HANDOFF_2026-07-15.md` (🚛-Display-Root-Cause, CLEANUP S1+S3 ausgeführt, Theme, Perf-1a).

> **Pflicht-Gates je Push:** `node_check` (0) · `_bracket_check` (`() -1` = **raw-Datei-Baseline `(-7,0,0)`**,
> siehe `test_invariants.py`) · `_check_version.js` · voller pytest lokal · Browser-Smoke (Boot, APP_VERSION,
> `#root`, 0 Script-Errors). **Kein optional chaining** (`?.`) — Samsung-TV/Sucrase. Build-Agenten NEIN,
> Review-Agenten OK (read-only, 20-min).

---

## ✅ Nacht gebaut & gepusht (v713 → 716) — Dispo Etappen 3+4

| Version | Inhalt |
|---|---|
| **v713** `db60d45` | Rechenkern-Fundament (pure `//@DISPO` ~Z.4579): `_dispoAdrKey`, `_dispoDauer`, `_dispoHaversine`, `_dispo2opt`, `_dispoKapazitaet` + Konstanten. |
| **v714** `cf9dfce` | E3-Verdrahtung: `_dispoTopf`, `_dispoAbwAbzug` (N8), **`_dispoPlan`** (Greedy + Bündelung + 2-opt, harte Wand, Warteliste). |
| **v715** `ebbc462` | **E4-Tab sichtbar** „🗓 Vorschlag" (Gate Büro/PL/Admin): `_dispoBuildInput` (App-Brücke, Folgewoche) + `DispoPanel` (Wochenraster Monteur×Mo-Fr, gestrichelte Chips, Auslastungsbalken, Warteliste). Browser-verifiziert: gleiche Adresse → gebündelt. |
| **v716** `6ab3216` | **E4b Übernahme**: „✓ Übernehmen" pro Chip → regulärer `updAs`-Edit (terminBestaetigt+monteur+dauer) → OFFA-Push, byte-gleich zu manuell. fz_bedarf nie gepusht. |

**Der Dispo-Tab ist sichtbar UND aktionierbar.** Stufe 1: `dist=0` (keine Geo-Daten) → Bündelung + Kapazität +
Load-Balance greifen; echte km/Fahrzeiten folgen mit `plz_geo`/`plz_distanz` (Sebastian-Gate „geo machst du").

---

## ⏳ Nachtlauf-Rest (exakte Wiederaufnahme, Reihenfolge)

### A4 — E2-UI 🚛-Block im AS-Form (fz_bedarf sichtbar/setzbar)
SQL `sql/AS_FZ_BEDARF_v1.sql` gestaged (Gate). Lesepfad da (`_mapArbeitsschein.fzBedarf`, v712). **Offen:** UI-Block
„🚛 Fahrzeug-Bedarf (nur App)" im AS-Formular — Anker: Formular-Render **~9588** (bei arbeitsanweisungen), außerhalb
`_asMtLocked` (**~8842**, auch für Monteure sichtbar), Save-`_lockedKeys` **~8929**. Typ-Chips (Steiger/LKW/Anhänger/
Stapler/Kompressor) → `form.fzBedarf` (jsonb-Array **als Objekt** im Body, v502-Lektion), 🚛-Badge in der Liste.
**42703-tolerant Pflicht:** Feature-Detect (`select=fz_bedarf&limit=1` einmal → Flag) — vor dem SQL-Run darf ein
fz_bedarf-Write den AS-Save NICHT brechen. `form.fzBedarf` bei `openEdit` aus `a.fzBedarf` laden.

### A5 — Büro-Extras (Reihenfolge): Chip-Telefon-Detail (`kund_tel` + `AK_KONTAKT_TELEFON1-3` als `tel:`, v548) →
„📞 vereinbart"-Status (macht Prio 1) → Morgen-Check-Kopfzeile → Tagesplan-PDF (jsPDF-Hausmuster).
**Dazu offen aus A3/A4:** Ablauf-Startzeiten (`terminZeit` ab 07:00, `PUFFER_JE_STOPP`, 5-min-Raster) in `_dispoPlan`/
DispoPanel → in die Übernahme mitschreiben; **„Alle übernehmen"** (Confirm) + ↑-Push-Symbol + `push_error` sichtbar;
Doppel-Übernahme-Guard (Chip nach Übernahme markieren, `_dispoBuildInput` filtert übernommene ohnehin via
„kein Termin/Monteur" beim nächsten Recompute).

### Block B — Bug-Hunt (nicht durchgeführt, Review-Agent gestoppt bei Session-Ende)
Fokus: Dispo-Kern (A1–A4), Theme v711, Perf-1a addTank-offline, fzBedarf, Kiosk-Loop-Guards, Pinch v708/709.
Prüf-Kandidaten für den Dispo-Kern (selbst gegenprüfen): `_dispoBuildInput` Folgewoche-Montag an Monats-/Jahresgrenzen;
`isoWof/isoWYof`-Key vs `wpHistory`-Key; absMap-Key `Name_ISO`-Match; Prio-1 `terminBestaetigt.slice(0,10)` vs `t.iso`;
`_dispoPlan` Score-NaN bei kap=0; Doppel-Übernahme. P1 sofort fixen+Test, P3 → `docs/BUGHUNT_2026-07-16.md`.

### Block C — Dead-Code Phase 2 Batch 2 (`docs/DEADCODE_2026-07.md`, v698-Muster, je Symbol 0-Ref-Beweis, max 8, test-gepinnte tabu).

### Block D — Handoff (dieses File) + Endreport. **Immer als letzter Push.**

---

## ⏳ Offene Sebastian-Gates (Zuruf / Einzel-ja)
- **geo** („geo machst du"): `sql/PLZ_GEO_v1.sql` + `sql/PLZ_DISTANZ_v1.sql` ausführen + OSRM-Einmallauf + INSERTs + 5-Strecken-Validierung.
- **AS_FZ_BEDARF**: `sql/AS_FZ_BEDARF_v1.sql` ausführen (Client 42703-tolerant, Push vorher gefahrlos).
- **CLEANUP S4** (je wörtliches „droppen"): `weekplans` (11 Z.) · `urlaubskontingent.urlaub` (0 Non-NULL, gefahrlos) · `_backup_arbeitsscheine_status_pre_a2` (108 Z.) · Rotation `notifications`/`activity_log`.
- **Perf-Migration** `_migrateTankFotos {go:true}` / Admin-Button (24 Alt-Base64-Tankfotos).
- **KV-Proposals** (`docs/KV_PROPOSALS_2026-07.md`, 4 Näherungen).
- **Dispo-Produktdefaults** bestätigen: Folgewoche/Mo–Fr, „offen"-Definition, Störungs-Topf.
- **PAT workflow-scope**.

## Dauer-Tabus
`_juprowaPush`/`_juprowaSanitize` · Auth/`auth.users` · `guard_urlaub_edit` · RLS-Writes · OFFA-Direkt ·
`.github/workflows/*` · DDL-Ausführung durch CC (SQL nur stagen) · Kiosk-Rollen byte-identisch wo nicht beauftragt.
