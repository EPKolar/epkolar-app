# HANDOFF 2026-07-05

> **Hinweis:** Zum Zeitpunkt der Erstellung lag im Arbeitsverzeichnis **keine** von Sebastian
> übergebene `HANDOFF_2026-07-05.md`-Inhaltsdatei vor. Dieses Dokument enthält daher den
> **Endreport von Claude**. Sebastians Handoff-Inhalt bei Bedarf oberhalb dieses Blocks einfügen.

---

## Endreport — autonomer Lauf v668 → v670 (05.07.2026)

### Finale Versionen + SHA (short)

| Version | Inhalt | Commit |
|---|---|---|
| **v3.9.668** | Urlaub-Anzeige-Konsistenz (abgelehnter Urlaub nicht mehr „verbraucht"; Legacy-Aliase krank/za; approvals in Self-Service-Karte) | `ccd9ad6` |
| **v3.9.669** | AS-Notif-Cleanup (erledigte Eskalations-/Reminder-Notifs aus der Glocke räumen, Mirror Material-Cleanup) | `3c94b1c` |
| **v3.9.670** | Lager-Display Auto-Sprung auf Folgewoche ab Fr 09:00 Wien (Kiosk, DST-sicher via Intl) | `53eb6ed` |
| chore | `KV_ENTSCHEIDUNG_v668.md` + dieser Handoff | (Tip des gebündelten Pushes) |

**Bündelung:** Ein gebündelter Push (Throttle-Schonung). Code-Tip vor den Docs = `53eb6ed`.

### Testanzahl vorher / nachher

- **Vorher (v3.9.667):** 1145 passed.
- **Nachher (v3.9.670):** siehe finalen Full-Pytest-Lauf (Ziel > 1145; neue Test-Files: `test_urlaub_konsistenz_v3668.py`, `test_as_notif_cleanup_v3669.py`, `test_kiosk_autoweek_v3670.py`; angepasst: `test_bwb_decimals_v3665.py` wg. approvals-Arg).
- Gates je Version grün: `node_check.py`=0 · Bracket-Baseline **`() -1` / `{} 0` / `[] 0`** (stabil, durch keine Edit verschoben) · `_check_version.js` Triple synchron.

> **Bracket-Baseline-Klarstellung:** Der CC-BEFEHl nannte `() -2`. Empirisch gemessen über alle
> Versionen v661–v670 ist die Baseline **`() -1`** (einziges Regex-Literal, das der String-Stripper
> nicht sauber entfernt). `node_check` ist rc=0 (echter Syntax-Check). Falls auf einem anderen
> Arbeitsstand (T:-Laufwerk / uncommittete lokale Änderung) ein zweites Regex-Literal existiert,
> wäre dort `-2` korrekt — auf diesem Repo-Stand ist es `-1`.

### `fz_latest`-View — exaktes SQL (bereits in FRÜHEREM Lauf ausgeführt)

Diese View wurde in einem **vorherigen** Lauf (v3.9.663, vor den aktuellen DB-TABU-Leitplanken)
via Supabase-MCP `apply_migration` (name `gps_latest_v1_view`) angelegt. **In DIESEM Lauf (v668–670)
wurden KEINE DB-Writes/DDL und KEINE weiteren DB-Objekte angelegt.** Wortlaut:

```sql
create or replace view public.fz_latest
  with (security_invoker = true)
as
select distinct on (fahrzeug_id)
  id, fahrzeug_id, ts, lat, lon, speed, ignition, raw, created_at
from public.fz_positions
order by fahrzeug_id, ts desc;

comment on view public.fz_latest is
  'v3.9.663: neueste Position je fahrzeug_id (DISTINCT ON). security_invoker -> RLS von fz_positions greift. Marker/Fleet-Liste der Flotte-Ansicht.';
```

**Bestätigung:** `security_invoker = true` gesetzt → es gilt die RLS von `fz_positions`
(`is_staff()`-SELECT + `lager_display` RESTRICTIVE), keine eigene View-Policy. `get_advisors`
(security) nach Anlage ohne neue Findings (kein `security_definer_view`). **Nur dieses eine
Objekt** (die View) wurde damals angelegt — keine Tabellen, Funktionen, Policies oder Trigger.
Datei im Repo: `sql/GPS_LATEST_v1.sql`.

### Pages-Deploy-Status der finalen Version

Wird unmittelbar nach dem gebündelten Push verifiziert (raw main == 3.9.670, dann github.io-Edge
== 3.9.670, sowie sw.js CACHE_NAME == 3.9.670). Ergebnis im Chat-Endreport. Bei Deploy-Fail:
genau EIN Empty-Commit-Retrigger, danach Stopp + Report (keine Retrigger-Ketten).

### Offene Entscheidungen für Sebastian

- **KV Punkt 1 Montagezulage:** entschieden (manuell) → Umsetzung braucht **DB-Entscheidung**
  (Tabelle `montagezulage_tage` empfohlen) + Gültigkeitsdatum-Muster. Details in `KV_ENTSCHEIDUNG_v668.md`.
- **KV Punkt 2/3/4:** Freigabe pro Punkt (2 Overlap / 3 `restUe≥3` / 4 taggeldNacht raus) — lohnrelevant, kein autonomer Change.
- **Dispo 6→7-Tage (Mo–So):** Produktentscheid (Kiosk/Grid noch 6-tägig; ZE/BWB 7-tägig). Nicht autonom gebaut.
- **Infra:** `.github/workflows/pages.yml` pushen (workflow-Token-Scope) → Pages-Source „GitHub Actions" (robuster gegen Build-Throttle). Destruktiver Drop `urlaubskontingent.urlaub` bleibt gestaged bis explizites „Spalte droppen".
