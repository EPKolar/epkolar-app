# Handoff 2026-07-21 — v3.9.783→785 gebaut + live

**Arbeitsklon** `C:\repos\epkolar-app`. **HEAD = v3.9.785** (`ed1aaea`), **working tree clean** (bis auf eine untracked Vorsession-Notiz, s.u.), gepusht + **live verifiziert** (raw main + github.io Edge beide auf 785; DDL gelaufen). pytest-Stand **1876 passed, 13 skipped**. Session-Kette: 783 (EZ-Abwesenheit) → 784 (Kiosk-Hardreset) → DB-Diagnose Riedmann → 785 (EZ 3-Stufen). **Queue (Sebastian bestätigt, NACH Chat-Claude-Tarball-Abnahme):** (1) read-only AS-Zeit-Klärung, (2) Konflikt-Warnung + Chef-Kachel-Kosmetik; AS-Zeit-Bau erst nach Entscheid.
**Pflicht-Gates je Push:** `python scripts/node_check.py index.html` (0) · `python scripts/_bracket_check.py index.html` (Baseline **`() -1`** — DIESEM Skript trauen, Subagenten melden teils falsch `() 12`) · `node sql/_check_version.js` · voller `python -m pytest tests/ -q` NACH dem Bump · Headless-Mount (Playwright; verklemmt sporadisch → dann statische Symbol-Verifikation + node-eval + Sicht-Check). Kein `?.`. sw.js-Bump GETRENNT lesen/schreiben (nie `open('w').write(open().read())` — truncatet). Version-Tripel (APP_VERSION ~Z.2811 via Python + SW_VER + sw.js CACHE_NAME).

## Abgenommen (Chat-Claude 21.07.): v775–782 BESTANDEN
Nacht-Batch live & verifiziert (Details `HANDOFF_2026-07-20_NACHT.md`). `_ezDayEff`/`_ezEffTage` waren bis v782 hash-identisch (lohnrelevanter Kern).

## ✅ ERLEDIGT — v3.9.783 (LOHNRELEVANT, Sebastian FREIGEGEBEN) — GEBAUT + LIVE (`355ad96`)
**EZ-Vorbelegung schließt genehmigte Abwesenheit aus.** Befund (Sebastian PDF-Sichtprüfung): Krank-Tag 01.07. Riedmann wurde fälschlich EZ-vorbelegt (`_ezDayEff`/daysMap rechnete nur `time_entries>6h`, ignorierte absences). Lohnzettel-Regel LA 2740: EZ NIE auf genehmigten Abwesenheitstagen. **Umgesetzt genau nach Spec:**
1. `_ezDayEff(std,flagEntry,absGenehmigt)` — 3. Param: `return (flag?…):(((std>6))&&!absGenehmigt)`. Flag-Override UNVERÄNDERT (explizit `aktiv=true` zählt AUCH auf Abwesenheitstag). Fehlt der Param → wie vor v783.
2. `_ezEffTage(daysMap,flags,wid,satz,absSet)` — neuer `absSet`={iso:true}-Param, an jeden `_ezDayEff` durchgereicht; fehlt → rückwärtskompatibel.
3. PURE Helfer `_ezAbsSet(abs,approvals,workerName)`→{iso:true} für GENEHMIGTE Abwesenheit (alle Typen; Key `name_iso` wie `_pzeBuildRows`/`_krankByMA`; Statusauflösung INLINE wie `_resolveApprK`, damit **node-eval-fest** — `_resolveApprK` liegt erst bei Z.20403, außerhalb des von den Tests extrahierten Modul-Blocks). window-exportiert.
4. Aufrufer reichen durch (kein neuer Fetch): KVZulagenReport `rows` (`_ezAbsSet(props.abs||{},props.approvals,_nm)`) + `_ezCount`/`_ezToggle`/EZKalender (neuer `_ezAbsDaysFor`: {iso:{label,color}} **auf der `_ezAbsSet`-Authority** aufgesetzt → keine Divergenz zur abgerechneten Menge), `_pzePdf` (`_ezAbsSet(abs||{},approvals,worker.name)`).
5. EZ-Kalender: Abwesenheitstage grau-gestrichelt mit AT_T-Label + Legende-Chip „Abwesenheit (kein Vorschlag)". Klick = Override (dazuzählen).
6. KONFLIKT-MARKER (genehmigter Fehlgrund UND `projMin>0`): PDF-Notiz „Konflikt Abw/Proj" (ASCII, `_pdfStr` strippt ⚠) + On-Screen-PZE „⚠ Konflikt" Desktop-Notiz-Zelle + Mobile-Card „⚠ Konflikt: Abwesenheit + Projektbuchung".

**€-Ergebnis verifiziert:** Riedmann Juli 8 Tage/93,68 € → **7 Tage/81,97 €** (01.07. krank raus; explizit dazugeklickt zählt wieder). Node-eval + In-Browser (Headless-Mount) beide bestätigt {tage:7,sum:81.97}.
**Gates alle grün:** node_check exit 0 · bracket `() -1` (Baseline gehalten) · version-Tripel 783 synced · voller pytest **1863 passed/13 skipped** · Headless-Mount (Playwright gegen lokal servierte Kopie): React-Root gefüllt, **0 Konsolen-Fehler**, live-geladene Helfer korrekt.
**Tests:** Riedmann-Pins in `test_ez_kalender_v775.py` + `test_pze_pdf_v776.py` auf 7/81,97 (alt 8/93,68 bewusst obsolet); neue `test_ezabsset` + `test_v783_abwesenheit_ausschluss_verdrahtung` + `test_v783_konflikt_marker`; v777-„unverändert"-Pins (`_ezDayEff` byte-Pin + Toggle-String) auf die freigegebene Kernänderung angepasst, RÜCKWÄRTSKOMPAT (ohne absSet = 8/93,68) bleibt gepinnt.
**Grenzen eingehalten:** kein DDL, absences READ-ONLY, `_ezSet`/Schreibweg + `taggeldAb6h:11.71`/`_kvTaggeldTag` unverändert.

## ✅ ERLEDIGT — v3.9.784 — Kiosk-Ansicht pro Tab ueberlebt SW-Update-Hardreset (`d86e97a`, live)
Lager-Display faehrt 2 Tabs (?screen=planung + ?screen=monteure); beim Hardreset sprang der Planungs-Tab auf die Monteurtafel. **Ursache (nicht der vermutete localStorage-Overwrite):** die Top-Level-Kiosk-Ansicht wird NICHT in localStorage persistiert; `_scr` kam rein aus `?screen`. `_forceCacheClear` (index.html:2887) reloadete auf `pathname+"?cc="+ts` und verwarf `?screen` → `_scr` fiel auf Default `'monteure'`. Beide Tabs verloren unabhaengig ihre Query, nur der Planungs-Tab sichtbar. **Fix (reines Routing/Persistenz):** (1) `_forceCacheClear` setzt cc ADDITIV via `new URL()` → `?screen`+`#hash` bleiben; (2) PURE `_kioskScreenPick(hash,query,session)` Prioritaet Hash>?screen>sessionStorage; (3) Boot-Pin per-Tab (`sessionStorage epk_kiosk_screen` + `#hash`, ueberlebt den `?cc=`-Reload); (4) `_scr` liest `window.__kioskScreen`. Auth/RLS/Kiosk-Gate/SW-Update-Entscheidung unberuehrt; Einzel-Tab-Handys unveraendert. **Headless verifiziert:** `?screen=planung` gepinnt, dann `?cc=<ts>` ohne screen → aus sessionStorage restauriert = planung (Sprung behoben), 0 Konsolen-Fehler. Gates: node 0, bracket `()-1`, version 784, pytest **1868/13**. Tests `test_kiosk_screen_persist_v784.py`; `test_stempel_terminal_v3695` `_scr`-Pin angepasst (Terminal-Zweig unveraendert). Auftrags-Punkt 3 (Kiosk schreibt geteilten localStorage-Key nicht mehr) = gegenstandslos, existierte nie.

## ✅ ERLEDIGT — v3.9.785 — Entfernungszulage 3-Stufen (LOHNRELEVANT + DDL, `ed1aaea`, LIVE)
KV ab 01.01.2026: **klein 11,94 / mittel 30,00 / groß 62,04** EUR/Tag, GENAU EINE Stufe pro Tag, nie additiv, Nächtigungsgeld unbenutzt. Alt-Satz 11,71 war FALSCH → 11,94. **DDL gelaufen** (Human-Gate, Chat-Claude verifiziert: stufe-Spalte da, aktiv nicht gedroppt, 8 Zeilen aktiv=true→klein, keine Waisen) — `sql/ENTFERNUNGSZULAGE_STUFE_v1.sql` (additiv, idempotent, aktiv NICHT gedroppt = separater Drop-Auftrag später). **Kern:** `_ezDayEff`→Stufe ('klein'/'mittel'/'gross'/''), Flag-Stufe-Override gewinnt (zählt auch am v783-Abwesenheitstag; null=keine); ohne Flag genehmigt-abwesend→'' sonst >6h→Vorschlag klein. `_ezEffTage`→`{tageKlein,tageMittel,tageGross,sum}`, Summe je Stufe×Satz, kein Deckel, Sätze via `_ezSaetze(kv)`. Neue PURE-Helfer `_ezStufeMeta`/`_ezStufeFromRow`(Migration-tolerant)/`_ezCycleNext`(Zyklus Vorschlag→klein→mittel→groß→keine→Vorschlag, „keine" nie in 1 Klick)/`_ezSaetze`; window-exportiert. `_ezSet(wid,datum,stufe,von)`: undefined=Zeile löschen, sonst upsert stufe+aktiv; `_ezFetch` liest stufe. **UX:** Kalender-Klick-Zyklus, Kachel zeigt ausgeschriebene Stufe + Farbe (grün/orange/violett) + Stunden, Legende, Abwesenheits-Label (v783) bleibt, Zähler „N klein / M mittel / G groß" ausgeschrieben. **PDF/Report:** ausgeschriebene Stufe je Tag in Notiz + Fuß je Stufe (klein/mittel/groß + Summe), EINE Rechenquelle `_ezEffTage`. **Zulagensatz-Maske im Büro-Portal** (KVZulagenReport, admin+büro via bueroexport-Gate, Persistenz system_config.kv_rules gemergt — nur 3 EZ-Felder) → 2027-Sätze anpassbar. **€-Beispiele (node-eval + pytest verifiziert):** 7 klein=83,58 · 5 klein+2 groß=183,78 · 3 klein+1 mittel+1 groß+2 keine=127,86. **Gates:** node_check 0, bracket ()-1, version 785 synced, **voller pytest 1876 passed/13 skipped**. KV_RULES: taggeldAb6h 11,71→11,94 (Feldname bleibt für klein), NEU ezMittel/ezGross; Legacy taggeldAb11h/taggeldNacht bleiben „ohne Funktion". 7 Alt-Test-Pins mit Begründung nachgezogen, alte €-Werte (81,97/93,68) als bewusst-obsolet in Docstrings markiert. **Noch offen (separat):** `alter table … drop column aktiv;` erst wenn v785 stabil. **Chat-Claude Tarball-Abnahme ausstehend** — bis dahin KEIN Queue-Punkt.

## 🔎 DB-DIAGNOSE (read-only, Sebastian autorisiert) — Riedmann w9 Projektzeit am Krank-Tag 01.07.
**Frage:** Warum existiert die 7,5h-Projektbuchung am genehmigten Krank-Tag 01.07.? **Ergebnis:** KEIN Datums-Shift, KEIN UTC-Grenzen-Bug. time_entry `ze_1782911762262_6ml6` (7,5h, 07:00–15:00, Projekt p4 „DR.-GSCHMEIDLERSTRASSE 10", KEIN Arbeitsschein) wurde **created_at 01.07. 15:16 Wien** auf ein **user-gewaehltes Datum 01.07.** geschrieben (`addEntry` index.html:23481 `date:addDay`, nicht `new Date()`). Absence Riedmann_2026-07-01 (krankenstand, genehmigt Sebastian) angelegt 01.07. 07:14 Wien. **activity_log hat KEINE Spur** (time_entries werden nicht geloggt; keine `created_by`-Spalte) → Eigen- vs. Fremdbuchung DB-seitig nicht beweisbar; das taegliche Nachmittags-Eigenerfassungs-Muster (45 Zeilen) spricht klar fuer **Eigenerfassung**. Systematik-Stichprobe alle 45 w9-Zeilen: die einzigen „Datum≠created-Tag"-Faelle sind **nachtraegliche Erfassungen** (Tage/Wochen spaeter), kein 23:xx-Mitternachtsfenster. **Befund fuer Buero:** Widerspruch liegt in den Stammdaten (Krank-Beginn evtl. falsch datiert ODER an gemeldetem Krank-Tag gearbeitet), NICHT im Schreibweg — der v783-Konflikt-Marker zeigt den Tag jetzt an. Kein Fix ohne separaten Auftrag.

## OFFEN (nicht autonom — Sebastian)
- **Datenbereinigung 01.07. Riedmann**: genehmigter Krank-Tag MIT Projektzeit-Buchung = widersprüchliche Datenlage; der v783-Konflikt-Marker macht es jetzt in PZE-PDF + On-Screen sichtbar, die Bereinigung (welche Buchung stimmt) ist Büro/Sebastian.
- **Heute-Ring-Farbe** EZ-Kalender: EP-Grün (nah am „vergeben"-Grün) vs. Blau — Sebastian entscheidet nach Sicht.
- **Live-Sicht-Co-Verifikation v783** durch Chat-Claude/Sebastian steht noch aus (Muster wie bei Dispo-Wellen): EZ-Kalender-Label + Konflikt-Marker im echten Büro-Portal mit echten abs/approvals einmal ansehen.

## Untracked (nicht von v783, NICHT committet)
- `docs/STEMPELUHR_STATUS_2026-07-20.md` — Vorsession-Notiz, liegt untracked im Tree. Bewusst NICHT in den v783-Commit gezogen (außerhalb des Auftrags). Sebastian entscheidet, ob sie eingecheckt oder verworfen wird.

## DEAD-CODE-NACHTSESSION-LISTE (eigene Runde)
(1) `OFFA_SB_MAP` raus (v698, tot seit v780) · (2) `test_pze_v3692::test_group_by_day_utc_rollover_wird_lokal_korrigiert` host-TZ-abhängig → Test muss TZ selbst pinnen (env/monkeypatch) · (3) `conftest.py` timeout=10 · (4) Klon-Extraktoren feiertag/hunt.

**Memory-Quellen:** `project_epkolar_session_2026-06-24` (Batch + Gotchas), `reference_epkolar_repo_path`, `feedback_epkolar_pytest_io`.

---

## Nachtrag — v3.9.806: Home-Schnellzugriff "📷 Foto aufnehmen" entfernt (chirurgisch, v698-Muster)

**HEAD = v3.9.806.** Ein Commit. Entfernt wurde im Home-Dashboard-Schnellzugriff-Block `dv('schnell')`
(~Z.13908) genau EIN `<label>`-Element — der einzige Aufrufer mit den Parametern
`captureAndQueue(f,"baustelle","schnellfoto","","Schnellfoto Baustelle")`. Das Element stand als letztes
Geschwister-Argument NACH dem `[array].filter(hasPerm).map(...)` im umschließenden
`React.createElement('div',{...flex...})`; entfernt inkl. des unmittelbar vorangehenden Kommas, sodass
der `div` sauber mit dem `.map(...)`-Argument schließt (`div(style, arrayMap)` — valide).

**Entfernte Zeile (wörtlich, ehemals Z.13908):**
`, React.createElement('label', { style: {...#f97316...} }, "📷 Foto aufnehmen" , React.createElement('input', { type:"file", accept:"image/*", capture:"environment", multiple:true, ..., onChange: e=>{ ... captureAndQueue(f,"baustelle","schnellfoto","","Schnellfoto Baustelle"); ... } } ))`

**Byte-identisch geblieben:** `captureAndQueue` (Aufrufer 8 → 7) + PhotoQ + getGPS; die anderen Foto-Buttons
(Mangelmeldung/AS-Formular/Fotos-Tab/Projekt-Schnellfoto/Tank-Schaden/Provisorien/Bautagebuch); die
restlichen Schnellzugriff-Nav-Buttons (Neues Projekt/Arbeitsscheine/…/Tanken).

**Gates:** node_check 0 · bracket `() -1` · version 3.9.806 synced (APP_VERSION+SW_VER+CACHE_NAME) ·
voller pytest **1956 passed / 13 skipped** · Browser-Mount grün (`captureAndQueue` intakt, 0 JS-Errors).
Kein Juprowa, kein DDL. (Kein neuer Test — reine Entfernung; kein Bestandstest pinnte den Button.)

---

## Nachtrag — v3.9.807: Dispo 3B — Auftragstyp als Dauer-Signal in die Quellen-Kette

**HEAD = v3.9.807.** Ein Commit. PLANUNGSRELEVANT, kein Lohn. Kein Juprowa, kein DDL.

**DATENLAGE (ehrlich):** Heute erreicht KEIN Auftragstyp n>=8 abgeschlossene Scheine mit gesetzter Dauer
(Median-Befund 3B-Stufe-1: nur störung n=28/Median 120 und kein n=8/Median 90 — beide tragen aber KEIN
Typ-Dauer-Signal). Das neue Signal liefert daher vorerst nur die **Fallback-Schätzwerte**; der echte
Typ-Median (`_dispoMedianJeTyp`) greift automatisch, sobald je Typ n>=8 abgeschlossene Scheine mit gesetzter
Dauer vorliegen.

**Neu:**
- `DISPO_TYP_DAUER_FALLBACK={reparatur:120, montage:180, mangelbehebung:90, garantie:90, lieferung:30}`
  (Startwerte ohne Realdaten, Sebastian). `kein`/leer/`stoerung` bewusst NICHT enthalten — kein Typ-Signal
  (störung läuft über Topf 0).
- PURE `_dispoMedianJeTyp(scheine)` — Median der GESETZTEN Dauern abgeschlossener Scheine je scheinart
  (analog `_dispoMedianJeKlasse`; nur die 5 echten Typen).
- `_dispoDauer`-Signatur → `(schein,regeln,gelernt,typMedian)`. Neue Ergebnis-Reihung:
  **gesetzt > Klassen-Median(n>=8) > Typ-Median(n>=8) > Typ-Fallback > Text-Keyword > Default.**
  Das Typ-Signal greift NUR bei übergebenem `typMedian` (Wiring-Stellen); fehlendes `typMedian` = altes Verhalten.
- Verdrahtung: `var _typMed=_dispoMedianJeTyp(scheine);` neben `_gelernt`; `_typMed` an die 2 Planner-`_dispoDauer`-
  Aufrufe (Plan-Chips + Kapazitäts-Abzug fixer Termine). Warteliste-Chip-Aufruf `_dispoDauer(s)` bleibt (backward-compat).

**Byte-identisch:** `_dispoParseDauer`, `_dispoMengeFaktor`, `_dispoMedianJeKlasse`, `DISPO_DAUER_REGELN`,
`_dispoTopf`/`_dispoPrioRang`/`_dispoTypRang`, Score/Kapazitätswand. Kein neuer Fetch.

**Gates:** node_check 0 · bracket `() -1` · version 3.9.807 synced · voller pytest **1964 passed / 13 skipped** ·
Browser-Mount grün (`DISPO_TYP_DAUER_FALLBACK.reparatur=120`, `_dispoMedianJeTyp` intakt, 0 JS-Errors).
Tests: `tests/test_dispo_typdauer_v807.py` (node-eval Median-je-Typ + volle Prioritäts-Kette + "ohne typMedian
identisch zu vorher"). Nachgezogen: `test_dispo_asfrische_abw_v767` Slice-Fenster 6000→7500 (der `_typMed`-Build
schob die absAbz-Zeile auf Offset 6003; Inhalt unverändert).

---

# ═══ SESSION-ABSCHLUSS 21.07.2026 (Kontext voll) — Resume-Punkt ═══

**HEAD = v3.9.807 (`e876b9e`), working tree clean, auf origin gepusht. pytest 1964 passed / 13 skipped.**
Arbeitsklon `C:\repos\epkolar-app`. Pflicht-Gates je Push: node_check 0 · bracket `() -1` · node sql/_check_version.js ·
voller pytest lokal grün · Browser-Mount (Playwright gegen `python -m http.server 8899`, 0 Console-Errors, #root gefüllt).

## Diese Session gebaut + live (v793 → v807), alle Chat-Claude-abnahmefähig, je 1 Commit
- **v793** Rollback Navigation-Etappe-2 (`git revert` v792 — System B `_navPush/_subViewRef` trug nirgends).
- **v794/795/796** Navigation-Neuaufbau auf **System A `useBackLayer`**: E1 Admin-Sub-Tabs · E2 AS-Schein-Detail
  (Ursprungs-Sub-Tab via `_asPrevSub`) · E3 Chef+Büro-Sub-Tabs (Mount-Default in Ref eingefroren, localStorage-Tabs).
- **v797 → v799** Scheinart-Push gebaut (AK_AUFART) → **wieder rausgebaut**: OFFA importiert AK_AUFART NICHT und
  re-exportiert seinen Stand (App-Push war stiller Revert). scheinart ist **Pull-only, OFFA=Wurzel**. Liste read-only,
  Formular disabled+Badge. `JUPROWA_PUSH_FIELDS` wieder bei 8 Push-Keys.
- **v798** Prio-Heilung: offener Schein mit Prio `keine` → `normal` + Push (AK_PRIOR 4) via `_prioHeilBedarf`; „keine" aus den Selects.
- **v800** Dispo **c-1A** Prio-Feinstufe (`_dispoPrioRang`: FIXTERMIN<sehr hoch<hoch, normal<niedrig) im Greedy-Sort.
- **v801** AS-Freitext-Suche findet jetzt Prio + Auftragstyp (deutsche Labels im `_hay`).
- **v802** Büro+Chef-Sub-Tab-Leisten auf flachen **AdminPanel-Stil** (nur Optik; minHeight:44 mobil, auch bei Admin).
- **v803** Kein-Dropdown-Grundsatz AS-**Formular**: Auftragstyp + Sachbearbeiter → Text (Liste unberührt).
- **v804** KV-**Zuschlags**-Report (lohnsensibel) raus aus Auswertung → Chef-Portal/Personal (Chef-Portal-Gate = admin ODER GF = Sebastian).
- **v805** Dispo **3A** `_dispoTypRang` als Gleichstand-Brecher NACH Prio (mangelbehebung<garantie<reparatur<montage<lieferung<kein).
- **v806** Home-Schnellzugriff „📷 Foto aufnehmen"-Button (Schnellfoto Baustelle) chirurgisch entfernt.
- **v807** Dispo **3B** Auftragstyp als Dauer-Signal (`DISPO_TYP_DAUER_FALLBACK` + `_dispoMedianJeTyp`; Kette
  gesetzt > Klassen-Median(n≥8) > Typ-Median(n≥8) > Typ-Fallback > Text-Keyword > Default).

**Dispo-Kette c-1A/3A/3B ist damit komplett.**

## GEPARKT (nicht deployt) — AS-Zeiten → Zeiterfassung-Übernahme
Fertig gebaut, in `docs/wip/`: `AS_ZEIT_UEBERNAHME_v801.patch` (Code) + `ZE_UEBERNOMMEN_v1.sql.staged` (DDL, **Human-Run-Gate**)
+ `test_as_zeit_uebernahme_v801.py.parked`. **BLOCKIERT auf 2 Dinge:**
1. **DDL `ze_uebernommen` (bool) MUSS VOR dem Code-Deploy laufen** (`docs/wip/ZE_UEBERNOMMEN_v1.sql.staged` → nach `sql/`
   kopieren + im Editor ausführen), sonst greift der Marker nicht und es entstehen Mehrfach-Einträge.
2. Sebastian-Entscheid **„spätere Schein-Zeit-Änderung nach Übernahme"** (Vorschlag: nichts / Hinweis ans Büro / Neu-Angebot).
**Design (Sebastian-freigegeben):** Trigger T1 (saveAs, fahrzeit/stunden>0 UND scheinstatus nicht aufgenommen/freigegeben);
EIN time_entry, hours=Arbeit+Fahrt (2B); Idempotenz über MARKER am Schein (`ze_uebernommen`) = genau EINMAL, danach
gehört der Eintrag dem Menschen (löschbar, kein Neu-Auto); project_id LEER (4B), taetigkeit „Arbeitsschein <Nr>";
time_entries NIE Push-Feld. Beim Un-Parken: Version renummerieren (nächste freie), Test entparken, DDL-Reihenfolge beachten.

## OFFENE ENTSCHEIDE / FRAGEN (Sebastian, nicht autonom)
- AS-Zeiten-Übernahme: DDL-Lauf + „spätere-Zeit-Änderung"-Regel (s.o.).
- SB + Monteur-Name auch ins AS-Freitext-Heu? (heute nicht drin).
- KVZulagenReport (Entfernungszulage/Taggeld) bleibt im **Büro-Portal** — ebenfalls einschränken?
- v533-Kundenstammdaten im AS-Formular auf Text? (Versuch verworfen — brach den Projekt-a11y-Standard „beschriftete
  Inputs"; disabled+labeled ist barrierefreier. Bräuchte a11y-bewussten Ansatz mit aria-label statt htmlFor.)
- 3A Garantie-Position: aktuell garantie VOR reparatur (Map `garantie:1/reparatur:2` — eine Zeile tauschbar).

## GOTCHAS (diese Session teuer gelernt)
- **OFFA importiert AK_AUFART NICHT** → scheinart ist Einbahnstraße OFFA→App, kein Push (v797→v799 Rückbau).
- **a11y-Test-Suite pinnt beschriftete Inputs** (≥20 htmlFor, Adressfelder) — disabled-Input→Text-Div bricht sie.
- **Fixed-Offset-Slice-Tests sind fragil**: `_dispoBuildInput`-Test slicte `start+6000`, mein `_typMed`-Build schob
  eine Zeile auf Offset 6003 → Fenster auf 7500 geweitet (Inhalt unverändert).
- Chef-Portal-Gate ist **admin ODER Geschäftsführer** (Z.~8517), NICHT strikt admin — Sebastian ist GF.
- sw.js-Header-Bump: der Header-Text wechselt je Version — beim Python-Bump den EXAKTen alten Header-Anfang matchen.

---

## Nachtrag — v3.9.809: AS-Zeit → Zeiterfassung-Übernahme (PRIO 1, Sebastian-Entscheid 21.07.)

**PLANUNGS-/LOHNRELEVANT, kein DDL (Spalte `ze_uebernommen` ist live), kein Juprowa.** Un-Park der geparkten
`docs/wip/`-Artefakte — der alte Patch war create-only (keine Rollen-Weiche), der geparkte Test pinnte das
verworfene 1C-DB-Read-Design; beides **verworfen**, die volle Ziel-Regel **frisch gebaut**.

**Ziel-Regel (umgesetzt):** Übernahme genau EINMAL je Schein über Marker `ze_uebernommen` (aus dem State) —
Trigger T1 (Zeit>0 UND Arbeit begonnen: nicht aufgenommen/freigegeben/storniert UND Monteur). Insert via
addEntry-Muster (`SQ.push /api/entries`), 2B `hours=(fahrzeit+stunden)/60`, 4B `project_id` leer +
`taetigkeit "Arbeitsschein <Nr>"`, dann `updAs({ze_uebernommen:true})`. Spätere Zeit-Änderung NUR bei echter
`fahrzeit/stunden`-Änderung (`_row` vs `s`): Monteur-Rollen auto (`_sbPatch`), Office (buero/pl/admin/unbekannt)
`_confirmModal`. Edge: Monteur schlägt Büro-Korrektur durch. Gelöscht bleibt gelöscht (`_sbGet=[]` → skip).

**Neu:** PURE `_asUebernahmeBedarf`/`_asUebernahmeStunden`/`_asZeitRolleConfirm` (window-exportiert, node-getestet)
+ async `_asZeitUebernahme` in ArbeitsscheinView + saveAs-Trigger. Test `tests/test_as_zeit_uebernahme_v809.py`.

**Adversariale Subagent-Review:** bestätigte Push-Sicherheit / gelöscht-bleibt-gelöscht / Marker-Reload-Durability
(`{...a}`-Spread + `select=*` + IndexedDB) / Scope. **4 Funde gefixt:** #2 Marker-Gate auf `_row` statt gemergtem
`s` (kein stale-Form-Shadow → kein Re-Create); #4 Update nur bei echter Zeit-Änderung (kein spurious Büro-Confirm /
kein stilles Zurücksetzen bei unabhängigem Save); #5 `storniert` aus T1; #7 Rollen-Denylist (Feld=auto, Office+
unbekannt=confirm).

**OFFEN für Sebastian (Review-Funde außerhalb Phasen-Scope — NUR NOTIERT, nicht gefixt):**
- **#1 Zwei-Geräte-Race:** Idempotenz ist rein client-seitig über den Marker; es gibt KEIN
  `UNIQUE(arbeitsschein_id)` auf `time_entries`. Speichern zwei Geräte (Monteur mobil + Büro Desktop) denselben
  Schein gleichzeitig als Ersteintrag (beide sehen Marker falsy), entstehen ZWEI Einträge. Härtung möglich ohne DDL
  (deterministische id `ze_as_<scheinId>` + insert-if-absent) oder mit DDL (UNIQUE).
- **#3 Marker vor Insert-Bestätigung:** Insert-POST und `updAs(marker)` sind zwei fire-and-forget-SQ-Items; scheitert
  der Insert dauerhaft (RLS-`WITH CHECK`-Edge, z.B. Fremd-Monteur-Schein), commitet der Marker trotzdem → nächster
  Save skippt → Eintrag fehlt still, obwohl „gebucht" gemeldet. Gängige Pfade (Eigen-Schein Monteur / Office) sicher.
- **#6 Eintrags-Datum:** date = `terminBestaetigt` (geplant), sonst Speicher-Tag. Bei Arbeit an anderem Tag als dem
  geplanten Termin landet die Stunde im falschen Lohn-Tagesbucket. Ziel-Regel hat das Datum nicht spezifiziert.

**Gates:** node_check 0 · bracket () -1 · version 3.9.809 synced · voller pytest **1970 passed / 13 skipped** ·
Browser-Mount 0 JS-Errors, #root gefüllt, Helfer + alle 4 Fixes live-verifiziert.

---

## Nachtrag — v3.9.810: AS-Freitext-Suche findet Sachbearbeiter + Monteur-Name

**Kein DDL, kein Juprowa, kein Lohn.** Der Freitext-Haystack `_hay` der AS-Liste (v801: Prio-Label +
Auftragstyp-Label) additiv erweitert um `(a.sachbearbeiter||"")` + Monteur-**Name** via Lookup
`(((monteure||[]).find(m=>m.id===a.monteur)||{}).n||"")` (Feld `.n`, kein optional chaining). `monteure`
in die useMemo-Deps (sonst kein Re-Run bei Monteur-Wechsel). Token-Logik (`_toks.every`), Filter-Dropdowns,
Sortierung byte-identisch. Recon per Subagent (Feld `.n` an 3 Stellen bestaetigt, `sachbearbeiter` String
ohne Map). Test `tests/test_as_freitext_suche_v810.py` (Superset: v801-Bestand + SB/Monteur + `?.`-Gegenprobe
+ `||{}`-Guard-Gegenprobe); v801-Test ersetzt. Gates: node_check 0, bracket ()-1, version 3.9.810 synced,
voller pytest **1971 passed / 13 skipped**, Browser-Mount 0 JS-Errors + SW_VER 810.

---

## Nachtrag — v3.9.811: AS-Zeit-Übernahme #3 — Marker erst bei bestätigtem Insert (lohnnah, PRIO 1)

**Kein DDL, kein Juprowa.** Review-Fund #3 aus v809 gefixt: `_asZeitUebernahme` setzte `ze_uebernommen:true`
DIREKT nach `SQ.push` (fire-and-forget), also bevor der Insert bestätigt war → scheiterte der Insert am Server
(RLS/Fremd-Monteur/403), war die Zeit weg UND der Marker sperrte jeden Retry (Erst-Übernahme läuft nur bei
`!ze_uebernommen`) → stiller Zeitverlust.

**SCHRITT 0:** `SQ` (Z.3104) ist eine persistierte Offline-Queue OHNE per-Item-Erfolgs-Callback (Callback
überlebt keinen Reload) → „Marker-im-Callback" nicht sauber. **Gewählt: Option A — awaited `_sbPost` mit r.ok**
(v703-Muster). Der Create-Zweig macht jetzt `await _sbPost("time_entries",{...},true)` in try/catch: bei
Fehler/offline → catch → Marker bleibt false + ehrliche Warn-Meldung + Retry beim nächsten Save; nur bei
bestätigtem Insert → `updAs({ze_uebernommen:true})`. Update-Zweig (`_sbPatch`, spätere Änderung) UNBERÜHRT.
`_asZeitUebernahme` bleibt async/fire-and-forget vom saveAs. Trade-off (akzeptiert): kein SQ-Offline-Auto-
Nachreichen für den Erst-Insert — stattdessen Retry beim nächsten Speichern.

Test `test_as_zeit_uebernahme_v809.py` erweitert: Insert-Format `await _sbPost`, Ordering-Assertion (Marker-updAs
NACH dem awaited Insert), catch-return-ohne-Marker. Gates: node_check 0, bracket ()-1, version 3.9.811 synced,
voller pytest **1971 passed / 13 skipped**, Browser-Mount 0 JS-Errors + SW_VER 811.
