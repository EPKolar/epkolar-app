# Handoff 2026-07-21 — Sessionstart (voriger Kontext voll)

**Arbeitsklon** `C:\repos\epkolar-app`. **HEAD = v3.9.782** (`b67d96d`), **working tree CLEAN**, alles gepusht + live (raw + github.io auf 782). pytest-Stand 1860 passed, 13 skipped.
**Pflicht-Gates je Push:** `python scripts/node_check.py index.html` (0) · `python scripts/_bracket_check.py index.html` (Baseline **`() -1`** — DIESEM Skript trauen, Subagenten melden teils falsch `() 12`) · `node sql/_check_version.js` · voller `python -m pytest tests/ -q` NACH dem Bump · Headless-Mount (Playwright; verklemmt sporadisch → dann statische Symbol-Verifikation + node-eval + Sicht-Check). Kein `?.`. sw.js-Bump GETRENNT lesen/schreiben (nie `open('w').write(open().read())` — truncatet). Version-Tripel (APP_VERSION ~Z.2811 via Python + SW_VER + sw.js CACHE_NAME).

## Abgenommen (Chat-Claude 21.07.): v775–782 BESTANDEN
Nacht-Batch live & verifiziert (Details `HANDOFF_2026-07-20_NACHT.md`). `_ezDayEff`/`_ezEffTage` waren bis v782 hash-identisch (lohnrelevanter Kern).

## ⭐ NÄCHSTER AUFTRAG — v3.9.783 (LOHNRELEVANT, Sebastian FREIGEGEBEN, NOCH NICHT GEBAUT)
**EZ-Vorbelegung schließt genehmigte Abwesenheit aus.** Befund (Sebastian PDF-Sichtprüfung): Krank-Tag 01.07. Riedmann wird fälschlich EZ-vorbelegt (`_ezDayEff`/daysMap rechnet nur `time_entries>6h`, ignoriert absences). Lohnzettel-Regel LA 2740: EZ NIE auf genehmigten Abwesenheitstagen.
**FIX (Kern-Änderung autorisiert):**
1. `_ezDayEff(std,flagEntry,absGenehmigt)` — 3. Param: `return (flag?…):((std>6)&&!absGenehmigt)`. Flag-Override UNVERÄNDERT (explizit `aktiv=true` zählt AUCH auf Abwesenheitstag; `aktiv=false` wie gehabt).
2. `_ezEffTage(daysMap,flags,wid,satz,absSet)` — neuer `absSet`-Param ({iso:true}), an jeden `_ezDayEff` durchreichen; fehlt → Verhalten wie vorher.
3. PURE Helfer `_ezAbsSet(abs,approvals,workerName)` → {iso:true} für GENEHMIGTE Abwesenheit (alle Typen krank/urlaub/za/sonder/pflege; Muster `_absShow`/`_resolveApprK`/`_pzeBuildRows`, Key `workerName_iso`, `status==="genehmigt"`). window-exportiert.
4. Aufrufer durchreichen (abs/approvals SCHON im Scope, KEIN neuer Fetch): KVZulagenReport `rows` (`_ezAbsSet(abs,props.approvals,name)`), `_pzePdf` (`_ezAbsSet(abs,approvals,worker.name)`), EZ-Kalender (absSet für gewählten MA als Prop).
5. EZ-Kalender: Abwesenheitstage optisch kennzeichnen (Label „Krank"/„Urlaub"/… aus abs-Map), damit Büro sieht WARUM nicht vorbelegt.
6. KONFLIKT-MARKER (Anzeige, PDF + On-Screen-PZE): Zeile mit genehmigtem Fehlgrund UND `projMin>0` → ⚠ „Konflikt: Abwesenheit + Projektbuchung" in Notiz-Spalte.
**€-BEISPIEL (Freigabe-Grundlage):** Riedmann Juli 8 Tage/93,68 € → nach Fix **7 Tage/81,97 €** (01.07. krank raus; explizit dazugeklickt zählt wieder).
**TESTS:** node-eval `_ezDayEff` (Krank+kein Flag→false; +aktiv=true→true Override), `_ezEffTage` Riedmann {tage:7,sum:81.97}, `_ezAbsSet` (genehmigt ja / beantragt-abgelehnt nein), Konflikt-Marker-Strukturtest. **Riedmann-Pin AKTUALISIEREN** (alt 8/93,68 in `test_ez_kalender_v775.py` + `test_pze_pdf_v776.py` → 7/81,97, im Commit begründen). `taggeldAb6h:11.71`/`_kvTaggeldTag` unverändert.
**GRENZEN:** kein DDL, absences READ-ONLY, `_ezSet`/Schreibweg unverändert. Playwright-Regel wie gehabt.
> Fertige Subagent-Spec lag vor; der Agent war beim Session-Ende erst in der Lesephase (nichts geschrieben) → sauber frisch bauen.

## OFFEN (nicht autonom — Sebastian)
- **Datenbereinigung 01.07. Riedmann**: genehmigter Krank-Tag MIT Projektzeit-Buchung = widersprüchliche Datenlage; der v783-Konflikt-Marker macht es sichtbar, die Bereinigung (welche Buchung stimmt) ist Büro/Sebastian.
- **Heute-Ring-Farbe** EZ-Kalender: EP-Grün (nah am „vergeben"-Grün) vs. Blau — Sebastian entscheidet nach Sicht.

## DEAD-CODE-NACHTSESSION-LISTE (eigene Runde)
(1) `OFFA_SB_MAP` raus (v698, tot seit v780) · (2) `test_pze_v3692::test_group_by_day_utc_rollover_wird_lokal_korrigiert` host-TZ-abhängig → Test muss TZ selbst pinnen (env/monkeypatch) · (3) `conftest.py` timeout=10 · (4) Klon-Extraktoren feiertag/hunt.

**Memory-Quellen:** `project_epkolar_session_2026-06-24` (Batch + Gotchas), `reference_epkolar_repo_path`, `feedback_epkolar_pytest_io`.
