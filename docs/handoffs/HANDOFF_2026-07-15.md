# Handoff 15.07.2026 — Kiosk-Fixes, Plan-Zoom, Hygiene, Perf-Baseline + große Queue

**Arbeitsklon:** `C:\repos\epkolar-app`. Push: `git push origin main`, Remote-Verify per curl raw/github.io.
**Stand HEAD (in-progress, wird beim letzten Push finalisiert):** **v3.9.709**.

---

## ✅ Heute gebaut, gepusht & (wo möglich) live-verifiziert

| Version / SHA | Inhalt |
|---|---|
| **v3.9.705** `8b52b4d` | Kiosk Self-Update: `sw.js` `skipWaiting()` im install (+ `clients.claim()`), Versions-Poller (index.html?_v=, RELATIV), lager_display stiller Auto-Reload, Loop-Guard (20 s Rapid + 10 min Version), Update-Banner sonst. |
| **v3.9.706** `dc8c6ec` | 🚛 Kiosk-Fahrzeuge kontrollierter Lesepfad: RPC `kiosk_fahrzeuge()` (v695-Muster) + Client-Fallback + Diagnose-Zeile „v · FZ:n · Spez:n". |
| **v3.9.707** `3dfe20f` | Kiosk täglicher Hard-Reset 03:00 Wien (Intl/Europe/Vienna, Marker-Guard, Erst-Lauf ohne Sofort-Reload, im v705-Poll-Takt). |
| **RPC-Fix** `9733feb` | `kiosk_fahrzeuge()` → `RETURNS TABLE` statt `SETOF jsonb`. **Der eigentliche 🚛-Fix.** |
| **v3.9.708** `36ed56c` | Plan-Viewer `_planZoomAt`-Refactor (Pinch-Zoom existierte schon vollständig; nur die 5×-duplizierte Fokuszoom-Mathe in eine pure getestete Fn gezogen). |
| docs `a0ff189` | Supabase-Hygiene-Inventur (READ-Blöcke, RLS-Snapshot, gestagetes CLEANUP — 0 scharfe DDL) + KV-Proposals (4 v664-Näherungen mit €-Delta). |
| docs `cd05b89` | Performance-Baseline 15.07. (Boot 245 ms · Daten 5,6 s · fahrzeuge-Fetch 2,37 s · Heap 89 MB · tank_log-Base64 15 MB). |

---

## 🚛 Root-Cause-Kapitel — warum das Display so lange leer blieb (ABGESCHLOSSEN)

Live-verifiziert 15.07. ~18:29–18:35 (Chat-Claude + Sebastian-Screenshot): Planung-Screen zeigt jetzt
**FZ:21 · Spez:1** und die Zeile **„🚛 TU-21CC — FC Moser Rohrendorf" (Fr, KW29)**. Fall zu.

Drei sich überlagernde Ursachen:
1. **`SETOF jsonb`** (erste RPC-Fassung): PostgREST verschachtelt ein SETOF-Scalar unter dem
   Funktionsnamen → Client sah `f.typ/f.modell` als undefined → Spez-Prädikat traf nie → 🚛 leer
   **trotz** geladener Fahrzeuge (FZ:>0 · Spez:0). Fix: `RETURNS TABLE` (flach), `9733feb`.
2. **Die Funktion existierte real NIE in der DB** (`pg_proc` 0 Zeilen vor dem Nachweis-Lauf) — auch der
   manuelle Mittags-Run hat nicht gegriffen. Erst der korrigierte Run ~18:29 zog.
3. **Panel hing auf altem Cache** — es hatte den RPC-Ladeweg (v706) lange gar nicht; erst der
   TV-Browser-**Hard-Reset** (Cache leeren) lud den frischen Code. Deshalb kam kein Push an, bis der
   Cache geleert war.

**Lehre (in CLAUDE.md als Hausregel ergänzen):** Nach JEDEM SQL-Run gehört ein `pg_proc`-/Ergebnis-
Selbst-Nachweis in dieselbe Editor-Sitzung. Der Selbst-Nachweis-Block der SQL-Datei ist ab jetzt
Pflichtteil des Run-Rituals. Der hasAny-Designfix (Spez-Zeilen immer zeigen) **entfällt** — Ursache war
RPC-Verschachtelung + fehlende Funktion + Cache, nicht das Anzeige-Verhalten.

---

## 🔧 Queue (ein index.html-Slot, seriell — Worktree-Isolation hier nicht verfügbar)

| Slot | Status |
|---|---|
| **Mobile-Pinch zentrieren** | ✅ **v3.9.709** (`8b99881`+Bump). Root Cause bestätigt+gemessen: Flex-Zentrierung verschiebt world-Layout-Ursprung um L≠0 → Fokus-Drift L·(r−1). Fix: `_worldOrigin()` (getBoundingClientRect, Flex-Offset+Scroll), `mid=finger−worldOrigin+pan`; gelockte Formeln byte-identisch, alle 4 Zoom-Pfade, +10 Tests, pytest 1562. |
| Team-Kachel → Planung | ⏳ queue |
| Material-Symbole (+Piktogramme +Messlauf gegen 25k Artikel) | ⏳ queue |
| Performance-Fix 1a/1c (tank_log-Base64 raus + Bootstrap-Diät) | ⏳ queue — größter Hebel, darf vorgezogen werden |
| **Dispo-Assistent** (Stufe 1 + Nachträge 1–5) | ⏳ großer Block, s. u. |

Hygiene-Agent ✅ fertig (docs/sql, gepusht `a0ff189`).

---

## 🗺️ Dispo-Assistent — große mehrteilige Spec (Nachträge 1–5, noch nicht begonnen)

Vorschlagsplanung offener Arbeitsscheine, deterministisch/erklärbar, **Zielfunktion: km + Fahrzeit
minimieren**. Bausteine für die Zerlegung in Teilagenten:
- **Geo:** `sql/PLZ_GEO_v1.sql` (PLZ→lat/lon) + `sql/PLZ_DISTANZ_v1.sql` (echte OSRM-Straßenmatrix,
  Einmallauf, ~8 distinct PLZ aktuell → Minuten). Firma-Anker 3470 Kirchberg, Marktplatz 17.
- **Adresse:** Quelle = `arbeitsort` (Freitext, PLZ per `/\b\d{4}\b/`), pure `_dispoAdrKey` fürs
  Haus-genaue Bündeln (22/36 offene in 3470). Haversine×1,3 nur noch Fallback, sichtbar „~".
- **fz_bedarf:** `sql/AS_FZ_BEDARF_v1.sql` (jsonb-Spalte, 42703-tolerant, v706-Muster). App-only Block
  am AS, Monteur darf setzen. **Nie** in JUPROWA_PUSH_FIELDS.
- **Algorithmus:** Fixtermine → Prio-Töpfe (Störung/SAT → hoch → Alter) → Greedy-Einfügung nach
  Mehr-km + **2-opt** je Monteur-Tag + Bündelung (gleiche Adresse/PLZ, Nachbarschaft <10 km,
  Tages-Regionen).
- **Übernahme (Nachtrag 5):** vollwertiger AS-Edit über bestehenden `updAs`/`saveAs`-Pfad → löst die
  vorhandene OFFA/Juprowa-Push-Mechanik aus. **Vor dem Bauen `JUPROWA_PUSH_FIELDS` wörtlich prüfen**
  (Termin + monteur enthalten?) — fehlt eines → STOPP + Sebastian-OK (Roundtrip-Bug-Warnung v583/615/616).
  `_juprowaPush` bleibt TABU-unangetastet. Übernahme trägt Spez-FZ zusätzlich in `z.<tag>.fz` (bestehender
  cellPick-Weg).
- **Ersparnis sichtbar:** „Woche X km · Y h · −N km gespart" vs. naiv (Einzelfahrten ab Firma).
- UI: Wochenraster Mo–Fr × Monteure (Vorschlags-Chips), Mobile Tages-Karten. Grenzen ehrlich als Fußnote.

---

## ⏳ Offene Sebastian-Gates (Human-Run / Entscheid)

1. **KV-Proposals** (`docs/KV_PROPOSALS_2026-07.md`): 4 Näherungen einzeln entscheiden (Montagezulage alle
   Std +46 €/Mt/MA · 100 %-Fenster vs Tages-Flag · 3.-Üe-nach-19h-Band · taggeldNacht 62,04 € verdrahten).
2. **Hygiene-CLEANUP** (`sql/CLEANUP_2026-07.sql`, v2): **verifiziert & ausführungsreif**, wartet auf
   Sebastians Run-Zuruf ("S1 machst du") — Chat-Claude führt sektionsweise aus (Backup → Verify → Drop →
   Gegenzählung, nach S1 Kiosk/Login-Check). **Offene S4-Entscheidungen** (je wörtliches „droppen"):
   (a) `weekplans` (11 Z., Backup vor Drop) · (b) `urlaubskontingent.urlaub` (0 Non-NULL, gefahrlos —
   guard_kontingent referenziert die Spalte NICHT, Regex-verifiziert) · (c) `_backup_arbeitsscheine_status_pre_a2_20260630`
   (108 Z., Backup vor Drop). **Rotations-Fragen:** (d) `notifications` (739 Z./2,5 MB, gelesen >90 T.?) ·
   (e) `activity_log` (8.446 Z./1,9 MB, >6 Mon.?).
3. **Dispo-SQLs** (plz_geo, plz_distanz, as_fz_bedarf): nur gestaged, Run durch Sebastian/Chat-Claude mit Nachweis.
4. **Perf-Migration** `_migrateTankFotos` `{go:true}` nur nach explizitem OK / Admin-Button.
5. Weiter gesperrt: Montagezulage-Phase-2, B034-SQL, KV-Punkte 2/3/4, Dispo 6→7 Tage, urlaubskontingent.urlaub-Drop,
   sync_supplier-Deploy, guard_urlaub_edit v3, PAT workflow-scope.

## Wiederaufnahme-Punkte (exakt)

- **Perf-Fix 1b (Migration):** `window._migrateTankFotos` (admin-gated, idempotent) räumt die
  bestehenden Base64-Tank-Belege nach Storage. Noch KEIN Admin-Button gebaut → nächster Schritt:
  Button (isWAdm) in der Tank-Kontrolle, der `_migrateTankFotos({go:true})` auslöst (Dry-Run zuerst).
  {go:true} nur durch Sebastian.
- **Perf-Fix 1c (Boot-Diät) — bewusst geskippt:** `getFahrzeuge` (`_sbGetOrder("fahrzeuge",…)`, ~Z.2805)
  ohne `tank_log`-Spalte zu booten scheitert an mehreren **Cross-Fahrzeug-Aggregations-Lesern** auf
  `fahrzeuge[].tankLog` aus dem Boot-State: Büro-Tank-Kontrolle ~11058, Dashboard-Tankkosten
  ~12666/12805/12820, Auswertung ~21381/21899. Die lassen sich nicht auf on-demand-Nachladen je
  Fahrzeug umstellen, ohne die Kosten-/Kontroll-Ansichten leerzuziehen. Nächster Schritt: eine
  schlanke Aggregat-RPC (nur tank-Summen je FZ) ODER ein separater `fahrzeuge_tanklog`-Fetch on-idle,
  statt tank_log im Boot. **Erst nach 1b** (dann ist der Store ohnehin klein) neu bewerten.
- **⚠ Storage-Abhängigkeit von 1a:** Tank-Fotos gehen jetzt über `_sbUploadFile` (wie der Live-AS-Foto-
  Editor). Falls der ES256-JWT-Storage-Block (`project_epkolar_storage_jwt_rootcause`) noch aktiv ist,
  scheitern Uploads → Tankung wird ohne Foto gespeichert (mit Warn-Toast, kein Crash/Datenverlust).
  Post-Deploy prüfen: geht ein Tank-Foto-Upload durch? Wenn AS-Fotos hochladen, geht auch Tank.

## Dauer-Tabus
Auth-Pfade/`auth.users`, OFFA-Writes, `_juprowaPush`/`_juprowaSanitize`, Lager-Passwort, RLS/Policies real
ändern, `.github/workflows/*`, DDL-Ausführung durch CC (SQL nur stagen).
