# EPKolar — Übergabe 2026-06-12 · v3.9.306

**main HEAD = origin/main (in sync, 0/0).** Triade grün: `node_check.py` exit 0, Versionen synchron 3.9.306, pytest 722 passed.

## Heute (v3.9.306)
### 1. Editierbarer Tank-Kontroll-Dialog (`addTank`)
Bisher: `_confirmModal` zeigte einen statischen Text — Tippfehler erforderte Modal-Cancel + neues Form-Edit + erneuten Save-Klick.
Jetzt: `window._tankConfirmModal({datum,liter,preis,km}, currentKm)` — vanilla-DOM analog `_confirmModal` (definiert direkt darunter, ~Z.3870). Vier editierbare Felder, Live-Literpreis, Inline-Fehlertext, Save erst bei gültigem Datensatz. Validierung im Modal:
- Datum Pflicht
- Liter > 0
- Preis Regex `/^[0-9]+([.,][0-9]{1,2})?$/` (leer = 0)
- km Regex `/^[0-9]+$/` (leer = kein km)
- km nicht unter `currentKm` (Odometer monoton).

`addTank` ist jetzt schlank: prefill aus Form-State → Modal → bei Confirm Diff-PUT (`tank_log`+`km_stand`, unverändert).

### 2. 0-rows-Safeguard für Fahrzeug-PUTs (`_translateAndExec` ~Z.2197)
RLS-Silent-Denial → HTTP 200 + leeres Array. Bisher droppt die Queue das Item still und User glaubt "Tankung erfasst". Jetzt: bei `table==="fahrzeuge" && _patchRes.length===0` Error-Toast 9 s
> „⚠️ Fahrzeug-Änderung NICHT gespeichert — keine Schreibberechtigung für dieses Fahrzeug…"

KEIN re-throw (0-rows ist permanent — gleiche RLS, Retry sinnlos → Queue bleibt sauber).

### 3. inputMode-Korrekturen für mobile Ziffern-Tastatur
4 km-Inputs (Tank-Modal, Quick-Action Tank, Quick-Action km, Batch-Tank) hatten `inputMode:"decimal"` — auf `"numeric"` + `pattern:"[0-9]*"` + `step:"1"`. Mobile-Tastatur zeigt jetzt reines Zahlenpad statt Dezimal-Tastatur.

## Kontext der Tank-Kette (Vorgeschichte)
- OCR Tankbelege via Google-Vision-Edge-Function `supabase/functions/ocr_tankbeleg/index.ts` (deployed v3 ACTIVE, `verify_jwt:true`, `GOOGLE_VISION_KEY`). `parseTankBeleg` (~Z.18000) ruft `/functions/v1/ocr_tankbeleg` (15s-Timeout), offline = manuelle Eingabe.
- **km-Parser Riedmann-Belegtyp offen**: `rawText` von Sebastian noch nicht da, Edge-Function-Justierung pausiert.
- Stiller Tank-Verlust GESCHLOSSEN: RLS-Policy `fahrzeuge_update_driver` in Prod (additiv) + jetzt zusätzlich Client-Toast bei 0-rows.

## Offene Folge-Punkte
- **v3.9.307 geplant**: ✏️-Edit in `ZeiterfassungView` (Monteur-Wochenuebersicht) — Pendant zu VZeit-Edit aus v3.9.301. Toter `editEntry/setEditEntry`-State wird wiederverwendet, 3-fach-Sync (DB-PUT + dayEntries + `setEntries`-Prop), `canDo('zeit_other')`-Guard.
- km-Parser Edge-Function: Beleg-`rawText` von Riedmann besorgen → km-Regex justieren.

## DB-Direktive (HART, gilt weiter)
`jiggujpruejkaomgxarp`. DB-Writes nur wenn Sebastian den konkreten Fall autorisiert, **höchste Vorsicht um Nutzdaten** (additiv bevorzugt, NIE blind UPDATE/DELETE/DROP), Worker-Guard (w1–w9 = 9) + BEGIN/COMMIT + read-only-Verify. `_juprowaPush` / Juprowa-Phase-1-Pull NIE anfassen.

## Validierungs-Triade (immer vor Commit)
```
python scripts/node_check.py index.html      # exit 0 = ok
python -m pytest tests/ -q                    # 722 passed (108 s)
node sql/_check_version.js                    # ✓ versions synced: 3.9.306
```
Version-Bump 4 Stellen: `SW_VER` (index.html ~Z.15), `APP_VERSION` (~Z.2360), sw.js Header-Kommentar (Z.1), sw.js `CACHE_NAME` (Z.2).
