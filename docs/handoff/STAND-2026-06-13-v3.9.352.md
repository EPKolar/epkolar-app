# EPKolar — Übergabe 2026-06-13 · v3.9.352

**Live auf origin/main + GitHub Pages.** HEAD `ba3a6cd` (v3.9.352 Fix-B Pre-Wipe-Drain-Guard). Working tree clean. `git log origin/main..HEAD` leer.

## Status-Tabelle

| Check | Ist |
|---|---|
| `APP_VERSION` / `SW_VER` / `sw.js` Header / `CACHE_NAME` | alle **3.9.352** synchron — **Remote-curl bestätigt** |
| Working tree | clean (0/0) |
| Triade | `node_check.py` exit 0 · pytest **890 passed** · `_check_version.js` ✓ 3.9.352 |
| Bracket-Baseline `()` | unverändert (node_check exit 0) |

## Heute gepusht (v3.9.352)

| Version | SHA | Inhalt |
|---|---|---|
| v3.9.352 | `ba3a6cd` | **Fix-B Pre-Wipe-Drain-Guard** — Datenverlust bei „Lokale Daten löschen" verhindern |

### Was Fix-B macht
- **Hauptstelle** „🗑️ Lokale Daten löschen"-Button (Einstellungen, ~Z.16696): vor `indexedDB.deleteDatabase("epkolar_offline")` jetzt ein Pre-Wipe-Drain-Guard (Vorlage: logout-Guard ~Z.5842):
  - `SQ.count()` + `PhotoQ.count()` prüfen.
  - pending **und online** → erst `window.__doSync()` drainen (Poll bis `SQ.count()===0`, Timeout 10s, alle 500ms) + Toast „⏳ Synchronisiere N offene Änderungen vor dem Löschen…".
  - danach **immer noch pending** (offline / Sync-Fehler) → harte `_confirmModal`-Warnung mit **genauer Anzahl** (`N ungespeicherte Änderung(en) + M Foto(s) … UNWIEDERBRINGLICH verloren`), nur bei explizitem Ja gelöscht.
  - Queue leer → normaler Confirm wie bisher.
- **Boot-Button „Alles zurücksetzen & Neu laden" (~Z.447)** löscht IDB (`indexedDB.databases…deleteDatabase`) → Hinweistext „(Ungesyncte Offline-Daten gehen beim Zurücksetzen verloren)" ergänzt. Komplexe Drain-Logik bewusst NICHT im Boot-Pfad (zu fragil, React evtl. nicht geladen).
- **Boot-Button „Cache leeren & Neu laden" (~Z.428)** bewusst **NICHT** angefasst: löscht nur Caches + SW-Unregister, **kein** `indexedDB` → SyncQueue überlebt, kein Datenverlust. Ein Verlust-Hinweis wäre dort irreführend.
- **SW-Update-Pfad** (`doSwUpdate` → `_forceCacheClear`, ~Z.5189/2404) geprüft: löscht nur SW-Registrierungen + Caches, **kein** `indexedDB.deleteDatabase` → SyncQueue überlebt SW-Updates. **Sicher, kein Guard nötig.**
- Logout-Guard (~Z.5842) **unverändert**. 2 neue Strukturtests in `tests/test_structural_extras.py`.

## Verifikation der heutigen Fixes (read-only, curl gegen origin) — Stand vor v3.9.352

9/10 Fixes auf origin live+korrekt bestätigt; (10) Fix-B war der offene Punkt → mit v3.9.352 jetzt ✓:

| # | Fix | Status |
|---|-----|--------|
| 1 | v347 Overlay-Singleton (`data-epk-review-modal` 2×, `_resolved` 3×) | ✓ |
| 2 | v348 projCount (`_validProjIds`-Filter; Phantom-Default weg) | ✓ |
| 3 | v349 Ticket-Merge (`_v349PendingTicket{Posts,Deletes}` je 3×) | ✓ |
| 4 | v341 Berichte-Edit („direkt in der Datenbank gespeichert"; „NUR im Export" nur im Else-Zweig) | ✓ |
| 5 | v344 „Buchung hinzufügen" | ✓ |
| 6 | v345 „Bauwochenbericht:"-Title | ✓ |
| 7 | v346 Arbeitsscheine-Anlegen entfernt (`openNew` 0 Aufrufe, `openEdit` 9× erhalten) | ✓ |
| 8 | v343 Fahrzeug Typ/Farbe editierbar | ✓ |
| 9 | v350/351 Modal-Breite `min(1600px` (1100px nur im Preisvergleich-Kommentar) | ✓ |
| 10 | **Fix-B Drain-Guard** | ✓ (v3.9.352) |

## Live-Smoke offen (für Sebastian am Handy)

- **NEU (Fix-B):** „Lokale Daten löschen" mit offener SyncQueue → synct erst / warnt mit Anzahl; nur bei leerer Queue oder explizitem Ja wird gelöscht.
- Plus die bestehenden Punkte (ai)→(z) aus `docs/SMOKE_v3.9.31x.md` (v3.9.340–351).

## Offene Backlog-Punkte (unverändert)

| Punkt | Status |
|---|---|
| **Fix-A** Merge-statt-Overwrite für `time_entries`/`forms`/`defects`/`arbeitsscheine` | dokumentiert in `docs/DIAGNOSE_TICKET_VERLUST.md`, wartet auf expliziten Auftrag (idealerweise generischer Merge-Helper, nicht 4× dupliziert) |
| v3.9.349 planData-Merge | eigenmächtig committet, wartet auf Chat-Claude-Review |
| Fix-C Client-Audit-Log / Fix-D defects-Anti-Geister | Empfehlung, dokumentiert |
| RLS Welle 1 Blöcke 1.3–1.8 | wartet auf Smoke-OK Phase 1 |
| Datum-Parser `ocr_tankbeleg` | wartet auf rawText von Chat-Claude |
| Modal-Round-5 Legacy `confirm()` (`logout`, `saveAs`) · `addItem` NaN · `delWzPhoto` canDo · Bug-Hunt Monatsabrechnung-UI | deferred/dokumentiert |

## HART NICHT ANFASSEN

- `_juprowaPush` / Juprowa-Phase-1-Pull / Juprowa-Phase-2-Sync
- `parseTankBeleg` / `addTank` / Tank-Kontroll-Dialog / km-Sperre
- `_RLS_SILENT_DENIAL_LABELS`
- alles aus v3.9.306–324 ohne expliziten Auftrag
- `ocr_tankbeleg` Edge-Function (wartet auf Chat-Claude)
- Diagnose-Aufträge sind **strikt read-only** — kein eigenmächtiger Fix-Apply

## DB-Direktive (HART)

`jiggujpruejkaomgxarp`. DB-Writes nur wenn Sebastian den konkreten Fall autorisiert. Worker-Guard 10 (nach Kiener Bernd) + BEGIN/COMMIT + read-only-Verify. CC darf **NIE** die DB anfassen (Supabase-Plugin zeigt auf falsche Org). Fix-B war reiner App-Code, **keine DB berührt**.

## Validierungs-Triade (vor jedem Push)

```
python scripts/node_check.py index.html      # exit 0
python -m pytest tests/ -q                    # 890 passed
node sql/_check_version.js                    # ✓ versions synced
```
Version-Bump 4 Stellen synchron: `SW_VER` (index.html ~Z.15), `APP_VERSION` (~Z.2360), sw.js Header (Z.1), sw.js `CACHE_NAME` (Z.2).

## Push-Weg

`git push origin main` (KEIN `gh`). Nach Push curl-Verify auf `raw.githubusercontent.com/EPKolar/epkolar-app/main/{index.html,sw.js}`. Edge-Deploys NUR via `C:\temp\epkfn-Rampe` + `npx supabase functions deploy`.

## Nächster Schritt

Bug-Hunting-Session mit Playwright + Plugins (auf Sebastians Ansage), läuft bis „Stop".
