# EPKolar — Stand 2026-06-15 (Übergabe für neue Session)

**Code: main `b481d98` = origin, working tree clean, v3.9.381 live + curl-verifiziert. 903 pytest grün, node_check 0.**
**DB (jiggujpruejkaomgxarp): RLS live-sauber — alle anon-Schreib- UND Lese-Löcher geschlossen (bis auf bewusst gelassene, s.u.).**

---

## 1. App-Änderungen heute (v3.9.361 → v3.9.381) — alle gepusht + Playwright-/curl-verifiziert

**Büro-Export / Bauwochenbericht (BWB):**
- v3.9.361/362: Inline-Vorschau = detaillierte **editierbare** KW-Blöcke (Single-Source `_renderBWBBlocks(...,projId)` mit dem Modal). projId durchgereicht, `editTaet` projekt-skopiert (`proj.id|kwKey`) → kein project_id:null-Waisen / kein Tätigkeits-Leak.
- v3.9.364: Monteur-Tätigkeit sofort sichtbar (Fallback aus time_entries/Bautagebuch) + **State-nach-Write-Fix** `_syncThenReload` (erst SQ-Queue drainen via __doSync, dann loadAll → kein „muss abgeglichen werden") + Excel-CI (EP-grüner Text-Header, kein base64-Logo).
- v3.9.365: KW-Tätigkeit-Feld schreibt onBlur via `_saveKwTaet` in ALLE time_entries der KW (Chef-Entscheidung, destruktiv-gewollt, `canDo zeit_other`).
- v3.9.368: **Lese-/Schreibpfad-Entkopplung gefixt** — alle 4 Lese-Stellen lasen bautagebuch-FIRST, schreiben ging nach time_entries → jetzt time_entries.taetigkeit als Single Source.
- v3.9.370 → **v3.9.380**: BWB-Spalten = **nur aktive Monteure (r==='Monteur'+austritt-leer) ∪ Bucher** (Chef-Präzisierung „role=monteur, kein Büro/Leitung"). Kiener (0h) erscheint, GF/Lagerleitung nur wenn sie buchen. Summen unverändert. Inline==Excel (gemeinsame `getProjectData.workers`).
- v3.9.379: Material-Verlauf für ALLE sichtbar (`myOrders=orders`), Buttons nur Verfasser/Lager/Admin (`_canEditOrder`/`_moOwner`).
- v3.9.378: Material-Anforderung schreibt `created_by_id=monteurId` (laufzeit-erkannt `_matHasCreatedById`, 400-sicher).

**Bug-Hunt-Welle v3.9.363** (6-Agenten-Audit): VCheck-del-Server-DELETE, gw-aus-gewerk-Mapper, Bautagebuch-Edit-Lockout, TicketDetail-Crash-Fallback + prio'normal'→'mittel', Überfällig-Off-by-one (`_dueIsPast`), Material-Dup-Kataloge, material_catalogs-RLS-Label.

**Arbeitsscheine-Audit v3.9.381** (read-only): Kalender + „Heute"-Filter Sentinel-Datum-Bug (`_hasTermin`-Guard → Scheine verschwanden), Suche Feld-Separator+Token-Splitting, `arbeitsscheine` in `_RLS_SILENT_DENIAL_LABELS`.

**UI/Layout:** v3.9.366/367 Auswertungen dichteres Chart-Raster (3-4/Reihe, Spalten-Max 480px, SvgBar-Höhen-Cap) · v3.9.371 Tablet-Nav eigenes 2-Zeilen-Wrap-Layout (`_isTabletNav` 600-1199px) · v3.9.372 Sidebar-Overlay-Labels · v3.9.374 Plan-Viewer `data-no-swipe` · v3.9.375 Projekt-Header-Worker-Select defaultet auf curUser · v3.9.376 Smartphone Tätigkeit-Umbruch · v3.9.377 Plan-Zoom rAF-coalesced.

## 2. DB-Migrationen heute (CC via Supabase-Plugin, Sebastian-Freigabe)
- **material_orders**: `created_by_id text` + Backfill (5/5) + owner-RLS (UPDATE/DELETE = `is_staff() OR created_by_id=current_monteur_id()`).
- **RLS-Härtung (2 Wellen, self-guarded, anon raus, authenticated unverändert):**
  - Welle 1 (ALL-true): `worker_projects`, `as_vorlagen`, `fahrzeug_buchungen`, `fz_termine`, `wz_service` → `ALL authenticated`.
  - Welle 2 (anon-Löcher): `projects`/`supplier_articles`/`supplier_configs`/`supplier_orders` (write+read true→authenticated, gated bleiben), `absence_files`/`bescheinigungen`/`exports` (INSERT→authenticated), + SELECT-true→authenticated auf `users`/`fahrzeuge`/`fz_schaeden`/`bautagebuch`/`material_orders`/`material_items`/`material_catalogs`/`project_documents`/`anmeldungen`/`fahrbewilligungen`/`urlaubskontingent`/`worker_kompetenzen`.
- **Ergebnis:** keine anon-erreichbare `true`-Policy mehr außer den bewusst gelassenen.

## 3. OFFEN / Sebastian-Aktionen
- **🚫 VOffa/ZUZEIT.ASC EINGEFROREN** — die 5 toten Buttons NICHT verdrahten/entfernen (OFFA-Lohnübergabe, Format unverifiziert; nur mit echtem OFFA-Spec+Test als separate Aufgabe).
- **Auth leaked-password-protection**: im Supabase-Dashboard (Auth Settings) aktivieren (kein SQL).
- **Bewusst gelassene RLS** (Einzelentscheidung): `system_config` SELECT-true (prüfen ob pre-login gelesen, dann härten), `juprowa_log`+juprowa_* (Phase-2 TABU), `notifications` notif_insert (Cross-User gewollt). Optionale Folge-Härtung mit Einzeltest: projects-write auf admin/PL, supplier_configs-read auf admin.
- **2 Backup-Tabellen** (`_rls_snapshot_v3923`, `_wp_orphan_backup_v3946`): nur auf explizites OK droppen (destruktiv).
- **Plan-Viewer „mehr PlanRadar"** (Feature-Wunsch, Richtung offen): z.B. Mini-Map, Mess-Werkzeug, „nur meine Tickets"-Filter.

## 4. Regeln / Arbeitsweise
- **DB (NEU 15.06.):** CC darf EP-Kolar-Prod-DB (`jiggujpruejkaomgxarp`) schreiben WENN Sebastian explizit anweist; Default sonst Chat-Claude. Pflicht: project_id-Check + self-guarding (idempotent, Self-Test+Rollback) + Vorher/Nachher-Log. TABU: Juprowa-Phase-2 + DROP/DELETE-ohne-Filter. Plugin sieht NUR `jiggujpruejkaomgxarp` (EP Kolar) — NICHT MOBOLog.
- **Triade vor jedem Push:** `python scripts/node_check.py index.html` (exit 0) · `python -m pytest tests/ -q` (903) · `node sql/_check_version.js`. Version bumpen: `index.html` SW_VER + APP_VERSION + `sw.js` ×2. Vor Push `git fetch` + ahead/behind (Parallel-Chat möglich).
- Backups: `_backups/epkolar_20260615_*.zip`. Browser-Bridge (MCP-Chrome) hängt ~alle 4 Min → verwaiste `mcp-chrome-*`-PIDs killen, neu navigieren.

**First-read nächste Session: diese Datei.** App live: https://epkolar.github.io/epkolar-app/ · Admin-Login: `admin` / `34kolar70`.
