# 🟢 START HIER — Stand 2026-06-09, v3.9.228

**Repo:** `epkolar-app` · github.com/EPKolar/epkolar-app · live https://epkolar.github.io/epkolar-app/ · Supabase `jiggujpruejkaomgxarp`
**main HEAD = `baa83e7` = v3.9.228**, lokal == origin, Working Tree sauber, **726 pytest grün**, alles gepusht.
Hard-Rules H0–H16, ehrlich, konservativ, autonomes Pushen auf main erlaubt. `_juprowaPush` NICHT anfassen.

## 🔴 PRODUKTIVBETRIEB MIT ECHTEN DATEN
Backoffice + Lager schreiben live. Keine destruktiven Ops, DB-Write nur additiv/explizit, jeder Push = SW-Bump = Client-Reload (bei aktivem Betrieb timen). Triade pro Change: `python scripts/_bracket_check.py index.html` (Baseline `() -1 / {} 0 / [] 0`) → `python scripts/node_check.py index.html` (exit 0) → `python -m pytest tests/ -q > _po.txt 2>&1; echo EXIT=$? >> _po.txt; tail _po.txt` (NIE `pytest|tail`). Version-Bump 3 Stellen: `SW_VER` + `APP_VERSION` (index.html) + `CACHE_NAME`/Header (sw.js).

## 🟢 NEU diese Session — Tooling
- **CC hat funktionierenden Supabase-MCP-DB-Zugriff** (`mcp__plugin_supabase_supabase__execute_sql` / `get_logs` gegen `jiggujpruejkaomgxarp`, ORG-GUARD: project_id hart). Read + write möglich; für read-only-Verify RLS-Kontext per `BEGIN; SET LOCAL ROLE authenticated; SELECT set_config('request.jwt.claims',…,true); … ROLLBACK;` simulierbar (write-frei).
- **jwt_exp = 28800 (8h)** gesetzt (war 3600) via Management-API + Sebastian-PAT. Refresh-Frequenz dadurch stark runter.
- **Live-Boot-Smoke** via Playwright möglich (anon, read-only): App bootet v3.9.228, 0 Console-Errors.

## Was diese Session live ging (v3.9.213 → 228)
**Auth-Resilienz (213–218, 226):** login_lookup-Hash-Leak raus (GoTrue-only + authentifizierter Profil-Self-Read) · 403≠Session-Ablauf · kein Logout solange Access-Token gültig · Cross-Tab-Token-Übernahme · Refresh-statt-anon-Key · Reload-Loop-Schutz · **v3.9.226: Anon-Boot-Reload-Loop gefixt** (F2-Regress, live gefunden).
**6 Bug-Hunt-Wellen (219–225):** Datenverlust-Schwerpunkt — Checkliste-items, RMW-transient-Drop, Save-Clobber paralleler Geräte, Katalog-Verlust, Mängel-Poll-Korruption (60s), Foto-Quota-Verlust, Cross-User-Leak, notif-throw, Glocken-Klick, Doppel-Bestellung, Pickerl/Vignette-Datum, 2× A11y, u.v.m. (siehe `BUGHUNT-MARATHON-2026-06-09.md`).
**notifications-403-Fix (227/228):** alle 4 Insert-Pfade (`_sbPost`/`_sbUpsert`/`_api.post`) senden für notifications `return=minimal` → kein RETURNING→SELECT→403 mehr für Monteur an Büro/PL. **DB-bewiesen** (Alt-Pfad=42501, Fix=ok).

## 🔴 OFFEN — Sebastian / DB / Entscheidung (kein Frontend)
1. **5 DB-Security-Migrationen anwenden** — `sql/fix_*_v3.9.213.sql` (+ROLLBACK), **nicht angewendet**. Reihenfolge + P0-Gate (`users.auth_user_id` für alle 10) + Capture für auth_role/login_lookup → `docs/handoff/HANDOFF-2026-06-09-DB-SECURITY-APPLY.md`. *(CC kann jetzt via MCP execute_sql selbst applizieren — aber nur mit expliziter Einzel-Freigabe pro Migration, da Live-Prod.)*
2. **2 hängende Sync-Items** (notifications-batch an u5–u8, unter altem Code entstanden) liegen in barger's **Geräte-IndexedDB** — nur auf seinem Tablet verifizier-/flushbar (Strg+F5 auf v3.9.228 → `window.__doSync()` → Network: POST notifications 201/204). Aus CC-Umgebung nicht erreichbar (andere IndexedDB, kein Login).
3. **nextNr Scheinnummer** → DB-Sequenz/UNIQUE (Duplikat-Risiko Shared-Tablets). **bautagebuch.anwesende** jsonb-String-Skalar → einmalige Normalisierung.

## 🔴 OFFEN — Review-Backlog (Frontend, braucht Chef-Entscheidung; NICHT blind gefixt)
Vollständig in `docs/handoff/BUGHUNT-MARATHON-2026-06-09.md` (14 Items). Highlights: Urlaub→Kontingent-Brücke (P1), Cross-Year-Wochenplan (F3), FZ/WZ-Voll-PUT-Clobber (P1, Architektur-Defer), JUPROWA-Status-Drift (P1), VMang→Plan-Ticket-Sync, tickets.type/due_date (DB-Spalten-Check zuerst), Bestell-Lücke, Kamera-Leak, PlanRadar-Zoom-Drift.

## ⚠️ Koordination
Ein **paralleler Chat** arbeitet evtl. auch am Repo. Vor jedem Push `git fetch` + ahead/behind prüfen; klären, wer `index.html` „besitzt" (Single-File → Konflikte/Clobber-Gefahr).

## Frische-Session-Start
`git -C "<repo>" log --oneline -8` + diese Datei + `BUGHUNT-MARATHON-2026-06-09.md` + `HANDOFF-2026-06-09-DB-SECURITY-APPLY.md`. Dann mit Chef priorisieren: Migrationen anwenden | Review-Backlog-Item | weiter Bug-Hunt.
