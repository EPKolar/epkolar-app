# EPKolar — Übergabe 2026-06-13 · v3.9.354

**Live auf origin/main + GitHub Pages, curl-verifiziert.** Working tree clean. Bug-Hunt-Befunde aus `BUGHUNT-2026-06-13-PLAYWRIGHT.md` abgearbeitet, soweit als CC-Code-Task sinnvoll & sicher.

## Heute gepusht

| Version | SHA | Inhalt |
|---|---|---|
| docs | `12473d2` | Bug-Hunt-Doc + #11 absences-RLS RESOLVED (DB) dokumentiert |
| v3.9.352 | `ba3a6cd` | Fix-B Pre-Wipe-Drain-Guard (Lokale-Daten-löschen) |
| v3.9.353 | `867fead` | #5 Sentinel-Datum 0001-01-01, #8 leere Projektnr-„()", #9 PlanViewer-Console-Rauschen |
| v3.9.354 | `1b2c895` | #6 Wochenplanung read-only (Monteur), #3 Glocke Dismissed-Gedächtnis, #7 Urlaub-Rundung |

## Befund-Status (12 aus dem Playwright-Hunt)

| # | Befund | Status |
|---|--------|--------|
| 1 | `activity_log` 403 RLS (Monteur-Login, Audit-Verlust) | **DB — offen** (Chat-Claude: Insert-Policy für Monteure) |
| 2 | Dashboard Cold-Cache-Stale (Projekte 5/8 + fremde Liste, kurz) | **deferred** — transient/self-healing (~1s); Fix berührt Kern-Projekt-Scoping (mehrere Komponenten) → zu riskant ohne Live-Test. Sicherer Fix-Vorschlag: Loading-Gate bis `worker_projects` geladen, dann KPIs/Baustellen rendern. |
| 3 | Glocke regeneriert gelöschte Auto-Notifs | ✅ **v3.9.354** (persistentes `epk_notif_dismissed`, cap 500, logout-fest; delete + clear-all schreiben rein, pushNotif filtert) |
| 4 | Werkzeug-Wert-Spalte „0" | **kein Bug** — 0-Wert-Geräte sind echt; `totalWert` summiert korrekt (€16.490) |
| 5 | Arbeitsschein-Datum `0001-01-01` sichtbar | ✅ **v3.9.353** (`_hasTermin`-Guard, Display-only) |
| 6 | Wochenplanung Phantom-Save (Monteur) | ✅ **v3.9.354** (alle Edit-Handler isAdmin-gegated + Read-only-Hinweis statt „automatisch gespeichert") |
| 7 | Urlaub 193h vs 192,5h Rundung | ✅ **v3.9.354** (1 Dezimale, konsistent) |
| 8 | Projekt-Header leere „()" | ✅ **v3.9.353** (Detail-Header + Export-Label). Rest-Cosmetic: hängender „·"-Untertitel bei leerem Ort — offen, trivial. |
| 9 | PlanViewer „PDF not loaded" Console-Warn | ✅ **v3.9.353** (Warn nur bei echtem Fehler) |
| 10 | Wochenspann Mo–So (Bericht) vs Mo–Sa (Planung) | **deferred** — Sonntag-Spalte entfernen = Datenrisiko (Sonntags-Überstunden); Produktentscheidung „ist Sonntag Arbeitstag?" nötig |
| 11 | `absences` RLS-Leak (Gesundheitsdaten, DSGVO Art.9) | ✅ **RESOLVED (DB)** — zeilenweise RLS, Chat-Claude/Human-Run-Gate |
| 12 | `time_entries` RLS-Leak (Arbeitszeiten aller MA) | **DB — nächst-dringend** (Chat-Claude) |

## Offen
- **DB (Chat-Claude, nicht CC):** #1 `activity_log`-Insert-RLS, #12 `time_entries`-RLS, weitere ungefiltert geladene Tabellen (projects/fahrzeuge/werkzeuge/defects/forms/plans/tickets/checklists/weekplans/worker_projects) — Geschäftsdaten-Leak (RLS-Welle 1).
- **CC-Code (klein, deferred):** #2 Dashboard-Loading-Gate (braucht sorgfältigen Live-Test), #10 Wochenspann (Produktentscheidung), Rest-„·"-Untertitel.
- **Noch nicht gehuntet:** Arbeitsschein Such-/Filter + Detail, Projekt-Sub-Tabs Mängel/Bautagebuch.

## Validierungs-Triade (vor jedem Push)
```
python scripts/node_check.py index.html   # exit 0
python -m pytest tests/ -q                 # 890 passed
node sql/_check_version.js                  # ✓ synced
```
Nach Push curl-Verify auf raw.githubusercontent.com/EPKolar/epkolar-app/main/{index.html,sw.js}.

## Schutz/DB-Direktive
`jiggujpruejkaomgxarp` — CC fasst DB NIE an. Tank-Flow/`parseTankBeleg`/`addTank`/km-Sperre, Juprowa-Phase-1/2, `_RLS_SILENT_DENIAL_LABELS`, v3.9.306–324 unberührt. Push `git push origin main` (kein `gh`).
