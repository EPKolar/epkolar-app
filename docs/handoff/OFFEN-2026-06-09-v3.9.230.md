# OFFENE ARBEIT — ab v3.9.230 (`a0eef26`)

main HEAD `a0eef26` = **v3.9.230**, 726 pytest grün, alles gepusht. Triade: `python scripts/_bracket_check.py index.html` (Baseline **`() -1`** / {} 0 / [] 0 — NICHT -2!), `python scripts/node_check.py index.html` (0), `python -m pytest tests/ -q` (726). Version-Bump 4 Stellen (SW_VER + APP_VERSION + sw.js Header + CACHE_NAME). Remote MUSS `EPKolar/epkolar-app` sein.
**Zeilennummern driften nach jedem Edit → jeden old-String per Grep frisch verifizieren.** Diffs sind aus Agenten-Synthese (Wave-7), gegen v3.9.229 validiert.

## ✅ ERLEDIGT diese Session (Auswahl)
Auth-Resilienz (213–218/226) · 6 Bug-Hunt-Wellen (219–225) · notifications-403-Fix `return=minimal` (227/228, **DB-bewiesen** via MCP) · tickets.type/due_date persist (229) · **#1 Urlaub→Kontingent-Brücke (230)**. jwt_exp=8h gesetzt. CC hat funktionierenden Supabase-MCP-DB-Lesezugriff (`execute_sql`/`get_logs`, `jiggujpruejkaomgxarp`); **Prod-WRITES blockt der Klassifizierer → nur read-only autonom, DDL/DML via Editor**.

## 🔜 BRIEF-SCOPE — noch offen (Reihenfolge b/c, dann Bug-Hunt, dann Bescheinigung)
**#2 VMang→Plan-Ticket-Sync (P2, VALIDIERT/AUTO-SAFE):** VMang `@10383` Signatur um `planData,setPlanData` erweitern; Render `@9554` durchreichen (ProjectShell `@9466` hat sie, an VDash/VPlan schon übergeben). Helfer `_syncTicketStatus(id,st)` + Inverse-Map `_DEF2TICKET_ST={offen:"offen","in Behebung":"in_bearbeitung","behoben":"erledigt"}` vor `updSt @10466`. Einhängen: `updSt @10472`, `delM @10473` (zusätzl. DELETE /api/tickets/<id> + planData-Filter), `reviewDone @10498-10501`, `reviewProgress @10493-10496`. Guards `planData&&planData.tickets` + `_tks.some(t=>t.id===id)` (freie Mängel → kein Geister-PUT). reviewAccept/Reject NICHT syncen.

**#3 FZ/WZ inkrementell (P1):** DIFF 1-5,7 = AUTO-SAFE — `upd @17632` optionaler 3. Param `syncBody`; `addKm @17648` body `{kmStand:km}`; `addTank @17699` `{tankLog_add:_e,kmStand:_km>0?_km:undefined}`; `addSchaden @17710` `setFahrzeuge` direkt (Schaden via /api/schaeden); Feld-Edits `@18287/18289/18291/18292` Diff-PUT (nur geändertes Skalar); WZ-`save @18778` serviceheft+fotos aus PUT ausschließen (delete-Variante). Server-RMW `@2046-2086` merged kmStand/tankLog_add. **DIFF 6 (serviceheft) ÜBERSPRINGEN = REVIEW:** 9 statt 6 `upd(...serviceheft...)`-Calls (`@18389/18396/18397/18398/18399/18400/18401/18403/18404/18411`, @18403/18404 = Befund/Rechnung base64 verschachtelt) + Stale-State-Risiko im `updSvc`-Helper → braucht `upd`-Signatur-Umbau (syncBody aus Updater-Ergebnis). **Hängt mit Interactive-Task „tu-563 Rechnung/Befund" zusammen.**

**Bug-Hunt confirmed (Synthese):**
- **P1 AUTO-SAFE `@10479` reviewAccept:** `status:"angenommen"` → `"offen"` — sonst fällt angenommener Kundenmangel aus MANGEL_ST (`["offen","in Behebung","behoben"]`) → verschwindet nach 60s-Poll vom Board. (`kunde_status:"angenommen"` bleibt.)
- **P2 AUTO-SAFE `@8601` diffWarn:** user→monteurId auflösen: `const _u=(users||[]).find(x=>x.id===d.worker_id);const _mid=_u?(_u.monteurId||_u.monteur_id||""):"";` Return `(wid===d.worker_id||(_mid&&wid===_mid))&&dt.startsWith(prefix)`.
- **P2 AUTO-SAFE `@14603/14604/14608/14621/14622/14919`:** `approved_at:new Date().toISOString()` in Abwesenheits-Genehmigungs-Bodies (Audit-Lücke).
- **P3 AUTO-SAFE `@5396`:** `d.status==="in_bearbeitung"` → `(d.status==="in_bearbeitung"||d.status==="in Behebung")`.
- **P3 AUTO-SAFE `@18871`:** `sub: stCounts.reparatur||0+" in Reparatur"` → `(stCounts.reparatur||0)+" in Reparatur"`.
- REVIEW: WZ-Bulk auf SQ.push `@19041/19063/19081`; lookupQR Exakt-Match `@18800` (leere inventarnr → `includes("")` matcht alles); PW-Reset Guard-Reihenfolge `@7932`.

**Bescheinigung lenkfreie Tage `genBescheinigung @3388`:** Layout näher ans amtliche EU-561/2006-Formular (Box-/Rahmen-Struktur). Felder 1-22 sind bereits da.

## 🖐 INTERAKTIVE UI-WÜNSCHE (out-of-Brief-Scope, vom Chef live)
- **Zulassungsschein klappbar** (collapsible, nicht immer sichtbar) — Fahrzeug-Detail.
- **tu-563 Serviceheft Rechnung/Befund** sieht schlecht aus + soll **mehr Dokumente** unterstützen (base64-in-DB beibehalten, nichts verlieren) — gehört zum #3-DIFF6-Bereich (`@18403/18404`).

## DB (Editor/Chat-Claude)
**login_lookup-Migration** (Hash-Leak) noch NICHT angewendet — fertige Self-Verify-Transaktion liegt vor (`docs/handoff/HANDOFF-2026-06-09-DB-SECURITY-APPLY.md` bzw. Chat-Verlauf). #1/#3/#4/#5 sind erledigt/No-op (MCP-verifiziert). nextNr-Sequenz + bautagebuch.anwesende-Normalisierung = DB-Action.

First-read neue Session: diese Datei + `STAND-2026-06-09-v3.9.228.md` + `BUGHUNT-MARATHON-2026-06-09.md`.
