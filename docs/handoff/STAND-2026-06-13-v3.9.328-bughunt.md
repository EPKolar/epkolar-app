# EPKolar — Übergabe Nacht-Session 2026-06-12 → 13 · Bug-Hunt v3.9.328

**main HEAD = origin/main (`364d67c`).** Autonomer Bug-Hunt über 6 Alt-Bereiche — 4 echte Bugs gefixt, 4 📝-Zweifelsfälle dokumentiert, 9 neue Regression-Tests. RLS-Welle-1 + Tank-Flow + v3.9.306-324 unangetastet.

## Status-Tabelle

| Check | Ist |
|---|---|
| `APP_VERSION` / `SW_VER` / sw.js Header / `CACHE_NAME` | alle 3.9.328 synchron |
| Working tree | clean |
| `git log origin/main..HEAD` | 0 ahead |
| Triade | `node_check.py` exit 0 · `pytest` **737 passed (+9 neu)** · `_check_version.js` exit 0 |
| Bracket-Baseline `()` | delta -2 |

## Bug-Tabelle

| Version | Commit | Bereich | Bug |
|---|---|---|---|
| v3.9.325 | 17b51dc | Wochenplanung | `clearRow` + `delRow` ohne `_confirmModal` → Modal-Guard ergänzt |
| v3.9.326 | bd8918f | Urlaubsplanung | `approve` + `reject` ohne Handler-`isAdmin`-Guard + ohne Modal → beides ergänzt |
| v3.9.327 | 3c4d9ee | Benachrichtigungen | `deleteNotif` + Inline-„Alle-löschen" ohne Modal → ergänzt |
| v3.9.328 | d1601cb | Material/Bestellwesen | `deleteSuppOrd` Render-canDo fehlte; `deleteCatalog` Handler+Render-canDo fehlten → alle 3 ergänzt |
| – | 364d67c | Regression | 9 Asserts in `tests/test_bughunt_alt_2026_06_12.py` |

## Bereich-Abdeckung

| Bereich | Bugs gefixt | 📝 dokumentiert | ✅ clean-Checks |
|---|---|---|---|
| Urlaubsplanung | 2 (approve, reject) | – | duplizierte Handler, NaN, 38.5h, Feld-Prios, kunde_freigabe |
| Wochenplanung | 2 (clearRow, delRow) | – | restliche Checks |
| Material/Bestellwesen | 3 (deleteSuppOrd-Render, deleteCatalog-Handler+Render) | addItem NaN-Maskierung | restliche |
| Werkzeuge | 0 | delWzPhoto isVAdmin-Inkonsistenz | del, checkout, checkin, NaN, exportWz |
| Benachrichtigungen | 2 (deleteNotif, Alle-löschen) | – | duplizierte Handler, canDo notif-Versand |
| Bautagebuch | 0 | – | delEntry-Modal, multi-layer-canDo, NaN, Feld-Prios |
| Monatsabrechnung-UI | – | – | 🔄 Agent läuft |

## Legacy-Befunde (📝 — nicht jetzt fixen)

- **`addItem`** (Material, Z.13421): `parseFloat(String(r.menge).replace(",","."))||1` maskiert leere Eingabe → falsche Mengen ohne Validierungs-Toast.
- **`delWzPhoto`** (Werkzeuge, Z.19225/19813): Guard ist `isVAdmin` statt `canDo("wz_edit")` — inkonsistent zu checkout/checkin, funktional korrekt.
- **`logout`-Handler** (Z.5555): 2× native `confirm()` für „N ungespeicherte Änderungen werden VERWORFEN" + „Timer läuft noch". Asynch-Logout-Sequenz muss umgebaut werden.
- **`saveAs`-Handler ArbeitsscheinView** (Z.6462): 1× native `confirm()` für AS-Duplikat-Warnung. Einfache Konvertierung, bei nächster Modal-Round-5 mitnehmen.

## 9 Regression-Tests

`tests/test_bughunt_alt_2026_06_12.py`:
1-2. Wochenplanung: clearRow + delRow Modal-Guard
3-4. Urlaubsplanung: approve + reject Modal + isAdmin-Guard
5-6. Notifs: deleteNotif + Alle-löschen Modal
7. Material: deleteSuppOrd Render-canDo
8-9. Material: deleteCatalog Handler+Render-canDo

Alle 9 grün. Verhindern dass die Bug-Hunt-Fixes durch Refactors wieder zurück-mutieren (wie das bei `clearRow`/`delRow`/`approve`/`reject` zwischen v3.9.11 Modal-Migration und jetzt passiert ist).

## NICHT angefasst

- Tank-Flow / `parseTankBeleg` / `addTank` / Kontroll-Dialog / km-Sperre / 0-rows-Safeguard
- `_juprowaPush` / Juprowa-Phase-1-Pull
- RLS-Client-Labels `_RLS_SILENT_DENIAL_LABELS`
- alles aus v3.9.306-324 (frisch geprüft in voriger Session)

## RLS-Welle-1 (von gestern abend — unverändert)

| Block | Status |
|---|---|
| 0.5 is_hr() Bootstrap | ✅ APPLIED |
| 1.1 fahrzeuge | ✅ APPLIED |
| 1.2 time_entries | ✅ APPLIED (additiv) |
| 1.3-1.8 | ⏳ OFFEN — wartet auf Smoke-OK |

## Validierungs-Triade
```
python scripts/node_check.py index.html      # exit 0
python -m pytest tests/ -q                    # 737 passed
node sql/_check_version.js                    # ✓ versions synced 3.9.328
```
