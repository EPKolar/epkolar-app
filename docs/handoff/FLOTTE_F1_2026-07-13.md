# Flotte / GPS — Handoff F1 (13.07.2026)

Stand am Ende der Session: **v3.9.679** (F1c) im Arbeitsbaum, Commit-Tip `8907af5` = v3.9.678.
Vorbild für das gesamte Feature ist das Fink-Portal (**FinkZeit FZE**), das Sebastian heute im Einsatz hat.

Arbeitsklon: `C:\repos\epkolar-app`. Der srvdc02-Share ist **nur Spiegel** (`git pull`), **nie** Push-Quelle.

---

## 0. Wo der Code liegt (Stand 13.07., `index.html`)

Alle drei Blöcke sind mit Sentinel-Kommentaren eingefasst — Tests hängen daran, Zeilennummern verschieben sich.

| Block | Zeilen | Inhalt |
|---|---|---|
| `//@FLOTTE-HELPERS-START` … `-END` | **21599–21622** | `_flotteLatestPerVehicle`, `_flotteRelTime`, `_flotteInactive` (Bestand ab v3.9.640/645) |
| `//@FLOTTE-STATUS-START` … `-END` | **21624–21706** | neu v3.9.678: `_fzFaehrt`, `_fzStatusSeit`, `_fzStatus`, `_fzDauerFmt`, `_fzSeitFmt`, `FZ_STATUS_LABEL`, `FZ_STATUS_COLOR` |
| `//@FLOTTE-SEGMENTE-START` … `-END` | **21708–21814** | neu v3.9.679: `_fzHaversine`, `_fzOdo`, `_fzNormPos`, `_fzSegmente` |

Weitere Ankerpunkte:

| Symbol | Zeile | Anmerkung |
|---|---|---|
| `_flotteFetchPositions()` | **21821** | liest die View `fz_latest` (DISTINCT ON je `fahrzeug_id`), fängt 404/`42P01` als „View fehlt" ab |
| `_flotteFetchTrack(fid)` | **21839** | Historie EINES Fahrzeugs on-demand, `select=lat,lon,ts,speed,ignition`, `order=ts.desc&limit=500` |
| `function FlotteView(props)` | **21852–21997** | Leaflet-Karte, Marker-Effekt, Fleet-Liste, Trail, Live-Follow |
| Marker-/Popup-Effekt | **21900–21936** | Null-Island-Guard Z. **21910**, `bindPopup` mit Status-Zeile Z. **21927**, Follow-Guard Z. **21934** |
| Leer-Banner | **21969** | nennt `fz_latest` / `sql/GPS_LATEST_v1.sql` |
| `fleet`-Ableitung (Liste) | **21945–21955** | Status je Fahrzeug + Sortierung `faehrt < steht < inaktiv < wartet` |
| Fleet-Liste (Panel) | **21977–21995** | Zähler-Zeile, Ampelpunkt, Status/Dauer rechts |
| `_showTrail` / `_clearTrail` | **21957–21958** | Trail lädt Historie und zieht daraus `_trailSeit` (exaktes „seit") |

DB: `sql/GPS_v1.sql` (Tabelle `fz_positions`), `sql/GPS_LATEST_v1.sql` (View `fz_latest`, `security_invoker`). Beide sind bereits ausgeführt.

**Tests** (`tests/`, alle direkt-runnable, Sentinel-/Quelltext-basiert):

- `test_flotte_helpers_v3645.py` (77 Z.)
- `test_flotte_fleetlist_v3655.py` (36 Z.)
- `test_flotte_gps_v3656.py` (34 Z.)
- `test_flotte_gps2_v3658.py` (35 Z.)
- `test_flotte_follow_v3660.py` (33 Z.)
- `test_flotte_datapath_v3663.py` (53 Z.)
- `test_flotte_nullisland_v3677.py` (32 Z.)
- `test_flotte_status_v3678.py` (299 Z., 25 Tests)

Für die Segment-Engine (v3.9.679) fehlt die Test-Datei noch — **erster Handgriff der nächsten Session**, bevor F2 darauf aufsetzt.

---

## 0b. NACHTRAG 14.07.2026 — SQL-Stand und Live-Test

### SQL Run-Gate

| Skript | Stand 14.07. |
|---|---|
| `sql/FZ_FAHRTEN_v1.sql` | **✅ gelaufen** (REST 200). Spalten live verifiziert, inkl. `start_ort`/`ziel_ort` — keine Altlasten `start_adresse`/`end_adresse`. |
| `sql/GEO_CACHE_v1.sql` | **✅ gelaufen** (REST 200). `geo_cache` da; die `ALTER`s für `start_ort`/`ziel_ort` an `fz_fahrten` sind durch. |
| `sql/MONTAGEZULAGE_v1.sql` | **⏳ ausstehend.** Liegt hash-verifiziert im SQL-Editor, Sebastians Run steht noch aus. Bis dahin ist die Tages-Vergabe im KV-Zulagen-Report gesperrt und die Montagezulage 0. |

**Das Fahrtenbuch ist damit vollständig scharf** — Persistenz, Projekt-Zuordnung und
Reverse-Geocoding sind funktionsfähig. Sichtbar ändert das nichts, solange `fz_positions` leer
ist: ohne Tracker keine Positionen, ohne Positionen keine Fahrten.

### Live-Test (Sebastian, 14.07.)

| Geprüft | Ergebnis |
|---|---|
| Barcode-Fix mobil (v3.9.680) | bestätigt |
| Zeiterfassung Mo–Sa (v3.9.682) | bestätigt |
| Urlaubs-Overlay, Referenzfall Günther KW29 (Mo+Di) | bestätigt |
| Fahrtenbuch (v3.9.681/683) | **by design nicht erreichbar** — der Einstieg hängt an den Karten-Controls, und die erscheinen nur, wenn es Fahrzeuge mit `tracker_imei` gibt. Ohne Tracker-Hardware also kein Zugang. Kein Bug. |

### Live-Verifikation v3.9.686 (14.07., ~17:30)

| Geprüft | Ergebnis |
|---|---|
| Planungs-Wetterzeile KW29 | **echte Werte**, deckungsgleich mit dem Home-Widget für Mi–Sa. Die Attrappe (4/2/7/5/6/3 °) ist weg. |
| Di: 1 ° Abweichung zum Home-Widget | **kein Mapping-Fehler, kein Fix nötig.** Beide Widgets rufen zu verschiedenen Zeitpunkten ab und Open-Meteo aktualisiert die Prognose laufend; die Planung cached zudem 15 Min. Ein Off-by-one hätte eine *ganze Tagesreihe* verschoben, nicht ein einzelnes Grad. |
| KW12 (weit außerhalb des Prognose-Fensters) | zeigt **„—"** statt erfundener Zahlen — genau wie vorgesehen. |
| Leaflet-Mausrad | zoomt beim Seiten-Scroll nicht mehr; erst nach Klick in die Karte. |

> **Konsequenz für F2/F3-Verifikation:** Fahrtenbuch, Geocoding und die Segmentierungs-Engine
> sind bis heute **ausschließlich durch Tests abgesichert**. Kein einziger echter GPS-Punkt, kein
> einziger echter Nominatim-Lookup ist je durch diese Kette gelaufen. Der erste Realitätstest ist
> der Traccar-Pilot — und dort wird sich auch zeigen, ob Nominatim für Kirchberger Baustellen
> brauchbare Namen liefert.

### Offene Punkte (Stand 14.07., unverändert)

- **E — `sync_supplier`:** Quelle der live laufenden **v9** ist ungeklärt. Sie liegt nicht im Repo
  (`ARCHITECTURE.md:144`). Ein „v3-Deploy" hätte eine produktive v9 mit unbekanntem Stand
  überschrieben. **Nichts anfassen, bis die Quelle geklärt ist.** Daneben existiert ein
  fachlich anderes `supplier-sync` (v2, `verify_jwt: true`) — nicht verwechseln.
- **Tracker-Bestellung:** 12× FMC003 + 1× FMC130 (LKW Scania). Sebastian bestellt in den
  kommenden Tagen.
- **Traccar-Pilot** — danach `gps_ingest` (Traccar → `fz_positions`), eigener Auftrag.
- **F4 Analyse** (Tageskilometer, Geschwindigkeitsdiagramm, Filtersuche/Favoriten).
- **KV-Punkte 2/3/4** — warten auf Format-Bestätigung.
- **Dispo 6→7 Tage** — Produktentscheid offen (Wochenplanung ist Mo–Sa, Zeiterfassung 7-tägig).
- **PAT ohne `workflow`-Scope** — `.github/workflows/*` bleibt darum tabu.

### Kommendes Thema: Stempeluhr

Die **Hardware wird bestellt**. Die **Softwareseite ist noch undefiniert** — Anforderungen folgen
von Sebastian. Bis dahin nichts bauen und nichts annehmen: es ist offen, ob das auf die bestehende
Stempel-Kiosk-Logik (`?screen=stempel`, v3.9.638/639/662) aufsetzt oder ein eigener Weg wird.

---

## 1. Was in dieser Session gebaut wurde

### v3.9.677 — F1a: eingefrorener GPS-WIP angewandt (`417dfd4`)

Quelle: `docs/wip/FLOTTE_GPS_WIP_2026-07.patch` (+ `.md`).

- **Null-Island- und NaN/Infinity-Guards** auf der Geo-Achse: `0/0` ist der Traccar-Default bei `valid=false` (Tracker bootet, hat noch keinen Fix). Ohne Guard zieht ein einziger 0/0-Punkt die `fitBounds` in den Golf von Guinea, und `L.marker([NaN,NaN])` wirft. Gefiltert wird an beiden Stellen: Marker-Aufbau (Z. 21910) und Live-Follow-Zentrierung (Z. 21934).
- **Leer-Banner** nennt jetzt die richtige View: `⚠ View fz_latest fehlt — sql/GPS_LATEST_v1.sql (braucht sql/GPS_v1.sql) ausführen` statt der alten `fz_positions`-Formulierung.
- Der Patch ließ sich **nicht** per `git apply` einspielen: Basis war `f182b40` (8 Tage Drift), die Versionszeilen (`SW_VER`, `APP_VERSION`-Changelog) kollidieren immer. **Drei Hunks von Hand nachgezogen.**
- `stash@{0}` auf dem srvdc02-Spiegel bleibt **unangetastet** — nichts daran gepoppt oder gedroppt.
- Neuer Guard-Test `test_flotte_nullisland_v3677.py`. pytest: **1178 passed**.

### v3.9.678 — F1b: Status-Modell (`8907af5`)

Vier Zustände statt bisher drei, jeweils mit „seit wann" und Dauer:

| Code | Bedeutung | Farbe |
|---|---|---|
| `faehrt` | `speed > 3 km/h` **oder** `ignition === true` | grün `#22c55e` |
| `steht` | sonst (Datensatz vorhanden, jünger als 24 h) | rot `#ef4444` |
| `inaktiv` | letzte Position älter als 24 h | grau `#9ca3af` |
| `wartet` | kein Datensatz (Tracker verbaut, nie gemeldet) | orange `#f59e0b` |

Die 3-km/h-Schwelle filtert GPS-Rauschen im Stand. Dauer-Format `HHH:MM` (Fink-Format, auch jenseits 99 h), „seit"-Format `HH:MM` bei heute, sonst `D.M. HH:MM`.

**Liste und Karten-Popup zeigen dieselbe Status-Zeile** (`Steht (13.7. 08:42) · 02:15`), Farben zentral aus `FZ_STATUS_COLOR` — damit Karte und Liste nicht zwei Wahrheiten erzählen.

`_flotteFetchTrack` lädt jetzt zusätzlich **`speed,ignition`**. Ohne die beiden Felder wäre in der Historie jeder Punkt „steht" und `_fzStatusSeit` immer falsch.

**Wichtig — „seit" ist in der LISTE eine Näherung.** Die Historie wird seit v3.9.663 nur **on-demand für das selektierte Fahrzeug** geladen (50 Trail-Requests für die Liste wären kein guter Tausch). In der Liste fällt „seit" darum auf den `ts` der letzten Position zurück: das sagt *„seit wann meldet er diesen Zustand"*, nicht *„seit wann besteht er"*. Für das **selektierte** Fahrzeug ist es exakt — `_trailSeit` aus `_fzStatusSeit(rows)`, gesetzt in `_showTrail` (Z. 21957), und sowohl Liste (Z. 21946) als auch Popup (Z. 21924) benutzen es dann.

**JS-Falle, dabei gefunden und gefixt:** `new Date(null).getTime()` ist **`0` (Epoch), nicht `NaN`**. Ohne expliziten null-Guard rutscht ein `ts:null` als „inaktiv seit 1.1.1970" durch, statt als „wartet" zu gelten. Guard sitzt in `_fzStatus` (Z. 21670).

Vier Bestandstests mussten nachgezogen werden (sie assertierten exakte Code-Zeilen im Wortlaut). pytest: **1203 passed** (25 neue Tests).

### v3.9.679 — F1c: Fahrt-Segmentierung (im Arbeitsbaum, Commit steht aus)

Reine Engine: **kein UI, kein DB-Zugriff, kein React** — bewusst. Das Fahrtenbuch (F2) setzt darauf auf.

- **Signal-Hierarchie:** **Zündung ist primär.** Der Teltonika FMC003 liefert per OBD ein echtes Zündungssignal; `ignition === false` beendet die Fahrt sofort (Motor aus = Fahrt zu Ende, auch mitten im Stau). **`speed` ist nur Fallback**, wenn das `ignition`-Feld fehlt (`null`/`undefined`, älterer Tracker) — dann trägt die Geschwindigkeit die Segmentierung allein.
- In **beiden** Pfaden beendet zusätzlich ein **Stillstand länger als `opts.stillMin` (default 5 min)** die Fahrt. Sonst gilt ein Fahrzeug, das mit laufendem Motor eine Stunde auf der Baustelle steht, als eine einzige Riesenfahrt. Derselbe Mechanismus fängt Signal-Lücken und den Übernacht-Fall ab.
- **Kilometer immer via Haversine** über die Punktkette. Der **OEM-Tacho (`odometer`) wandert nur als `tachoVon`/`tachoBis` mit** — er ist fahrzeugabhängig verfügbar und taugt darum nicht als Rechenbasis.
- **Mini-Fahrten unter `opts.minFahrtM` (default 200 m) werden verworfen** (Rangieren; Fink zeigt 0,026-km-Fahrten an, wir nicht).
- `_fzNormPos` normalisiert defensiv: kaputte `ts`, nicht-endliche Koordinaten und Null-Island (0/0) fliegen raus, aufsteigend sortiert.
- Segment-Form: `{beginn, ende, dauerMin, km, startPos, endPos, tachoVon, tachoBis}`, `km` auf 2 Nachkommastellen.

---

## 2. Offene Architektur-Entscheide

### Reverse-Geocoding — **ENTSCHIEDEN (13.07.): Nominatim (OSM), gratis.** Umsetzung in F3.

Spezifikation, wie sie zu bauen ist:

- Endpoint `nominatim.openstreetmap.org`, Query `format=jsonv2&zoom=17&accept-language=de`.
- **CSP:** `connect-src` um **genau diesen einen Host** erweitern — begründet wie beim Leaflet-Muster aus v3.9.645 (Tile-Hosts wurden dort ebenfalls einzeln freigegeben, kein Wildcard).
- **PFLICHT-Cache** wegen Nominatim-Fair-Use-Policy: Koordinaten auf **4 Dezimalstellen (~11 m)** gerundet als Key → Adresse. Persistenz in **IndexedDB** über das bestehende ODB-Muster.
- **Rate-Queue: max 1 req/s.** Bei Cache-Miss zunächst `…` anzeigen, nachziehen sobald die Antwort da ist.
- Anzeigeformat wie Fink: **„Ort, Straße Hausnr"** aus `address.road` + `house_number` + `town`/`village`/`city` — robust bei fehlenden Feldern (Landstraßen liefern oft keine Hausnummer, manchmal keinen `road`).
- Fehler/Timeout → **Koordinaten-Fallback**, niemals die UI blockieren.
- **Zukunft (nicht bauen, nur notieren):** Sobald Traccar/`gps_ingest` läuft, kann die Adresse **serverseitig beim Ingest** angereichert werden — Traccar kann Nominatim nativ. Das Client-Geocoding wird dann nur noch Fallback für Altbestand.

### Zweck privat/geschäftlich — **ENTSCHIEDEN (Sebastian, 13.07.2026): entfällt**

> „Zweck privat gibt es bei uns nicht, alles geschäftlich."

**Konsequenz für F2: kein `zweck`-Feld, kein Privat/Geschäftlich-Umschalter, kein Filter danach.**
Nicht „vorsichtshalber" einbauen — jede Fahrt ist geschäftlich. Das vereinfacht das Fahrtenbuch
spürbar: keine Zweck-Spalte, keine Zweck-Pflichtangabe, keine steuerliche Trennung im Export.

Falls sich das je ändert (Privatnutzung eines Firmenfahrzeugs), ist es ein Feld an der Fahrt und
kein Umbau — die Segmente tragen ohnehin schon Beginn/Ende/km/Fahrer.

### Kunden-/Projekt-Zuordnung je Fahrt + Persistenz — **weiterhin offen (Datenmodell)**

Heute existiert **nur `fz_positions`** — Fahrten werden zur **Laufzeit** segmentiert
(`_fzSegmente`), nicht persistiert.

**Frage an Sebastian:** Sollen Fahrten **persistiert** werden (dann DDL: `fz_fahrten` mit
`kunde_id`/`projekt_id`, Fahrer, Tacho, km) — oder bleibt es bei Laufzeit-Segmentierung, und die
Kundenzuordnung entsteht per Zeitfenster-Abgleich gegen die Wochenplanung (wer war an dem Tag auf
welcher Baustelle eingeteilt)? Letzteres käme ohne DDL aus und nutzt Daten, die es schon gibt.
Ohne diesen Entscheid ist F2 nur lesend baubar.

### ⚠-Warnsymbole — **ENTSCHIEDEN (Sebastian, 13.07.2026): werden NICHT nachgebaut**

Die Warnsymbole aus dem Fink-Portal brauchen wir nicht. **Nicht bauen, auch nicht als Platzhalter
oder „für später" vorbereiten.** Der Punkt ist erledigt, nicht vertagt — falls er je zurückkommt,
muss zuerst geklärt werden, was so ein Symbol überhaupt bedeuten soll (Geschwindigkeit? Geofence?
Wartung? Tracker-Fehler?). Ohne diese Antwort ist ein Warnsymbol nur ein hübsches Dreieck.

---

## 3. Hardware-Kontext (Einkaufsliste 03.07.2026)

- **13 Tracker gesamt = 12× Teltonika FMC003 (OBD) + 1× Teltonika FMC130 (festverdrahtet)** + 1NCE-SIMs, **Traccar auf srvdc02**, Pilot ca. **85–100 €**. **Bestellung ist noch offen.**
- FMC003 liefert per OBD: **Zündungssignal**, **OEM-Kilometerstand** (fahrzeugabhängig), **Tankfüllstand**.
- Vor dem Rollout zu klären:
  - **Teltonika-Kompatibilität je Fahrzeug-Baujahr** (OBD-Protokoll/PIDs).
  - **Portfreigabe + DynDNS** für Traccar auf srvdc02.
  - **§96-Zustimmungen** (Kontrollmaßnahme, Betriebsvereinbarung/Zustimmung nötig). Monteure sehen den Flotte-Tab **nicht** — Gate ist `isStaff`.
- **LKW Scania TU-83JM — ENTSCHIEDEN (Sebastian, 13.07.2026): wird getrackt, Pflicht, nicht optional.**
  Hardware: **Teltonika FMC130, festverdrahtet** (Dauerplus + Zündung + Masse, 12–24 V) statt des
  OBD-gebundenen FMC003. Damit ist die Einkaufsliste **12× FMC003 + 1× FMC130**.
  **Vor der Bestellung:** Baujahr/Typ des Scania prüfen — hat er einen Standard-OBD-Port, wäre auch
  ein FMC003 möglich; der **FMC130 ist die sichere Default-Wahl**, weil er nicht auf OBD angewiesen ist.
  **Für den Code irrelevant:** Traccar behandelt beide Geräte identisch, es braucht **keine
  Sonderbehandlung**. Der FMC130 liefert das Zündungssignal über die feste Verdrahtung — die
  Signal-Hierarchie der Segmentierungs-Engine (Zündung primär) greift also genauso.
- **Tankfüllstand: nicht gebaut.** Zukunftspunkt „Kopplung an die bestehende Tank-Kontrolle" (`tank_log`).

---

## 4. Nächste Phasen

- **F2 — Fahrtenbuch-UI: ERLEDIGT am 14.07.2026 (v3.9.681).** `FahrtenbuchView`, erreichbar über den
  Button „📖 Fahrtenbuch" in den Karten-Controls. Fahrtenliste je Fahrzeug mit Tagesgruppierung,
  Zeitraumfilter, km-/Dauer-Summe und nachträglicher Projekt-Zuordnung. Mobile von Anfang an als
  Karten statt Tabellenzeilen (Lehre aus dem Barcode-Bug v3.9.680).
  - Die Fahrten kommen **aus der Engine**, nicht aus der DB: bei jedem Laden wird `fz_positions`
    neu segmentiert. `fz_fahrten` hält nur Persistenz + Kundenzuordnung.
  - **Run-Gate offen:** `sql/FZ_FAHRTEN_v1.sql` ist gestaged, aber **nicht ausgeführt**. Bis dahin
    zeigt die Liste berechnete Fahrten mit Banner, eine Zuordnung ist nicht möglich.
  - **Write-on-Read, bewusst:** beim Laden werden neue Segmente idempotent hochgeschrieben
    (`on_conflict=fahrzeug_id,beginn`). Ohne diesen Schritt gäbe es nie eine Zeile, an der die
    Zuordnung hängen könnte. Konsequenz, die man kennen muss: **ändert jemand `stillMin` oder
    `minFahrtM`, verschieben sich Fahrt-Grenzen und es entstehen Karteileichen** mit alten
    `beginn`-Zeitpunkten. Wer an den Parametern dreht, muss `fz_fahrten` neu aufbauen.
  - **Noch offen aus F2:** Export (Excel/PDF) der Fahrtenliste, Trail je *einzelner* Fahrt (heute
    zeigt die Karte weiterhin die ganze 500-Punkte-Historie als eine Polyline).
- **F3 — Geocoding** nach der oben festgehaltenen Spezifikation (Nominatim + Pflicht-Cache + Rate-Queue).
- **F4 — Analyse:** Tageskilometer, Geschwindigkeitsdiagramm, Filtersuche/Favoriten.
- **Infrastruktur (eigener Auftrag): `gps_ingest` Edge Function** (Traccar → `fz_positions`). Ohne sie liefert `fz_positions` **weiterhin keine Live-Daten**. Die gesamte Flotte-UI muss mit leerer Tabelle sauber degradieren — und tut das aktuell auch (Leer-Banner, `wartet`-Zustand, keine Crashes).

---

## 5. Zustand / Gates

- **pytest: 1203 passed** (Stand v3.9.678, `8907af5`). Voller Lauf im Arbeitsklon `C:\repos\epkolar-app` in **unter 1 Minute** — auf Z: (Netzlaufwerk) dauerte derselbe Lauf unter Subagenten-Konkurrenz >60 min, darum wird nur noch im Klon getestet.
- v3.9.679 ist **noch nicht committet** (`index.html` modifiziert, +108 Zeilen) und hat **noch keinen Test**. Vor dem Commit: Test-Datei `test_flotte_segmente_v3679.py` schreiben, dann `SW_VER`/`APP_VERSION` auf 3.9.679 ziehen.
- Bracket-Baseline und „kein optional chaining"-Regel gelten in den Flotte-Blöcken wie überall — die neuen Helper sind bewusst ES5-nah geschrieben (`var`, `function`, kein `?.`).
- **Arbeitsstandard:** Arbeitsklon `C:\repos\epkolar-app`; srvdc02-Share ist nur Spiegel (`git pull`), **nie Push-Quelle**.
