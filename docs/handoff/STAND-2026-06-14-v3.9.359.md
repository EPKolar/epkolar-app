# EPKolar — Tagesabschluss 2026-06-14 · v3.9.359

**Live auf origin/main + GitHub Pages, curl-verifiziert.** Working tree clean. HEAD `b9f3f6c`.

## Heute gepusht (v3.9.352 → v3.9.359)

| Version | SHA | Inhalt | verifiziert |
|---|---|---|---|
| v3.9.352 | ba3a6cd | Fix-B Pre-Wipe-Drain-Guard (Lokale-Daten-löschen) | curl + Triade |
| v3.9.353 | 867fead | Bug-Hunt #5 Sentinel-Datum · #8 leere Projektnr-„()" · #9 PlanViewer-Warn | curl |
| v3.9.354 | 1b2c895 | #6 Wochenplanung read-only (Monteur) · #3 Glocke Dismissed-Gedächtnis · #7 Urlaub-Rundung | curl |
| v3.9.355 | 8b05c7d | #1 activity_log-Login-403 (client, return=minimal) · Büro-Export G1-Cleanup (Variante B) | curl + Playwright |
| v3.9.356 | b82a8c7 | Abwesenheits-Bilanz auf modul-level Single-Source-Helfer extrahiert (Vorbereitung) | **Playwright-Equivalenz** (barger Rest 192,5h unverändert) |
| v3.9.357 | d6199d3 | Büro-Export: klappbare Inline-BWB-Vorschau pro Projekt (KW×MA-Matrix) + Excel | **Playwright (schober)** |
| v3.9.358 | 5aa1c9c | SW-Cache-Fix (Navigation no-store) · #3 Abwesenheiten · #4 Krankenstand (read-only) | **Playwright (schober) + Network-Check** |
| v3.9.359 | b9f3f6c | Owner-Felder created_by/erstellt_von bei Anlage befüllen | Code-Pfad + DB-Spalten (read-only) |

## Büro-Export #1–#5 — DONE
- **#1 (G1, Variante B):** Projekt-Dropdown + globaler Button entfernt → „📥 Alle als Excel" im Sektionskopf; Zeilen-Excel=generateBWB; 0-Buchungen ausgegraut. ✓ live.
- **#2 KPI/Monteur-Übersicht:** unverändert (Styling-Glättung bewusst minimal gehalten).
- **#3 Abwesenheiten 2026** (alle MA, read-only): Gesamt/Urlaub/Krank/ZA/Sonder/Rest via modul-level `_yearStK`/`_resturlaubK` (kein Duplikat) + Excel + Link Urlaub-Modul. **Playwright-verifiziert (schober):** alle 10 MA; **Pinger Rest 90,0 exakt**. (Günther 103,5 statt Spec-102,5 = Client-Session-Approval-Staleness `absApprovals` prev-wins, v3.9.111 — identisch im Urlaub-Modul, kein #3-Bug; DB-Wahrheit 102,5 bei frischem Client.)
- **#4 Krankenstand 2026** (read-only): absences type=krankenstand + Attest-Status aus absence_files; Warnleiste. **Playwright-verifiziert:** „⚠️ 1 Mitarbeiter ohne Attest", Pinger 16.+17.04. ✓.
- **#5 (klappbare Inline-Vorschau):** ✓ live (KW×MA-Matrix, Σ 300,0 = Projekt-Total).
- **Safety:** Network-Check (schober) → beim Anschauen von #3/#4 **nur GET auf /absences, keine PATCH/POST/DELETE**. Büro-Export schreibt NICHT in absences.
- **Entscheidung (Chef):** Inline-Vorschau bleibt READ-ONLY — editierbares Tätigkeits-/Bautagebuch-Feld NICHT gebaut.

## Owner-Felder (v3.9.359) — ab jetzt befüllt
Beim **Anlegen** wird der Owner konsistent mit `curUser.monteurId` (= users.monteur_id) gesetzt, an den bestehenden Insert-Stellen:
- tickets, defects (createTicket + updateTicket-Typ→mangel) → `created_by`
- checklists (Template + Custom) → `created_by`
- forms (useEditable) → `created_by` (via `window._curUser().monteurId`, Hook ohne curUser-Prop)
- bautagebuch (neue Einträge) → `erstellt_von` (Edit-Branch behält Bestand)

**Verifiziert:** `_mapBody` ist Passthrough (created_by schlägt durch); alle 5 Zielspalten existieren (DB read-only). **Nur ab jetzt** — Alt-Leerwerte NICHT migriert/geraten.

## Offen — DB-Seite (Chat-Claude, CC fasst DB NICHT an)
- **Alt-Daten-Migration:** bestehende leere created_by/erstellt_von in defects/forms/tickets/checklists/bautagebuch — separates DB-Thema.
- **Owner-basierte Write-RLS** für diese 5 Tabellen erst möglich **NACHDEM** Owner-Felder befüllt (ab jetzt ✓) **UND** Alt-Zeilen migriert sind.
- **Heikle always-true-Tabellen bewusst offen, einzeln bewerten:** juprowa_log, notifications, as_vorlagen, fahrzeug_buchungen, fz_termine, worker_projects, wz_service, supplier_* (+ 2 SECURITY-DEFINER-Views supplier_articles_public/safe = Advisor-ERROR, von Sebastian gemanagt RUNBOOK v3953-v3959).
- **Bereits gehärtet (Chat-Claude heute):** gefahrstoff_files/folders, material_catalogs (Write=is_staff). Trigger-geschützt, NICHT anfassen: absences (guard_urlaub_edit), projects (guard_projects), users (guard_users_privilege). absences/time_entries SELECT row-scoped (DSGVO/Lohn) — **read-only verifiziert** (RLS-Sim: barger sieht nur eigene).

## DB-Zugriff-Status
Supabase-Plugin erreicht **korrekte** Org „EP Kolar & Sohn" / Projekt `jiggujpruejkaomgxarp` (alte „falsche-Org"-Warnung überholt). Heute aktuell: **CC = nur App, Chat-Claude = DB-RLS** (Koordination, kein Parallel-Edit-Konflikt).

## Offener Bug-Hunt-Backlog (CC-Code, deferred mit Begründung)
#2 Dashboard-Loading-Gate (Kern-Scoping, Live-Test nötig), #10 Wochenspann (Produktentscheidung Sonntag), Rest-„·"-Untertitel, Calendar-Chip „193h"-Rundung. Nicht gehuntet: Arbeitsschein Such-/Filter + Detail, Projekt-Sub-Tabs Mängel/Bautagebuch.

## Triade / Push / Schutz
node_check 0 · pytest **891** · `_check_version.js` 3.9.359. Push `git push origin main` (kein gh) + curl-Verify. Tank-Flow/Juprowa/`_RLS_SILENT_DENIAL_LABELS`/v3.9.306–324 unberührt. Backups: `_backups/epkolar_20260614_*.zip`.
