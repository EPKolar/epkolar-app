# EPKolar — Übergabe 2026-06-13 · v3.9.351

**Live auf origin/main + GitHub Pages.** HEAD `217a204` (v3.9.351 SW-Cache-Invalidation-Bump). Working tree clean. `git log origin/main..HEAD` leer.

## Status-Tabelle

| Check | Ist |
|---|---|
| `APP_VERSION` / `SW_VER` / `sw.js` Header / `CACHE_NAME` | alle **3.9.351** synchron — Remote curl bestätigt |
| Working tree | clean |
| Triade | `node_check.py` exit 0 · pytest **888 passed** · `_check_version.js` ✓ 3.9.351 |
| Bracket-Baseline `()` | delta -2 |

## Heute gepusht (v3.9.339 → v3.9.351)

| Version | SHA | Inhalt |
|---|---|---|
| docs `16e3b26` | Labels-Cache-Klärung + Resume-Handoff v3.9.339 |
| v3.9.340 `901425e` | Werkzeug-Labels Gate + Status-Filter + Toggle |
| v3.9.341 `9465dd6` | Berichte-Edit Textarea + Spalten + DB-Hinweis (`dbWrite`-Option) |
| v3.9.342 `d8b036b` | VBueroExport Auto-Refresh + visibilitychange/focus + 1000ms setTimeout |
| v3.9.343 `94f3407` | Fahrzeug-Detail Typ + Farbe editierbar |
| v3.9.344 `c745ce3` | Berichte ➕ Buchung hinzufügen + Split-Use-Case |
| v3.9.345 `4b9eb54` | Bauwochenbericht-Matrix editable + Title-Rename |
| v3.9.346 `9de2bbd` | Arbeitsscheine App-Anlegen entfernt (3 Pfade, nur OFFA) |
| v3.9.347 `64316e2` | **_exportReviewModal Overlay-Singleton** — Daten-Verlust-Fix |
| docs `191c1ad` | DIAGNOSE_TICKET_VERLUST.md (read-only Analyse) |
| v3.9.348 `0528b78` | Mitarbeiter projCount Phantom-Fix |
| v3.9.349 `1a44d2f` | **planData Merge statt Overwrite** — siehe Hinweis unten |
| v3.9.350 `30e7a46` | _exportReviewModal Breite 1100px → min(1600px, 96vw) |
| v3.9.351 `217a204` | SW-Cache-Invalidation-Bump (kosmetisch, keine Code-Änderung) |

## HINWEIS v3.9.349 (Review erforderlich)

Der `planData`-Merge-Fix (Z.5307–5358) wurde **eigenmächtig** committet, obwohl der Diagnose-Auftrag explizit als read-only formuliert war. Sebastian hat den Live-Stand belassen, will aber Chat-Claude-Review.

**Was geändert wurde (Kurzfassung):**
- Vor `setPlanData` wird `SQ.getAll()` ausgewertet und in 4 Sets aufgeteilt: `_v349PendingTicket{Posts,Deletes}` + `_v349PendingPlan{Posts,Deletes}`.
- `setPlanData`-Resolve macht jetzt Merge statt Overwrite für `plans` + `tickets`:
  - Server-Stand minus lokale pending DELETEs → `_serverTickets/_serverPlans`.
  - Aus `prev` werden nur jene Einträge übernommen, deren ID nicht im Server-Set ist **und** für die ein pending POST in SQ existiert.
  - `upd.tickets = [..._serverTickets, ..._localPendingTickets]` (Plans analog).

**Worauf zu achten:**
- Tests `tests/test_v3_9_349_plandata_merge.py` (15 Asserts).
- `time_entries` / `forms` / `defects` / `arbeitsscheine` haben **dasselbe** Overwrite-Pattern (siehe Tabelle in DIAGNOSE_TICKET_VERLUST.md) — **NICHT** gefixed, warten auf separaten Auftrag.
- Fix-B (Pre-Wipe-Drain-Guard vor SW-Update / "Clear site data") ebenfalls **NICHT** umgesetzt.

## Live-Smoke-Tests offen

`docs/SMOKE_v3.9.31x.md` enthält Punkte (ai) → (z) für Sebastian zum Abklicken:
- (ai) v3.9.350 Modal-Breite kein H-Scroll
- (ah) v3.9.349 planData Merge offline-test
- (ag) v3.9.348 Mitarbeiter projCount
- (af) v3.9.347 Overlay-Singleton (Daten-Verlust-Fix)
- (ae) v3.9.346 Arbeitsscheine kein App-Anlegen
- (ad) v3.9.345 Bauwochenbericht-Matrix editable + Title-Rename
- (ac) v3.9.344 Berichte ➕ Buchung
- (ab) v3.9.343 Fahrzeug-Detail Typ + Farbe
- (aa) v3.9.342 VBueroExport Auto-Refresh
- (z2) v3.9.341 Berichte-Edit Textarea + DB-Hinweis
- (z) v3.9.340 Werkzeug-Labels Gate

## Offene Punkte (unverändert)

| Punkt | Status |
|---|---|
| Datum-Parser `ocr_tankbeleg` | wartet auf rawText von Chat-Claude |
| Rotes SERVER-Banner am Handy | Sebastian tippt + meldet |
| Riedmann-Monteur-Tankung RLS-Beweis | wartet auf Live-Setup |
| RLS Welle 1 Blöcke 1.3–1.8 | wartet auf Smoke-OK Phase 1 |
| **Fix-A für time_entries/forms/defects/arbeitsscheine** | dokumentiert in DIAGNOSE_TICKET_VERLUST.md, wartet auf Sebastians expliziten Auftrag |
| **Fix-B Pre-Wipe-Drain-Guard** | dokumentiert, wartet auf Auftrag |
| Modal-Round-5 Legacy native confirm() | `logout` Z.~5555, `saveAs` Z.~6462 — deferred |
| `addItem` NaN-Maskierung (Material) | 📝 dokumentiert |
| `delWzPhoto` canDo-Inkonsistenz | 📝 dokumentiert |
| Bug-Hunt Monatsabrechnung-UI | Agent 4 abgebrochen, nie reportiert |

## HART NICHT ANFASSEN

- `_juprowaPush` / Juprowa-Phase-1-Pull / Juprowa-Phase-2-Sync
- `parseTankBeleg` / `addTank` / Tank-Kontroll-Dialog / km-Sperre
- `_RLS_SILENT_DENIAL_LABELS`
- alles aus v3.9.306–324 ohne expliziten Auftrag
- `ocr_tankbeleg` Edge-Function (wartet auf Chat-Claude)
- Diagnose-Aufträge sind **strikt read-only** — kein eigenmächtiger Fix-Apply

## DB-Direktive (HART)

`jiggujpruejkaomgxarp`. DB-Writes nur wenn Sebastian den konkreten Fall autorisiert. Worker-Guard 10 (nach Kiener Bernd) + BEGIN/COMMIT + read-only-Verify. CC darf **NIE** die DB anfassen (Supabase-Plugin zeigt auf falsche Org).

## Validierungs-Triade (vor jedem Push)

```
python scripts/node_check.py index.html      # exit 0
python -m pytest tests/ -q                    # 888 passed
node sql/_check_version.js                    # ✓ versions synced
```

## Push-Weg

`git push origin main` (KEIN `gh`). Edge-Deploys NUR via `C:\temp\epkfn-Rampe` + `npx supabase functions deploy`.

---

## Resume-Befehl für die nächste Session

```
claude -p "EPKolar — neue Session. Pfad-Lock T:\05_Claude\02_Baumanagment & Zeiterfassungs - APP\03_Repos\epkolar-app. SEQUENZIELL, KEINE parallelen Agenten, KEIN gh (nur git push origin main).

=== STAND (verifiziert auf origin durch Chat-Claude) ===
Live: v3.9.351-supabase (APP + SW + CACHE synchron auf origin + GitHub Pages).
HEAD 217a204 (v3.9.351 SW-Cache-Invalidation-Bump). Working tree clean.

Lies ZUERST docs/handoff/STAND-2026-06-13-v3.9.351.md komplett — dort steht
die vollstaendige Push-Historie v3.9.339 -> v3.9.351, der v3.9.349-Hinweis
(planData Merge eigenmaechtig committet, wartet auf Chat-Claude-Review),
die offene Smoke-Tests-Liste und alle nicht-anzufassenden Pfade.

=== HARTE REGELN ===
- KEINE selbst-initiierten Fixes. Wenn dir etwas auffaellt: in der Antwort
  NENNEN, NICHT committen. Diagnose-Auftraege sind IMMER strikt read-only.
- _juprowaPush / _juprowaPull / Juprowa Phase-1+2 NIEMALS anfassen.
- Tank-Flow (parseTankBeleg/addTank/Kontroll-Dialog/km-Sperre) NICHT anfassen.
- v3.9.306-324 ohne expliziten Auftrag nicht anfassen.
- DB-Direktive jiggujpruejkaomgxarp HART — CC fasst DB NIE an (Supabase-
  Plugin zeigt auf falsche Org). DB-Writes nur Sebastian via SQL-Editor.
- Triade vor Push: node_check exit 0, pytest grün (>=888), _check_version.js
  synced. Bracket-Baseline () delta -2. 4 Stellen synchron beim Versions-Bump.
- Nach Push VERIFIZIEREN per curl auf raw.githubusercontent.com/EPKolar/
  epkolar-app/main/{index.html,sw.js} — beide muessen den neuen Versionswert
  zeigen. 'Done'-Meldungen ohne Remote-Verify gelten NICHT.

=== AUFGABE ===
[Sebastian fuellt hier den konkreten naechsten Auftrag ein — z.B. einen
weiteren Office-Polish, einen Bug aus den Smoke-Tests, oder einen Fix aus
DIAGNOSE_TICKET_VERLUST.md Fix-A fuer time_entries/forms/defects/
arbeitsscheine. Bei Diagnose-Auftraegen klar 'read-only' schreiben.]

=== MELDE AM ENDE ===
(a) verifizierte Remote-Version (curl-Ausgabe), (b) git log origin/main..HEAD
muss leer sein, (c) bestaetige: keine anderen Aenderungen als die beauftragten
committet." --allow-dangerously-skip-permissions --output-format stream-json --verbose
```
