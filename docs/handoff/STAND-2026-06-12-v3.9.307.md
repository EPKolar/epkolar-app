# EPKolar — Übergabe 2026-06-12 · v3.9.307

**main HEAD = origin/main (after push).** Triade grün: `node_check.py` exit 0, Versionen synchron 3.9.307, pytest 722 passed.

## Heute (v3.9.307)
### ✏️ Edit-Button in `ZeiterfassungView` (Monteur-Wochenuebersicht)
Bisher: nur VZeit (Projekt-Tab, v3.9.301) hatte den Eintrag-Edit. Die Haupt-Wochenuebersicht für Monteure hatte nur ✕ (Delete) — Bürokraft konnte einen Tippfehler im Stundensatz / der Tätigkeit nur durch Delete-und-Neuanlegen korrigieren. Toter `editEntry/setEditEntry`-State war schon da, jetzt verwendet.

**Sichtbar (Z.17817):** ✏️-Span vor ✕-Span in der Top-Row jedes Entry-Cards, gated `canDo('zeit_other', curUser)` (Admin / Projektleiter / Büro). Style analog ✕ aber blau (`#3b82f6`) + Title „Bearbeiten (Büro/Admin)".

**Prefill (Z.17314 `_startEditEntry`):** Schreibt `editEntry={id}` + alle Add-Form-State-Setter (`setAddDay`, `setAddType`, `setAddId`, `setAddFrei`, `setAddTaetigkeit`, `setAddBemerkung`, `setAddHours`, `setAddVon`, `setAddBis`, `setAddPause`). Add-Type wird aus den Spalten geraten: `arbeitsschein_id` → 'schein', `project_id` → 'projekt', sonst 'frei'.

**Save-Branch in `addEntry` (Z.17340 ff):** Bei `editEntry` greift PUT statt POST + dreifacher Sync:
- (a) **DB** — `SQ.push PUT /api/entries/<id>` mit Body identisch zu `updateEntryHours` (id, worker_id, project_id, arbeitsschein_id, date, hours, taetigkeit, gewerk, bemerkung, von, bis, pause). `gewerk` wird vom alten Entry beibehalten (lookup über `dayEntries`).
- (b) **dayEntries lokal** — alter Entry aus altem Tag entfernen, neuer Entry im (potenziell geänderten) `addDay` einfügen. So funktioniert Datum-Wechsel ohne Doppelung.
- (c) **`setEntries`-Prop** — globale entries-Liste für VZeit/Dashboard syncen (`hours`, `stunden`, `taetigkeit`, `task`, `von`, `bis`, `pause`, `bemerkung`, `date`, `datum`, `p`, `pid`, `project_id`, `arbeitsschein_id`, `label`). Memory: SyncQueue-RLS-Denials werfen den Tank-/Fahrzeug-Toast (v3.9.306 #3) — Entries-Tabelle nicht betroffen.

**Modal-Anpassungen:**
- Header (Z.17626) dynamisch: „✏️ Eintrag bearbeiten" bei editEntry, sonst „+ Neuer Eintrag".
- ✕-Top-Button (Z.17627), Cancel (Z.17704), Submit (Z.17705): alle setzen `setEditEntry(null)`.
- Submit-Label: „💾 Änderungen speichern" bei Edit, sonst „✓ Eintrag speichern".

**canDo-Guard:** Defense-in-Depth — `_startEditEntry` UND `addEntry`-Edit-Branch prüfen `canDo('zeit_other', curUser)`. `zeit_other` = Admin / Projektleiter / Büro (Z.3803).

## DB-Direktive (HART, gilt weiter)
`jiggujpruejkaomgxarp`. DB-Writes nur wenn Sebastian den konkreten Fall autorisiert, **höchste Vorsicht um Nutzdaten**. `_juprowaPush` / Juprowa-Phase-1-Pull NIE anfassen.

## Offene Punkte (vom User heute angemeldet)
- **TXT-Exporte komplett raus** — werden nicht genutzt. Wahrscheinlich an mehreren Export-Stellen (Zeit/Lager/Fz/Projekt/Schein).
- **Büro-Export verbessern** — Plattform fürs Büro, hier muss alles editierbar/anpassbar sein (Zeiterfassung war Anfang — andere Bereiche analog auditieren).

## Validierungs-Triade (immer vor Commit)
```
python scripts/node_check.py index.html      # exit 0 = ok
python -m pytest tests/ -q                    # 722 passed (~108-128 s)
node sql/_check_version.js                    # ✓ versions synced: 3.9.307
```
Version-Bump 4 Stellen: `SW_VER` (index.html ~Z.15), `APP_VERSION` (~Z.2360), sw.js Header-Kommentar (Z.1), sw.js `CACHE_NAME` (Z.2).
