# EPKolar — Übergabe 2026-06-13 · v3.9.355

**Live + curl-verifiziert.** Working tree clean. Fortsetzung von v3.9.354 (Bug-Hunt-Fixes).

## Heute zusätzlich gepusht
| Version | SHA | Inhalt |
|---|---|---|
| v3.9.355 | `8b05c7d` | #1 activity_log-Login-403 (client) + Büro-Export G1-Cleanup (Variante B) |

### #1 activity_log Login-403 — GEFIXT (client, kein DB)
Login-Audit-Insert (Z.~2268) nutzt jetzt `return=minimal`. Ursache: `activity_log_select` ist staff-only → der RETURNING→SELECT-Back warf bei **jedem Monteur-Login** 42501. Insert selbst ist erlaubt (WITH CHECK=`authenticated`). Analog notifications-Fix v3.9.227.

### Büro-Export Umbau (Variante B)
- **G1 = JA** (verifiziert: `generateAll` = `ps.forEach(generateBWB)` — selber Generator wie per-Projekt-Excel-Buttons). → Redundanter Projekt-Dropdown + globaler Download-Button **entfernt**; globaler Download jetzt als **„📥 Alle als Excel"** im Bauwochenberichte-Sektionskopf. Per-Projekt-Export über Projektkarten; **0-Buchungen-Projekte schon ausgegraut/disabled** (Item 2 war bereits da). `selProj` via Karten-Klick.
- **G2 = NEIN → #4/#5 (Abwesenheiten-/Krankenstand-Blöcke) NICHT gebaut, gestoppt + gemeldet** (wie vorgegeben): `resturlaub`/`yearSt` sind **AbsView-intern** (hängen an `abs`-Map + `_resolveAppr`=Genehmigungslogik + `_wocheOf`/`stdVonTag`/`kontingent`); VBueroExport bekommt `absences` nicht mal als Prop. Ein read-only-Spiegel ohne (a) Duplizieren der verbotenen Genehmigungslogik oder (b) riskanten State-Lift-Refactor des HR-kritischen Urlaub-Moduls ist nicht sauber möglich — und read-only nicht live-testbar.
  - **Wenn #4/#5 gewünscht:** sauberer Weg = `yearSt`/`resturlaub` + Deps in **modulweite pure Helper** extrahieren (Params: absences-Map, approvals, kontingent, wocheOf, stdVonTag, m, yr) und aus AbsView **und** VBueroExport aufrufen → Single Source. Braucht sorgfältigen Live-Test (Urlaub-Modul = geld/HR-kritisch).
- Item 1 (KPI-Strip „Styling glätten") unverändert gelassen (funktional ok, rein subjektiv).

## DB-Status (read-only via Supabase-MCP verifiziert — kein Write nötig/gemacht)
- **Plugin erreicht die KORREKTE DB**: Org „EP Kolar & Sohn" (`ckdhpqcnlbqwvbcwtrul`), Projekt `jiggujpruejkaomgxarp` „Baumanagement & Zeiterfassung", ACTIVE_HEALTHY. **Die alte „falsche-Org"-Warnung ist überholt.**
- **#11 `absences`**: row-level RLS (`is_staff() OR worker_id=eigenes monteur_id`) — ✅ verifiziert.
- **#12 `time_entries`**: `te_read` = `is_staff() OR worker_id=current_monteur_id()` — ✅ **RLS-Simulation: barger sieht nur 13 Zeilen / nur w2** (vorher alle 52). Bereits gefixt (Chat-Claude nach Report). Helper `is_staff()`/`current_monteur_id()`/`is_hr()` sauber.
- **#1** war kein DB-Bug (s.o., client).
- Security-Advisors: 19× `rls_policy_always_true` (meist INSERT/ALL-**Write**: notif/supplier/material_catalogs/worker_projects/fz_termine/projects-write …) + 2× `security_definer_view` (ERROR). = Geschäftsdaten/Write-Lücken, **nicht Art.9**; einige evtl. bewusst geteilt. Offen für gezielte Härtung auf Ansage.

## Offen
- **DB-Härtung (auf Ansage, CC darf jetzt mit „wenn nötig"-Freigabe):** geschäftsdaten-Tabellen SELECT/Write je nach Soll-Sichtbarkeit; 2 SECURITY-DEFINER-Views (ERROR).
- **CC-Code deferred:** #2 Dashboard-Loading-Gate (Kern-Scoping, Live-Test nötig), #10 Wochenspann (Produktentscheidung Sonntag), Rest-„·"-Untertitel; #4/#5 Büro-Export (s. G2-Weg oben).
- **Noch nicht gehuntet:** Arbeitsschein Such-/Filter + Detail, Projekt-Sub-Tabs Mängel/Bautagebuch.

## Triade / Push / Schutz
node_check 0 · pytest **891** · `_check_version.js` 3.9.355. Push `git push origin main` (kein `gh`) + curl-Verify. Tank-Flow/`addParseTankBeleg`/km-Sperre, Juprowa-1/2, `_RLS_SILENT_DENIAL_LABELS`, v3.9.306–324 unberührt.
