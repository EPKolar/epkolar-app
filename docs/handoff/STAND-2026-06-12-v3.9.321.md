# EPKolar — Übergabe 2026-06-12 · v3.9.321

**main HEAD = origin/main (`5f4998f`).** Session 2 nach STAND-v3.9.318: weitere 4 Commits gepusht, Backlog leergeraeumt, Working Tree clean.

## Status-Tabelle

| Check | Ist |
|---|---|
| `APP_VERSION` / `SW_VER` / sw.js Header / `CACHE_NAME` | alle 3.9.321 synchron |
| Working tree | clean (alle alten Reports per .gitignore weg) |
| `git log origin/main..HEAD` | 0 ahead |
| `supabase/functions/ocr_tankbeleg/index.ts` letzter Commit | `c07e7d7` ACTIVE (deployed v4) |
| Triade | `node_check.py` exit 0 · `pytest` 721 passed · `_check_version.js` exit 0 |
| Bracket-Baseline `()` | delta -2 (Baseline -1, +1 Drift, JS valid) |

## Diese Session (seit v3.9.318)

| Version | Commit | Inhalt |
|---|---|---|
| v3.9.319 | 904b2f9 | PDF-Pendant Phase C (Urlaubskontingent + Fahrtenbuch + Auswertungen + Bauwochenbericht + Fuhrpark) — 5 weitere PDF-Buttons + 3 inline COMPANY_FOOTER.name-Refactors |
| v3.9.320 | 817e12b | COMPANY_FOOTER-Refactor: 13 hardcoded Firmen-Strings durch zentrale Konstante + 4 Helfer (CF_HTML_NAME / LINE1 / LINE2 / AGB) ersetzt |
| v3.9.321 | 1368272 | Dead-Code-Removal Tesseract (-_loadTesseract / _preprocessOcrImage / _tessPromise / _extractTankFields, ~60 Zeilen) |
| chore | 5f4998f | .gitignore: historische Overnight-Reports + Tooling-Artefakte ignorieren — Working tree clean |

## Smoke-Doc

→ `docs/SMOKE_v3.9.31x.md` jetzt mit 13 Testpunkten (a–m + 2 Bonus). Punkt (m) COMPANY_FOOTER-Konsistenz ergaenzt, (i) PDF auf Phase A+B+C aktualisiert.

## Backlog-Komplett-Status (per User-Auftrag „autonom bis leer")

✅ PDF Phase C
✅ COMPANY_FOOTER-Refactor
✅ Tesseract Dead-Code-Removal
✅ Untracked Files via .gitignore

❌ SERVER-Banner-Mystery — **Sebastians Punkt**, kein Befund vorhanden
❌ Riedmann-RLS-Test — **Sebastians Punkt**, braucht Live-Setup
❌ km-Parser Riedmann-Beleg — braucht `rawText` von Sebastian

## DB-Direktive (HART, gilt weiter)
`jiggujpruejkaomgxarp`. DB-Writes nur wenn Sebastian den konkreten Fall autorisiert. `_juprowaPush` / Juprowa-Phase-1-Pull NIE anfassen. Tank-Flow (parseTankBeleg/addTank/Kontroll-Dialog/km-Sperre/0-rows-Safeguard) ist **verifiziert-stabil** — nicht mehr refactoren.

## Was im aktuellen Stand neu spuerbar ist

- **Konsistente Firmenangaben** in allen Excel-Subtitles + PDF-Footers + Print-Outputs (Bautagebuch / Wochenplanung / Abwesenheit / Werkzeuge / Fuhrpark / Auswertungen / Bauwochenbericht / Mängel-PDF / Material-Warenkorb / Monatsabrechnung / FinkZeit / OFFA / EU-561-Bescheinigung). Telefon/UID-Update braucht jetzt eine Zeile statt 13.
- **PDF-Buttons** an 11 Stellen — Office kann jeden Excel-Export auch direkt drucken oder als PDF speichern (Browser-Native-Druckdialog).
- **Saubere Working-Tree** — kein Rauschen mehr in `git status`.

## Validierungs-Triade (vor jedem Commit)
```
python scripts/node_check.py index.html      # exit 0
python -m pytest tests/ -q                    # 721 passed (~115-135 s)
node sql/_check_version.js                    # ✓ versions synced
```
Version-Bump 4 Stellen: `SW_VER` (index.html ~Z.15), `APP_VERSION` (~Z.2360), sw.js Header-Kommentar (Z.1), sw.js `CACHE_NAME` (Z.2).
