# EPKolar — Übergabe 2026-06-12 · v3.9.305

**main HEAD `eb15551` = origin/main (in sync, 0/0).** Triade grün: `node_check.py` exit 0, Versionen synchron 3.9.305, Tank/Version-Tests 9 passed.

## Heute zuletzt erledigt (v3.9.305)
**Tankung-Speicherung gehärtet** (`index.html`, `addTank` ~Zeile 18036, jetzt `async`):
1. **Zahlen-Validierung** — Liter/Preis/km müssen Zahlen sein (keine Buchstaben). Regex-Guards:
   - Liter: `parseFloat>0`
   - Preis: `/^[0-9]+([.,][0-9]{1,2})?$/` (leer = 0 erlaubt)
   - km: `/^[0-9]+$/` (leer = kein km erlaubt)
   - Bei Ungültigkeit: Warn-Toast + Abbruch (kein Save).
2. **km-Sperre** — neuer km darf NIE kleiner als aktueller `f.kmStand` sein (`fahrzeuge.find(id)`-Lookup vor Save). Sonst Warn-Toast „km-Stand darf nicht kleiner sein als der aktuelle (NNNNN km)" + Abbruch. (Odometer monoton; `if(_km>f.kmStand)f.kmStand=_km` bleibt.)
3. **Kontroll-Fenster vor dem Speichern** — `window._confirmModal(_msg,{confirmLabel:"✓ Speichern",cancelLabel:"Zurück"})` zeigt Datum / Liter / Gesamtpreis / km zur Prüfung. `_confirmModal` rendert `white-space:pre-wrap` → `\n`-Mehrzeiler ok. Erst bei Bestätigung läuft `upd(...)` (Diff-PUT tank_log+km_stand, unverändert).

## Kontext der Tank-Kette (Vorgeschichte)
- **OCR Tankbelege:** läuft jetzt über **Google-Vision-Edge-Function** `supabase/functions/ocr_tankbeleg/index.ts` (deployed v3, ACTIVE, `verify_jwt:true`, `GOOGLE_VISION_KEY` gesetzt). `parseTankBeleg` (~17998) ruft `/functions/v1/ocr_tankbeleg` (15s-Timeout, offline/Fehler = kein Blocker, manuelle Eingabe bleibt). Felder kommen server-seitig geparst zurück (`{ok,datum,liter,preis,km,confidence,rawText}`).
- **km-Parser offen/pausiert:** Riedmann-„falsche km" — DB zeigte letzte echte Tankung (TU-794FZ, fahrer=w9) km=51740 KORREKT gespeichert. Der Fehlfall ist ein anderes Beleg-Layout. **Zum präzisen Fix fehlt der `rawText`** des fehlerhaften Belegs (von Sebastian anfordern, dann `parseTankbeleg` km-Regex in der Edge-Function justieren — 3-stufig, Zeile 40-46).
- **Stiller Tank-Verlust GESCHLOSSEN:** RLS-Policy `fahrzeuge_update_driver` (Fahrer darf eigenes Fahrzeug updaten) existiert in Prod (additiv, OR-kombiniert mit admin/PL/buero). Monteur-Tankungen kommen jetzt durch.
- `ocr-test.html` (Repo-Root): Standalone-Testseite, Vision-Modus + Tesseract-Vergleich.

## Offene Folge-Punkte
- **km-Parser Edge-Function:** Beleg-`rawText` von Riedmann besorgen → km-Regex justieren.
- Optional UX: `inputMode="numeric"`/`"decimal"` an den Tank-Form-Inputs (km/Liter/Preis) für mobile Ziffern-Tastatur — Save-seitige Sperre deckt die Anforderung aber bereits ab.
- Optional: client-seitiger 0-rows-Safeguard auf Tank-Save (RLS-Denied erkennen).

## DB-Direktive (HART, gilt weiter)
`jiggujpruejkaomgxarp`. DB-Writes nur wenn Sebastian den konkreten Fall autorisiert, **höchste Vorsicht um Nutzdaten** (additiv bevorzugt, NIE blind UPDATE/DELETE/DROP), Worker-Guard (w1–w9 = 9) + BEGIN/COMMIT + read-only-Verify. Read-only-SELECT für Diagnose frei. `_juprowaPush` / Juprowa-Phase-1-Pull NIE anfassen. Siehe Memory `epkolar-db-write-stop`.

## Validierungs-Triade (immer vor Commit)
```
python scripts/node_check.py index.html      # exit 0 = ok (autoritativ; node --check direkt auf .html geht NICHT)
python -m pytest tests/ -q                    # ~720 passed
node sql/_check_version.js                    # Versionen synchron
```
Version-Bump 4 Stellen: `SW_VER` (index.html ~Z.15), `APP_VERSION` (~Z.2353), sw.js Header-Kommentar (Z.1), sw.js `CACHE_NAME` (Z.2).
