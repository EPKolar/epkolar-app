# EPKolar — Übergabe 2026-06-12 · v3.9.318

**main HEAD = origin/main (`e660a5b`).** Heute 13 Commits + 1 Edge-Function-Fix gepusht und (für ocr_tankbeleg) deployed.

## Status-Tabelle

| Check | Ist |
|---|---|
| `APP_VERSION` / `SW_VER` / sw.js Header / `CACHE_NAME` | alle 3.9.318 synchron |
| Working tree (modified tracked files) | clean |
| `git log origin/main..HEAD` | 0 ahead |
| `supabase/functions/ocr_tankbeleg/index.ts` letzter Commit | `c07e7d7` (zeilenbasierter km-Parser) deployed (`ACTIVE` v4) |
| Triade | `node_check.py` exit 0 · `pytest` 721 passed · `_check_version.js` exit 0 |
| Bracket-Baseline `()` | delta -2 (Baseline war -1 → +1 Drift; JS valid) |

## Heute auf origin

| Version | Commit | Inhalt |
|---|---|---|
| v3.9.306 | 352d463 | Tank-Modal editierbar + 0-rows-Safeguard + km-Inputs auf numeric |
| v3.9.307 | 852f56f | Edit-Button ✏️ in ZeiterfassungView |
| v3.9.308 | e4134f0 | Alle TXT-Exporte raus |
| v3.9.309 | 55ef060 | Kachel-Scroll-to-Schein |
| – | c07e7d7 | km-Parser server-seitig zeilenbasiert (Edge-Function) |
| v3.9.310 | 0582d59 | parseTankBeleg km-Plausi-Guard |
| v3.9.311 | b90c96b | VExport-Vollständigkeitsprüfung + MitarbeiterView-Aggregator (gebündelt) |
| v3.9.312 | a4b2dbf | Version-Sync |
| v3.9.313 | bf55131 | Tab-Swipe-Fix (touch-action + Clamp) |
| v3.9.314 | f7e6a8c | Kundenportal-Anonymisierung |
| v3.9.315 | 9463b5a | PDF-Foundation: COMPANY_FOOTER + globales Print-CSS + 3 PDF-Buttons |
| v3.9.316 | b2e7c68 | Mobile Projekt-Navzeile lesbarer (Icons + Text + dicker Indicator) |
| v3.9.317 | ff0e449 | ProjList Status-Badges (⚠️ Mängel / 📋 Regie / 🤝 Abnahme) |
| v3.9.318 | e660a5b | PDF-Pendant Phase B (Abwesenheit + Monteur-KW + Werkzeuge) |
| docs | ae512f3 | Smoke-Checkliste v3.9.31x |

## Smoke-Doc

→ `docs/SMOKE_v3.9.31x.md` (12 Testpunkte: a–l + 2 Bonus). Bei jedem Feature-Commit heute ergänzt.

## Offene Punkte (NICHT erledigt, deferred)

- **PDF-Pendant Phase C** (OFFA / Projekt-genCsv / Fahrtenbuch / Auswertungen / Bauwochenbericht) — kein expliziter Office-Druckbedarf gemeldet, wird bei Bedarf nachgereicht.
- **COMPANY_FOOTER-Refactor** der hardcoded Firmen-Strings in `genXls` / `genFormPdf` / `printPdf` / `exportMonat` / `exportFinkZeit` — Konstante ist eingebaut, Migration der ~7 Stellen ist nachzuschieben.
- **Rotes „SERVER"-Banner am Handy** — Ursache unklar, Sebastian tippt drauf und meldet Banner-Text.
- **Riedmann-Monteur-Tankung als RLS-Beweis** — bitte gezielt re-testen sobald Riedmann-Zuordnung wieder live ist (0-rows-Safeguard greift jetzt).
- **km-Parser Riedmann-Beleg** — Original-`rawText` fehlt für Final-Regex-Justierung (server-seitig zeilenbasiert seit c07e7d7).
- **Aufräumen Tesseract-Helper** (`_loadTesseract` / `_preprocessOcrImage` / `_extractTankFields`) + `ocr-test.html` — seit v3.9.304 nicht mehr aktiv; löschen oder Fallback behalten, Entscheidung offen.
- **Untracked Files** im Repo-Root (CHANGELOG_v3.8.*, NACHT_REPORT_*, SPRINT_FINDINGS_*, `_po.txt`, `_run_nacht_v3867.ps1`, `bughunt-fix-v2.zip`) — historisch, kann archiviert oder gelöscht werden.

## DB-Direktive (HART, gilt weiter)
`jiggujpruejkaomgxarp`. DB-Writes nur wenn Sebastian den konkreten Fall autorisiert. `_juprowaPush` / Juprowa-Phase-1-Pull NIE anfassen. Tank-Flow (parseTankBeleg/addTank/Kontroll-Dialog/km-Sperre/0-rows-Safeguard) ist verifiziert-stabil — **NICHT** mehr refactoren, nur bei explizitem Bug.

## Validierungs-Triade (vor jedem Commit)
```
python scripts/node_check.py index.html      # exit 0
python -m pytest tests/ -q                    # 721 passed (~110-135 s)
node sql/_check_version.js                    # ✓ versions synced
```
Version-Bump 4 Stellen: `SW_VER` (index.html ~Z.15), `APP_VERSION` (~Z.2360), sw.js Header-Kommentar (Z.1), sw.js `CACHE_NAME` (Z.2).
